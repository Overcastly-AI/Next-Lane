# Patterns to ignore when packaging the chart.
.DS_Store
.git/
.gitignore
*.tmproj
*.bak
*.swp
.idea/
.vscode/
# Example values are docs, not part of the packaged chart defaults.
values-*.example.yaml
