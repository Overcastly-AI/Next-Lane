# Next Lane — Dev Board (Backlog)

Single prioritized board maintained by the **backlog-groomer**, fed by the two independent auditors (`docs/AUDIT-PRODUCT.md`, `docs/AUDIT-ENGINEERING.md`), the front-end QA review (`docs/UI-REVIEW.md`), and the roadmap (`docs/ROADMAP.md`). The autonomous build loop pulls from **Ready (top of queue)**.

Format: `- [ ] (P1, M) title — description [src]` · P0 critical / P1 now / P2 next / P3 later · size S/M/L. Checked `[x]` = done.

> **Better-than-Jira gaps** (vision-steward, Pass-12, 2026-07-02) — per the
> founder's operating question ("Is this better than Jira?" — `docs/VISION.md`
> § The operating question), the full honest scorecard lives in
> `docs/VISION.md` § Better-than-Jira scorecard, re-scored this pass against
> the two independent Pass-12 audits: **4 dimensions better, 3 parity, 3
> behind** (up from 3/2/5 pre-Pass-12). Search & query power moved
> Parity → Better (NLQL now spans board grouping, saved filters, automations,
> *and* dashboards). Reporting and Reliability/coherence-of-state both moved
> Behind → Parity (dashboards Phase 1 verified strong with real data; the
> Pass-11 workspace-chip coherence bug is structurally fixed via
> route-derived layouts). The three remaining **Behind** rows are the
> backlog-groomer's standing priority, sequenced per `docs/ROADMAP.md` §
> Current focus's Pass-12 order:
> 1. **Mobile** — this pass's own flagship feature (Swimlanes v2) shipped a
>    fresh P1 regression (invisible/touch-unusable board-toolbar dropdown
>    menus at 393px, plus a silently-clipped quick-filter chip row) on top of
>    the pre-existing "no native app" gap — mobile went from statically
>    behind to actively regressing. **Fix batch shipped 2026-07-02** (see the
>    ticked Ready-queue entries below); the underlying "no native app" gap is
>    unchanged and still the reason this row stays Behind overall.
> 2. **Admin controls** — SSO/OIDC Phase 1 shipped and is correctly gated;
>    the in-app SSO configuration screen (`/admin/sso`, instance-admin gated,
>    encrypted-at-rest, live no-restart effect) **shipped 2026-07-02** — see
>    the ticked Ready-queue entry below — closing the first of the two
>    blockers an evaluating enterprise/agency admin checks first. The second,
>    per-project role override (`ProjectMembership` layer, schema-confirmed
>    absent since Pass 9), had its **schema + backend slice shipped
>    2026-07-02** (elevate/restrict a workspace member's role scoped to one
>    project, REST + MCP surface) — see the ticked Ready-queue entry below;
>    only its frontend UI companion slice (a per-project Members panel)
>    remains open, so this row stays Behind on end-user-visible grounds until
>    that lands.
> 3. **Integrations** — GitHub v1 confirmed genuinely working end-to-end (a
>    real signed-webhook round trip, not a mock); **GitLab v1 shipped
>    2026-07-03** (real token-verified webhook round trip, live-verified
>    against the running API — see the ticked Ready-queue entry below) — still
>    missing auto-transition-on-merge, live PR/CI status, smart-commits, and
>    Gitea (tracked as a new Next item) — the incumbent's day-one SCM feature
>    set is closing but not yet fully matched.

> **Founder-flagged discoverability finding (vision-steward, 2026-07-02) —
> RESOLVED.** Three separately-reported "lost features" (workspace branding,
> board default filters, the Gantt-style roadmap timeline) were confirmed
> already shipped but unreachable without knowing where to look; root-caused
> to the lack of a persistent nav/IA surface. The Navigation & IA overhaul
> (Phases 1+2) and a light/dark mode pass shipped 2026-07-02 and were
> independently re-verified by the Pass-12 product audit as closing this
> exact complaint (`docs/VISION.md` § Better-than-Jira scorecard, Onboarding
> row). CSV export column-completeness (also founder-flagged the same
> session) shipped the same day. See the ticked ✅ entries below for detail.

> **Founder Q&A + directives (vision-steward, 2026-07-02) — RESOLVED.** "Are
> DB schema overhauls needed?" — answered no; the wave since has been small
> additive tables only (`Dashboard`/`DashboardGadget`, `GithubIntegration`/
> `IssueGithubLink`, `Board.defaultGroupBy`), the existing Prisma model
> holds. All four founder directives shipped: Kanban sections keyed off
> arbitrary fields ("Swimlanes v2"), dedicated Settings + Workflows
> robustness passes, NLQL-native configurable dashboards (every gadget is an
> NLQL query + a visualization), and an MCP coverage parity sweep (two sweeps
> this wave took the server 55→76→**85** tools, with a standing rule that
> every feature's definition of done includes MCP exposure where sensible).
> See the ticked ✅ entries below for full detail.

> **Groom state (2026-07-06, Hardening Night close-out — this pass):**
> Reconciled `git log` for tonight's shipments against the board and found
> almost all of it **already correctly ticked** by the build agents'
> own same-commit doc updates — the epic-lane e2e test (`b40fbbf`), the
> `@RequireScope` PAT-scope rollout completion (`4aec12a`), the
> docker-compose env-passthrough fix, the frontend P1 batch (mobile
> toast/Modal, dark-mode "Merged" badge, dashboard `?dashboard=` URL param,
> drag-handle tap target, live-status spinner), and security wave 2
> (SSRF DNS-pin, multer/lodash CVE overrides, issue-create `$transaction`
> hardening) all had entries in "Already Done" before this pass started.
> **Fixed the one gap:** the Ready-queue's "Epic-swimlanes e2e coverage gap"
> item was still unticked even though it shipped tonight — ticked, dropped
> from the queue, and cross-referenced into the original fix's "Already
> Done" entry. **Re-sequenced Ready** per the Better-than-Jira switcher-impact
> test (see the queue's own intro note): NLQL unresolved-name diagnostics
> first (cheap, fully unblocked), then Gitea v1 and dashboard public embed,
> then cross-project MOVE last (still founder-decision-gated, kept visible
> rather than silently dropped). **Ingested 16 new items** from tonight's
> inputs, deduped against the existing board (none were already filed):
> a dynamic PAT-scope route-coverage test (code-reviewer-endorsed) and a
> config-parity CI smoke test for `docker-compose.yml` vs `.env.example`
> (both structural closers named in `docs/AUDIT-ENGINEERING.md` pass 13's
> ideation section — the config-parity one is P1, filed at the top of
> "Next (P1)"); three `docs/MCP-QA.md` pass 2 P2/P3 findings (`bulk_update_
> issues` `expectedProjectKey` + echo, `list_project_activity` raw-cuid
> summaries, malformed-cursor 400); a GitLab auto-transition e2e parity gap
> (qa-tester P3); the remaining `pnpm audit` moderates split into an
> `@nestjs/core` v10→v11 major (its own P3/M ticket, not bundled) and a
> `file-type`+`js-yaml`+`qs` dependency-refresh bundle (P3/S); two
> undone audit pass 13 "Later" items (`GITLAB_TOKEN_ENCRYPTION_KEY`
> docs, the swallowed `bulkUpdateAtomic` catch); and six
> `docs/UI-REVIEW.md` 2026-07-06 P2/P3 findings not closed by the frontend
> P1 batch (`Badge` variant consolidation, `Badge` custom-color dark-mode
> awareness, `ink-300` contrast, Development-section naming, the
> starter-gadget first-run hint, and the staleness-pill touch tooltip).
> Per this task's explicit scope, only `docs/BACKLOG.md` was touched this
> pass — `docs/ROADMAP.md`/`docs/VISION.md` reconciliation is a concurrent
> doc-syncer's territory tonight. Pruned this header's Pass-13 prose (fully
> preserved in the Changelog at the bottom) to keep it from growing
> unbounded.

---

## Ready (top of queue)

**Re-sequenced 2026-07-06 (Hardening Night close-out groom).** The
epic-swimlanes e2e coverage gap that sat here shipped tonight (`b40fbbf`
— see "Already Done" below) and drops off the queue. Per the
Better-than-Jira switcher-impact test (`docs/VISION.md`): the cheap,
fully-unblocked NLQL fix goes first, then the two remaining ready
extensions, with the founder-decision-gated cross-project MOVE kept
visible at the back rather than silently dropped:

1. **NLQL: unresolved name → 400/warn instead of silent-empty** (P2, S) —
   cheap, closes the one deliberately-deferred half of the shipped
   NLQL name-resolution fix; a wrong-but-confident answer is worse for an
   agent than a rejected query.
2. **Gitea integration v1** (P2, M) — third self-hosted forge; GitHub+GitLab
   already cover most of the "Integrations" gap, so this is lower-leverage
   than the item above but still the next ROADMAP Phase-9 slice.
3. **Dashboard sharing — public read-only embed** (P2, M) — cheap extension
   of a proven-strong Phase 1/2.
4. **Cross-project issue MOVE** (P2, M, decision-needed) — real,
   frequently-hit parity gap (MCP-QA finding 4, product-wide) but needs a
   founder/product call on re-keying/sub-task semantics before it's
   buildable; filed visibly rather than silently blocked.

- [x] (P1, M) **PR-status + auto-transition-on-merge, with a board-card "linked PR" badge — SHIPPED 2026-07-03** — now that the GitHub v1 linking plumbing is confirmed solid (real signature verification, correct idempotent upsert), the highest-leverage next slice is making the existing links *act*, not adding more link types (AUDIT-PRODUCT.md Pass 12, Ideation #2). **Shipped scope:** (1) a per-project config toggle (off by default, both GitHub and GitLab) — a `merged` PR/MR webhook event moves the linked issue(s) to a configurable target status, reusing `IssuesService.move()`'s existing workflow-transition enforcement path via the automation-bypass flag (`{automated:true}`), the exact mechanism the automation engine's own TRANSITION action uses; a token-free `PATCH /projects/:projectId/{github,gitlab}/automation` (separate from the full repo/token save, so flipping the toggle never forces re-entering the PAT); no natural human actor for a webhook-triggered move, so a new `common/automation-actor.util.ts#resolveAutomationActor` falls back assignee → reporter → project lead → longest-tenured workspace ADMIN, verified against real membership, with each issue's transition independently try/caught so one ineligible actor never blocks a sibling issue or fails the webhook response; idempotent (an issue already at the target status is skipped, never a redundant rank shuffle) — verified with a re-delivered-webhook case. (2) a board-card "linked PR" badge — `board.service.ts`'s `issueInclude` gained a compact `githubLinks`/`gitlabLinks` state-only select (the `parent` include from `503b48d` deliberately left undisturbed), aggregated into `IssueDto.prLinkSummary: {open, merged}` by `issue.mapper.ts`, rendered as a small emerald "PR"/purple "Merged" badge on `IssueCard.tsx` mirroring the existing blocked-issue badge pattern exactly. (3) live PR/CI status in the issue drawer's Development section — the first REAL outbound calls through the previously-seamed-but-unused `GithubClient`/`GitlabClient` (`getPullRequestStatus`/`getMergeRequestStatus`, polled on drawer open via `GET /issues/:issueId/{github,gitlab}-links/live`), rendering a small CI-checks dot per PR/MR row and degrading gracefully (a quiet "live status unavailable" hint, never a hard failure) when the live call fails — live-verified in this network-isolated environment: `{"error":"GitHub API unreachable"}` per-link, exactly as designed. **SSRF (mandatory, done):** both clients' every outbound call goes through the shared `resolveAndCheckBlocked()` DNS-preflight + `redirect:'manual'` guard reused (imported, not duplicated) from `webhooks.service.ts` — defense-in-depth for GitHub's fixed host, the PRIMARY mitigation for GitLab's admin-supplied self-hosted `gitlabBaseUrl` (unit-tested with a mocked DNS resolution returning a blocked IP, confirming `fetch` is never called). **Tests:** 69 new API unit tests (1731→1800, all green) — disabled-by-default, automation-bypass interaction, idempotent no-op, actor-fallback chain, one-issue-failure-doesn't-block-sibling, wrong-project statusId rejected, board-payload `prLinkSummary` aggregation (open/merged/closed-excluded/relation-not-loaded-stays-undefined), both clients' live-status methods (SSRF-guarded, DNS-mocked, zero real egress); 4 new tenant-isolation-matrix rows (108/108 BLOCKED); `apps/web/e2e/pr-auto-transition.spec.ts` (6 cases × desktop+mobile, all green) — badge open→merged flip, settings toggle persists across reload, a **locally-HMAC-signed `merged` webhook drives a real status transition** (asserted against the REST issue endpoint, not just the UI), disabled-by-default regression case, mobile badge-renders-without-overflow; full regression re-verified green (`github-integration.spec.ts`, `gitlab-integration.spec.ts`, `board.spec.ts`, `settings-robustness.spec.ts`, `issue-detail.spec.ts`, `issue-drawer-overlay.spec.ts`). **Live end-to-end verification** (real running API + Postgres): registered a user → connected GitHub → enabled auto-transition targeting "Done" → posted a locally-signed `merged` PR webhook → the issue's `statusId` flipped to "Done" in the same response cycle; board payload showed `prLinkSummary: {open:0, merged:1}`; live-status endpoint correctly degraded with no real GitHub egress. **MCP:** 6 new tools — `get_issue_github_live_status`/`get_issue_gitlab_live_status` (real live-status reads) and `get_github_automation_config`/`get_gitlab_automation_config` (deliberately narrower than the REST GET — never return the webhook secret/PAT, even to an admin-scoped token) and `set_github_automation_config`/`set_gitlab_automation_config` (config-only writes, ADMIN-gated); connecting the repo/token itself remains web-UI-only (unchanged, secret-bearing). 104 tools total (was 98). [product-auditor Pass-12 Ideation #2 / Backlog-Groomer Ingest; ROADMAP Phase 9]
- [x] (P1, M) **Configurable dashboards — Phase 2: cross-sprint trend + cross-workspace scoping + gadget caps — SHIPPED 2026-07-03** — Phase 1 verified strong (AUDIT-PRODUCT.md Pass 12, 8/10); this is what flipped Reporting Behind → Parity, and the natural next increment given that strength (not a new pillar). **Shipped scope:** (1) **cross-sprint velocity trend** — `GET /projects/:id/reports/velocity-trend?sprints=N` (default 6, clamped 1-24; `ReportsService.velocityTrend` queries the N most-recent ACTIVE/COMPLETED sprints DESC + reverses to oldest→newest, mirroring `velocity()`'s committed/completed derivation) rendered as a new `VELOCITY_TREND` gadget visualization — project-wide by design (the gadget's NLQL `query` is validated but not applied, since there's no single issue set to filter across every sprint's own issues) — reuses the exact same `VelocityChart` component the Reports page uses (genuinely "the gadget framework reusing an existing chart", not a bespoke report page), with a `config.sprints` picker in the gadget form. (2) **cross-workspace gadget scoping** — audited the existing implementation (already correctly project-derived, never keyed off the app's global "active workspace" selection) and closed the *verification* gap the audit actually flagged: a new e2e case proves a user in two workspaces gets fully correct, non-leaking dashboard content and editability when deep-linking into a project in the NON-active workspace (explicitly set workspace A active via the Pulse page's selector, then deep-link into workspace B's project — correct tabs/gadgets/write-affordances, zero cross-tenant leakage). Also caught and fixed a **real, previously-uncaught race** while building this: creating a second dashboard while one was already selected briefly snapped the selection back to dashboard #1 before the list-refetch landed (`DashboardsPage`'s "reset selection if not in the list" effect saw a stale pre-refetch list) — fixed by having `useCreateDashboard`'s `onSuccess` synchronously append the new dashboard to the cached list instead of only invalidating (async). (3) **Phase-1 scope deltas revisited**: drag-to-reorder gadgets — v1's up/down buttons replaced with a real dnd-kit sortable grid (`rectSortingStrategy`, a dedicated grab handle so Edit/Delete stay clickable), using a numeric fractional-midpoint `config.position` (deliberately NOT the string `rankBetween` helper — a JSON-embedded, low-cardinality field where legacy integer positions aren't valid fractional-indexing keys; the midpoint-of-neighbors approach keeps the same "touch only the moved row" guarantee) computed client-side and PATCHed for the one moved gadget only, with optimistic cache reordering; a project's first-ever dashboard is now pre-populated with 3 starter gadgets (Open issues / Status overview / My open issues) — every dashboard after that still starts empty. (4) **engineering hardening**: `MAX_DASHBOARDS_PER_PROJECT` (20) / `MAX_GADGETS_PER_DASHBOARD` (30) caps (`BadRequestException` before insert, mirroring `DASHBOARD_ISSUES_CAP`) and `getDashboardData`'s per-gadget evaluation loop parallelized with `Promise.all` (was sequential — up to ~200 serial NLQL evaluations per read before the cap existed). **Tests:** new API unit tests for cap rejection (dashboards + gadgets), default-gadget seeding, `VELOCITY_TREND` gadget evaluation (ignores query, defaults to 6 sprints, per-gadget error on failure) and `velocityTrend` aggregation/clamping (API suite 1731→1800, shared with the sibling PR-auto-transition batch landing the same day); 1 new tenant-isolation-matrix row for the trend endpoint (108/108 blocked); `apps/web/e2e/dashboards-phase2.spec.ts` (6 cases × desktop+mobile, all green) — trend gadget rendering (real sprint data, reuses `VelocityChart`), default-gadget seeding + second-dashboard-stays-empty, drag-to-reorder via a real `page.mouse` sequence (Playwright's own `dragTo` doesn't trigger dnd-kit, which listens on Pointer Events) with a `waitForResponse` guard so a reload-immediately-after-drag assertion can't race the async PATCH, and the cross-workspace scoping case; `dashboards.spec.ts` (Phase 1, 12 cases) regression re-verified green. **MCP:** new `get_velocity_trend_report` tool; `dashboardVisualizationEnum`/`dashboardGadgetConfigSchema` extended with `VELOCITY_TREND`/`sprints`/fractional `position`; `create_dashboard`/`create_dashboard_gadget` descriptions updated to name the caps proactively (cap-rejection 400s already surface the API's own message verbatim through the existing error-propagation path) — 104 tools total (was 103 after the sibling PR-auto-transition batch; +1 from this pass). [product-auditor Pass 8/9/10/11/12 flat-3→closed carryover; engineering-auditor Pass-12 P2-2; VISION.md § Better-than-Jira gaps #4]
- [ ] (P2, S) **NLQL: an unresolved assignee/sprint name silently matches zero issues instead of erroring** — the deliberately-deferred half of the shipped NLQL name-resolution fix (`ec9f02e`; `docs/MCP-QA.md` pass 1, finding 1): `assignee = "Alex Rivera"` and `sprint = "July-B"` now correctly resolve when the name is real, but a *typo'd or nonexistent* name still returns a confident, silent `0 results` instead of a 400 — an agent (or a human) can walk away believing "Alex has nothing assigned" when the real answer is "there is no Alex." **Acceptance:** a `user`- or `sprint`-kind comparison whose value resolves to no known entity in `ctx.users`/`ctx.sprints` returns a 400 ("unknown user 'Alex Rivera'; use an id from `list_users`/`me()`" — mirroring the MCP-QA-suggested error shape) instead of a silent empty match, applied consistently across all three server evaluation call sites (`exportCsv`/CSV export, dashboard gadgets, automation conditions) via the shared `loadNlqlEvalContext` helper; unit tests for the resolved/unresolved/id-based cases on both fields; MCP tool-error surfacing verified end-to-end. **Product/engineering tension noted:** the product-auditor lens tends to favor forgiving-degrade (never hard-fail a filter); this MCP-QA/AX finding favors fail-loud for a query an agent will trust blindly — resolved here in favor of fail-loud, since a silently-wrong answer is worse than a rejected query on an agent-facing surface. [MCP-QA pass 1, finding 1 residual — see the ticked NLQL fix entry below for the resolved half]
- [ ] (P2, M) **Gitea integration v1 — two-way link (GitLab follow-up)** — the second self-hosted forge from the "GitLab/Gitea" Better-than-Jira gap, now that both GitHub (HMAC) and GitLab (shared-secret-token) v1s are shipped and the shared `common/issue-key.util.ts` extraction + parallel-table pattern are established precedents to mirror. Gitea's webhook scheme is HMAC-SHA256 like GitHub's (`X-Gitea-Signature` header, hex-encoded, verified against the raw body) — NOT GitLab's plain shared-secret compare — so `GithubModule`'s signature-verification shape is the closer template than `GitlabModule`'s token-verification shape, even though the DB tables (`GiteaIntegration`/`IssueGiteaLink`) mirror both providers' shape. **Acceptance:** additive `GiteaIntegration`/`IssueGiteaLink` parallel tables; ADMIN-gated `PUT/GET/DELETE /projects/:projectId/gitea` + `GET /issues/:issueId/gitea-links`, both `@RequireScope`-gated with new `gitea:read`/`gitea:write` `PAT_SCOPES`; public `POST /gitea/webhook/:projectId` verifying `X-Gitea-Signature` HMAC-SHA256 against the raw body; `push`/`pull_request` event parsing (Gitea's payload shapes closely mirror GitHub's, confirm against Gitea's actual webhook docs before implementing — do not assume byte-identical) scoped to the project's own issue-key prefix via the shared `common/issue-key.util.ts`; issue-drawer Development section renders Gitea links beside GitHub/GitLab (three-provider layout, same pattern as the GitHub+GitLab side-by-side precedent); unit tests mirroring `github-signature.util.spec.ts`/`github.service.spec.ts` coverage depth; e2e with a locally-HMAC-signed fake webhook (zero real Gitea egress); tenant-isolation matrix rows for the new endpoints; MCP: `list_issue_gitea_links` following the compact/verbose/pagination envelope, mirroring `list_issue_gitlab_links`. [ROADMAP Phase 9; VISION.md § Better-than-Jira gaps #3, § The thesis "Open & extensible"]
- [ ] (P2, M) **Dashboard sharing — public read-only embed (share-token reuse)** — reuse the same `ShareToken` pattern already used for public boards to let a dashboard (not just a board) be published read-only, no-login, to a URL a manager or client can bookmark. Given how strong Dashboards Phase 1 turned out to be this pass (real NLQL, real gadget variety, genuinely good empty/error states), this is now a cheap, high-leverage extension rather than a speculative one (AUDIT-PRODUCT.md Pass 12, Ideation #1). **Acceptance:** ADMIN mint/list/revoke endpoints mirroring `/projects/:id/share-tokens`; `/share/dashboard/:token` standalone read-only view (no add/edit/delete/reorder); rate-limited public endpoint; e2e (desktop+mobile); MCP: not applicable (public-web surface, not an agent action). [product-auditor Pass-12 Ideation #1; VISION.md § Better-than-Jira gaps #4]
- [ ] (P2, M) **Cross-project issue MOVE — product decision needed before build** — MCP-QA pass 1, finding 4: a mis-filed issue cannot be moved to the correct project today; the only correction path is delete+recreate, which loses history/comments/links and the original key (measured: 2 calls, real data loss). Product-wide, not MCP-specific — no web-UI surface has a "Move" action either, unlike the comparable-tracker table-stakes feature. **Needs a founder/product call before scoping build work:** does a cross-project move re-key the issue (new project's number, old key redirects/aliases) or keep its original key; do sub-tasks/epics move with it or get orphaned; is it MEMBER+ or ADMIN-gated given it changes an issue's permission boundary. Once decided: `move_issue_to_project` (guarded, re-keys, preserves history) exposed both as a web-UI action and an MCP tool. Filed as a decision-needed item rather than blocking on speculative scope. [MCP-QA pass 1, finding 4; product-auditor cross-project gap]

- [x] (P1, S) **NLQL silently returned wrong (empty) results for name/email-based person and name-based sprint queries — SHIPPED 2026-07-03** (`docs/MCP-QA.md` pass 1, finding 1 — blocked "what is X working on?" / "what's left in sprint N?", the two most common PM questions after epic status; `assignee = "Alex Rivera"` and `sprint = "July-B"` both silently returned 0 items with no error). Root cause: the shared evaluator (`packages/shared/src/nlql/evaluator.ts`) already supported name/email resolution for `user`-kind fields via `ctx.users`, but every server-side evaluation call site (`IssuesService.exportCsv`, the dashboards gadget evaluator, the automation engine's condition eval) only ever populated `{currentUserId}`/`{customFieldDefs}` — `ctx.users` was always empty; separately, `sprint` was declared `kind: 'id'` in `fields.ts`, so name matching wasn't implemented at all despite the autocomplete surface (`suggest.ts`) already advertising it. Fix: added a `sprint` `FieldKind` + `ctx.sprints` (mirrors the existing `resolveUserOperand` — accepts an id OR an exact, case-insensitive name, same unresolved-name-matches-nothing-no-error semantics as `user`-kind fields, deliberately kept consistent rather than redesigned this pass); added `getReferencedFieldKinds(query)` (shared) so a call site can batch-load only the side-context a query actually needs; wired all three server call sites through a new `loadNlqlEvalContext` helper (`apps/api/src/common/nlql-eval-context.util.ts`) — `exportCsv` and the dashboard-gadget evaluator load conditionally per query/per-dashboard (one round trip, not per issue/per gadget), the automation engine (event-driven, must stay cheap) loads conditionally per triggered-rule-batch. Live-verified over REST against a real fixture (named user + sprint, real issues): `assignee = "Alex Rivera"` and `assignee = "<email>"` → 5/5 correct (0/5 before the fix, id-based lookup already worked before and after); `sprint = "July-B"` → 4/4 correct (0/4 before). 18 new shared vitest tests (evaluator + `getReferencedFieldKinds`, 128→146) + 27 new API jest tests across the three call sites (issues/dashboards/automations, 1650→1683), all green; full shared vitest and API jest suites re-run green. **Note for future work (not fixed this pass, by design):** an assignee/sprint name that resolves to no known entity still silently matches zero issues rather than 400ing — kept consistent with existing `user`-kind semantics rather than inventing a new error channel; worth a follow-up pass if agents keep hitting it blind (a "0 results, and here's why" hint would need real design work, not a one-line change).

- [x] (P1, M) **Per-project agent context memory over MCP — SHIPPED 2026-07-03** (founder directive, top priority: "for each project a LLM via the MCP should be able to dump their context for the next run or agent… We should prompt it to do so. It should keep up to date from this." + "It should be a skill."). One shared handoff document per project (`ProjectAgentContext`, 64 KB cap): `GET/PUT /projects/:id/agent-context` (VIEWER+ read / effective-MEMBER+ write, PAT-scoped, tenant-isolation rows added), MCP `get_project_context` / `update_project_context` (read-first + hand-off-last prompting in the tool descriptions AND the MCP server's protocol-level `instructions` — every connecting client gets it), measured staleness (`changesSinceUpdate` from ActivityLog+AuditEvent newer than the doc — live-verified 0→1 after an issue create), realtime `project-agent-context.updated` event, and the distributable **`skills/project-context` Agent Skill** (+ `skills/README.md`, install docs in `apps/mcp/README.md` § "Ship your agent with memory" + main README). 91 MCP tools (was 89). Evidence: live stdio round-trip (initialize → instructions present → update → get, `contentBytes`/`updatedBy`/staleness all correct). **Follow-up (P2) — ✅ shipped 2026-07-03: human-visible "Agent context" panel in the project web UI.** New `AgentContextSection.tsx` on project Settings (`SettingsPage.tsx`, placed after Members/GitLab, matching section-card conventions): one-line explainer ("Persistent handoff memory for AI agents working in this project — also editable by humans"), content rendered via the app's existing `MarkdownRenderer` with an empty-state message, a metadata row (relative `updatedAt` + `updatedBy` name) and an amber "N changes since last update" staleness pill (reusing the existing amber-pill pattern), edit-in-place (Edit → textarea prefilled → Save/Cancel, toast on save, inline error — not just a toast — on the 64 KB 400) gated on EFFECTIVE project MEMBER+ (computed from the project's effective-members list, same source as `MembersSection`, falling back to workspace role while it loads); VIEWERs get a read-only render with no Edit control. Realtime: `SettingsPage` now subscribes via the existing `useBoardRealtime` project-socket hook, which gained a `project-agent-context.updated` case (someone saved) and an issue-activity case (the staleness count moves on project activity) both invalidating the new `qk.projectAgentContext(projectId)` key — verified live, no reload, from a second browser session. New `apps/web/e2e/agent-context.spec.ts` (12 cases × desktop 1280 + mobile 393, all green): empty state, write→renders-as-markdown→survives reload, a second live session sees the save with no reload, the staleness pill appearing live after another session creates an issue, a workspace VIEWER sees read-only, and the 64 KB cap surfacing as an inline error without discarding the draft; regression re-verified green: `settings-robustness.spec.ts` (15), `gitlab-integration.spec.ts` (5), `project-members.spec.ts` (8).

- [x] (P1, M) **Agent Experience (AX) batch — founder-relayed field report from a real MCP agent user (2026-07-02, photo of an agent's honest review after organizing 40+ tickets over `@next-lane/mcp`) — ✅ shipped 2026-07-03 (both phases).** The agent's verdict validates the agent-native pillar ("closer to Linear in ergonomics… would use it again") and lists five concrete frictions — each becomes an acceptance criterion. Split in two phases so Phase A could run while the GitLab builder held `apps/mcp`:
  - [x] **Phase A (API + UI, no `apps/mcp` touches) — ✅ shipped 2026-07-03.** (1) `Issue.startDate DateTime?` — additive migration (`20260703030000_add_issue_start_date`); create/update DTOs (`startDate?: string`, `IsDateString`, null clears, cross-field `startDate <= dueDate` validation enforced in `IssuesService` — 400 otherwise, mirrors dueDate's activity-log entry); issue drawer `StartDateField` beside Due date (same picker/clear-affordance UX); CSV export "Start Date" column beside "Due Date" (20 columns) + CSV/Jira importer maps a "Start Date" column when present (GitHub/Linear exports don't have one — noted, not attempted); NLQL `startDate`/`start` field registered everywhere `dueDate` is (field allowlist, evaluator, autocomplete suggestions, `docs-site/guide/features.md`); Roadmap/Gantt timeline's epic-window derivation now prioritizes the epic issue's own `startDate`→`dueDate` range over the existing child-sprint-dates/createdAt fallback (`RoadmapEpicDto.fromOwnDates`), keeping the fallback chain intact when `startDate` is absent. The agent had to "jam start dates into description first lines" — this is a data-model gap, not just an MCP gap, now closed. Unit tests: create/update validation (reject inverted range, null clears, boundary-inclusive equal dates), NLQL startDate filtering/sorting mirroring dueDate, CSV export/import spec updates, roadmap own-dates-priority cases — API suite 1650 green. E2e: `start-date.spec.ts` (drawer set/clear/persist-after-reload + inverted-range rejection, 8/8 desktop+mobile) + `roadmap.spec.ts` extended with an own-dates-priority case; `due-date.spec.ts` regression re-verified green (fixed a pre-existing `getByLabel` strict-mode flake found while touching this area). **Phase-B handoff:** `IssueDto.startDate: string | null` (ISO 8601, same null semantics as `dueDate`); DTO field name is `startDate` on both create/update; no MCP changes made in this phase.
  - [x] **Phase B (MCP surface) — ✅ shipped 2026-07-03**, `apps/mcp` only, every fix composed from existing REST endpoints (no API/schema changes): (2) `list_issues` gains an NLQL `query` param — passed to `GET /projects/:id/issues.csv?q=` (the only existing endpoint that runs the real parser/evaluator server-side) as a match-oracle, then hydrated into full issue objects via the existing cursor-paginated `GET /issues`; an invalid query surfaces the API's own parser message (`Invalid NLQL query: ...`), not a generic failure — **missing-endpoint note:** a JSON NLQL-filtered issue-list endpoint would collapse this to one call and drop the id-hydration page cap (10 pages × 200 issues); filed for whoever next touches `apps/api/src/issues`. (3) Token-efficiency sweep across **every** `list_*`/`search_issues` tool (25 tools): uniform `{ items, total?, limit, offset?, hasMore }` envelope, a hand-picked compact field set per resource (`list_issues` → `key/title/status/assignee/priority/type/startDate`) with `verbose: true` for the full DTO, `limit`/`offset` defaulting to 50 (max 200) — live-verified against a 44-ticket fixture: the same `list_issues` call is 11 KB compact / 3.8 KB NLQL-narrowed (was 84 KB verbose, i.e. the field report's ~150 KB regime). (4) Wrong-project guard: `create_issue` response always echoes `project: {id, key, name}`; description tells agents to confirm the target; optional `expectedProjectKey` 400s on mismatch **before** creating anything (the agent filed 9 issues into the wrong project with no nudge — this closes it at the point of the mistake, not after). (5) `get_epic_overview` tool: one REST call (`GET /issues/:id`, the same parent→children relation the issue drawer's sub-tasks list uses) returns epic + compact children + per-status rollup + `{done, total, fraction}` progress; `startDate` exposed on `create_issue`/`update_issue`/`list_issues` (compact + verbose) — `get_epic_overview` children can't carry it because the API's `IssueRefDto` doesn't project `assignee`/`startDate` on that relation, filed as a small follow-up. 89 tools total (38 read / 51 write, up from 88/37/51); `apps/mcp/README.md` counts/tables updated (and a stray uncommitted GitLab-tools README claim from a since-abandoned concurrent edit was removed as untrue-against-committed-code, per this task's own instructions).
  - **QA — ✅ done.** 18 new MCP unit tests (80 total, all green) covering: `query` pass-through + invalid-NLQL error propagation, compact-vs-verbose shape for `list_issues`/`list_boards`/`list_users`/`list_notifications`, `limit`/`offset`/`total`/`hasMore` on a generic list tool, `expectedProjectKey` match/mismatch/no-op, `get_epic_overview` composition (progress, status breakdown, single-call assertion) including the zero-children case, and `startDate` on create/update/list. Live-run evidence (real API + Postgres, 44-ticket fixture project): epic overview answered in **one** tool call; NLQL-filtered "in-progress" list was 3.8 KB, not 150 KB. E2e for the drawer startDate + NLQL unit tests for the new field — done in Phase A.
- [x] (P2, M) **docs-site features guide refresh — ✅ shipped 2026-07-03** — doc-syncer's first pass (2026-07-02) flagged `docs-site/guide/features.md` as needing a full rewrite, not fact-patches: checklists, time tracking, components, versions, WIP limits, swimlanes, issue templates, configurable workflows, MCP (55 tools), CSV import, SSO/OIDC, Mermaid, and email notifications are all shipped but undocumented in the user guide (~26 commits of drift). Owner: oss-curator (user-facing voice). [doc-syncer pass 1] **Done:** `features.md` fully rewritten (every claim code-verified — corrected the stale NLQL reference too: real operators/functions/`ORDER BY`, no invented `active()`/`7d` syntax; real automation triggers/actions); NEW flagship chapter `guide/agents-mcp.md` ("AI Agents & MCP": 92-tool surface, Claude Code/Desktop config, PAT scopes incl. `gitlab:*`, NLQL-over-MCP, compact/verbose/pagination token-efficiency, per-project agent context memory + staleness + `project-context` skill install, `expectedProjectKey` guard, `get_epic_overview`) added to nav + sidebar — this closes the doc-syncer's flagged agent-docs gap; `index.md` feature table/AI card brought current (MCP: Roadmap → Live); `configuration.md` gains the missing GitLab-integration section (`GITLAB_TOKEN_ENCRYPTION_KEY`/`GITLAB_WEBHOOK_BASE_URL`, Secret-Token webhook setup); FAQ/quick-start/architecture drift patched (`apps/mcp` + `skills/` in layout, module list current). `docs:build` green, dead-link check on.

> **Parity gaps** (mirrors `docs/VISION.md` § Better-than-Jira scorecard — kept
> visible until each row closes, per CLAUDE.md; Pass-12 numbers): Dashboards/
> gadgets **5/5 — CLOSED this pass** (Phase 1 verified live with all 4 gadget
> types, custom-field grouping, precise NLQL validation; Phase 2 items below
> are refinement, not a structural gap) · Permissions granularity **5/5 —
> CLOSED 2026-07-02** (the in-app SSO/OIDC admin configuration screen and
> per-project role override — schema+backend AND frontend UI — both shipped
> 2026-07-02; see the two ticked Ready-queue entries above) · Workspace-switcher scale **5/5 — CLOSED
> this pass** (search + recently-visited both verified working with a real
> 14-workspace fixture) · Cross-surface state coherence **4/5** (Pass-11's
> structural route-derived fix is genuinely and fully verified this pass; the
> remaining point is a product-decision nuance — does viewing a workspace page
> silently persist a default-workspace switch — tracked as a P2 item below, not
> a coherence bug) · Integrations **4/5, closing** (GitHub v1 AND GitLab v1
> both confirmed genuinely working end-to-end with a real signed/token-verified
> webhook round trip; auto-transition-on-merge + the board-card PR badge +
> live PR/CI status all shipped and live-verified 2026-07-03 — see the ticked
> entry above; still missing smart-commit `#done` syntax and Gitea, tracked as
> Next items below) · Mobile board
> toolbar **back to 5/5 — fixed 2026-07-02** (Swimlanes v2's own invisible
> dropdown + chip-row overflow regression closed via a portalled, viewport-
> clamped `<DropdownPanel>`; see the ticked Ready-queue entry above — the
> broader "Mobile" Better-than-Jira row above stays Behind only because of
> the unrelated, unchanged "no native app" gap). Category-parity gaps
> outrank further infra/observability polish until these close (CLAUDE.md
> operating principle).

- [x] (P1, S) **Mobile — Swimlanes v2's own paint regression** ✅ fixed 2026-07-02 — the board's "Group by" and filter-chip dropdown menus rendered **completely invisible** on a real 393px phone (functionally clickable, `isVisible()` true, but zero rendered pixels — Chromium's `overflow-x-clip` on the app-shell root suppressed paint of the entire absolutely-positioned menu once it extended past the viewport, not just the overflowing slice), the pre-existing quick-filter chip row silently clipped "Recently updated" off-canvas with no scroll cue, and the new "Group by" chip's label wrapped to two lines unlike every sibling chip (AUDIT-PRODUCT.md Pass 12, top-ranked finding — mobile went from "missing native app" to actively regressing on this pass's own flagship feature). Fixed at the pattern level: a new portalled, viewport-clamped `<DropdownPanel>` component (`apps/web/src/components/ui/DropdownPanel.tsx` — `createPortal`-to-`document.body`, measures the trigger+panel and clamps `position: fixed` fully inside the viewport on open/resize/scroll, flips above the trigger when there's no room below) replaces every toolbar `position: absolute` menu in `BoardPage.tsx` (Group by, Labels, Type, Priority filters, saved-filter menu, NLQL help — not just the two the audit named, the whole shared pattern); the quick-filter chip row gets real `overflow-x: auto` + `shrink-0` chips + the house `.nl-scroll` thin-scrollbar treatment (was `overflow-x: visible`, silently clipping); the "Group by" chip gets `whitespace-nowrap`/`shrink-0` matching its siblings. **Verification (paint-level, not just DOM presence):** two new e2e cases in `swimlanes.spec.ts` open the Group-by and Priority menus at 393px, assert the panel's `boundingBox()` is fully inside the viewport AND decode a real Playwright screenshot of the panel via the browser's own `<canvas>`/`getImageData` to assert non-trivial (>5) distinct sampled colors — closing the exact "isVisible() lies" gap the audit called out, via a genuine visibility-by-composite check, not a visibility-by-style one; two new cases in `quick-filters.spec.ts` assert `overflow-x: auto` and that "Recently updated" is fully reachable after scrolling, plus that the "Group by" chip stays single-line. 106/106 e2e green desktop+mobile (`swimlanes.spec.ts`, `board.spec.ts`, `quick-filters.spec.ts`, `nav-sidebar.spec.ts`); `tsc --noEmit` + `pnpm --filter @next-lane/web build` clean. Screenshots: `/tmp/nav-shots/mobile-groupby-fixed.png` (menu open and painted), `mobile-chips-scroll.png`. Also fixed the related P3 in the same pass (see the new ticked "Sidebar auto-collapse at the 1024px small-laptop breakpoint" entry below). [product-auditor Pass-12 Backlog-Groomer Ingest #1-3; AUDIT-PRODUCT.md Pass 12]
- [x] (P3, S) **Sidebar auto-collapse at the 1024px small-laptop breakpoint** ✅ fixed 2026-07-02 — the persistent left sidebar had no viewport-width-aware default: at 1024×768 it stayed fully expanded at its 240px desktop width, visibly cramping the 3-column board (the `Done` column's header/"+ Add issue" button partially cut off — AUDIT-PRODUCT.md Pass 12, Top gaps #6). Fixed in `SidebarContext.tsx`'s `readCollapsed()`: when there is **no stored preference** (checked via `localStorage.getItem(...) !== null`, not just a falsy read) AND the viewport is in the 1024–1279px band (the width range `AppSidebar`'s own `hidden lg:flex` actually renders it at, below the `xl` breakpoint), the sidebar now defaults to the collapsed rail; a real, explicit user preference (collapsed or expanded) always wins at any width, unchanged from before. 3 new e2e cases in `nav-sidebar.spec.ts` (defaults collapsed with no stored pref at 1024px; an explicit "expanded" choice overrides the default and survives reload; the board's `<main>` region is measurably wider than the old always-expanded default). Screenshot: `/tmp/nav-shots/sidebar-1024.png`. [product-auditor Pass-12 Top gaps #6 / Backlog-Groomer Ingest #6; AUDIT-PRODUCT.md Pass 12]
- [x] (P1, M) **[IN FLIGHT batch, largely complete — see ticked entries below] Reliability artifact gap — CSP blocks the dark-mode bootstrap + dashboards zero-realtime-coverage** ✅ — the engineering P1 fix batch (AUDIT-ENGINEERING.md Pass 12, P1-1 + P1-2) shipped this pass: the dark-mode no-flash bootstrap moved from a CSP-blocked inline `<script>` to a self-hosted `theme-init.js` (see the ticked "CSP blocks the dark-mode inline bootstrap in the Docker artifact" entry below), and Dashboards gained full realtime coverage (see the ticked "Configurable dashboards — zero realtime coverage" entry below) — bundled with two bonus mechanical/perf fixes from the same audit (tenant-isolation matrix, `resolveEnforcedWorkflowId` N+1), also ticked below. Still open, not part of this batch: a per-project/per-dashboard gadget cap (engineering P2-2) — folded into the "Configurable dashboards — Phase 2" Ready item below.
- [x] (P1, M) **In-app SSO/OIDC admin configuration screen** ✅ shipped 2026-07-02 — an instance admin now sets provider issuer URL/client ID/client secret/button label from a new settings page (`/admin/sso`) instead of an env-var edit + API redeploy — closing the first of the two "Admin controls" blockers (product-auditor Pass-12 rank #4). **Schema (additive):** singleton `OidcConfig` table (`enabled`, `issuerUrl`, `clientId`, `clientSecretEncrypted`, `label`) + `User.isInstanceAdmin` (migration `20260703000000_add_instance_admin_oidc_config`, hand-authored per the established `GithubIntegration`-migration precedent) — no existing instance-admin concept existed, so this is the new gate: strictly narrower than workspace `Membership.role: ADMIN` (appropriate for a secret that isn't scoped to any one workspace), defaulting the very first user on a fresh install to `true` (`AuthService.register`'s `isFirstUser` check, mirrored in `OidcService.findOrProvisionUser` for a first-ever SSO login) and backfilling the oldest existing user on an already-provisioned install (the migration's own `UPDATE`); `demo@nextlane.dev` is explicitly `isInstanceAdmin: true` in `seed.ts` for local/CI determinism. **Encryption:** the GitHub PAT's AES-256-GCM implementation was extracted into a shared `apps/api/src/common/crypto/secret-crypto.util.ts` (`deriveEncryptionKey`/`encryptSecret`/`decryptSecret`) so `github-crypto.util.ts` and the new `auth/oidc/oidc-secret-crypto.util.ts` (`OIDC_CONFIG_ENCRYPTION_KEY`, falling back to `JWT_SECRET` — zero-config self-host path unaffected) delegate to ONE audited primitive instead of a second copy; the GitHub module's behavior/env vars/tests are byte-for-byte unchanged (its own spec still passes verbatim). **Precedence + live effect (the acceptance criterion that mattered most):** `OIDC_ISSUER_URL`/`OIDC_CLIENT_ID`/`OIDC_CLIENT_SECRET` env vars still WIN over the DB config when all three are set (12-factor — operators who pin config in the environment keep that behavior unchanged; the settings-page form renders a read-only "env-managed" banner in that state and the server rejects a write outright rather than silently no-op'ing it); a new `OidcConfigService` (`apps/api/src/admin-settings/`) resolves the *effective* config fresh on every call (env branch, else an enabled+fully-populated DB row) — `OidcController.login`/`callback` and `AuthController.providers()` (`GET /auth/providers`, the login page's capability probe) now call it instead of the old sync env-only `isOidcConfigured()`, and `OidcService`'s OIDC-client discovery cache is keyed by a fingerprint (issuer+client id+secret hash) of the *effective* config rather than being a process-lifetime singleton — so saving from the UI takes effect on the very next login attempt, confirmed live end-to-end (`PATCH /admin/oidc-config` → immediate `GET /auth/providers` flip, no restart, verified via curl during build). **API:** `AdminSettingsModule` (`admin-settings.controller.ts`/`.service.ts`, `GET`/`PATCH /admin/oidc-config`) — instance-admin gated via a new `assertInstanceAdmin` helper alongside `assertProjectRole`/`assertProjectMember` in `common/membership.util.ts`; the client secret is write-only (`clientSecret?` in the PATCH DTO, omit to keep the stored value) and NEVER returned by any GET — `OidcConfigDto.hasClientSecret: true` only; enabling without a fully-populated config (issuer + client id + a secret, new-or-already-stored) is rejected with a friendly 400. Deliberately no import cycle: `AdminSettingsModule` depends on neither `AuthModule` nor `OidcModule` (both of which depend on IT for `OidcConfigService`) — `PrismaService` is global, `CurrentUser`/`AuthUser` are plain decorator imports. **Frontend:** `AdminSsoSettingsPage.tsx` (route `/admin/sso`, sidebar entry `nav-sidebar-admin-sso` gated on `useAuth().user?.isInstanceAdmin` — both in `AppSidebar` and `MobileSidebarDrawer` via the shared `SidebarNavContent`) — enabled toggle, issuer/client id/secret/label fields, secret input shows `placeholder="••• saved"` once `hasClientSecret`, inline validation, success/error toasts, an unsaved-changes `beforeunload` guard, and the env-managed read-only banner; `UserDto` deliberately stays unchanged (still embedded everywhere as a reference — comment author, issue assignee, attachment uploader) and a new `MeDto extends UserDto` (adds `isInstanceAdmin`) is used ONLY for "yourself" (login/register/`GET`+`PATCH /auth/me`/SSO session) so this didn't ripple a required field through a dozen unrelated mappers. **Tests:** 33 new API unit tests (`secret-crypto.util.spec.ts`, `oidc-secret-crypto.util.spec.ts`, `oidc-config.service.spec.ts` — env>DB precedence, secret never in `toDto()`, disabled/missing-field DB configs ignored — `admin-settings.service.spec.ts` — instance-admin gate on both routes, env-pinned-deployment write rejection, enable-requires-all-fields validation, partial-merge semantics) plus the existing `oidc.service.spec.ts`/`oidc.controller.spec.ts`/`auth.controller.spec.ts` updated for the new `OidcConfigService` dependency (behavior-preserving refactor, not new coverage) — **1554 API tests green** (was 1521); `tsc --noEmit` + `pnpm --filter @next-lane/api build` clean. New `apps/web/e2e/admin-sso-settings.spec.ts` (non-admin: no sidebar entry + access-denied on direct route navigation; instance admin: configure+enable via per-keystroke typing, persists across reload, unsaved-changes indicator, login page's "Continue with `<label>`" button appears with zero API restart, disabling removes it — desktop AND mobile, including a drawer-nav path) — 27/27 green (7 correctly skipped on the `mobile-chrome` Playwright *project* specifically to avoid a real cross-project race on the shared singleton `OidcConfig` row reproduced during QA — both viewports are still fully exercised via nested `test.use({viewport})` sub-describes within the one project that runs them, serialized); `sso.spec.ts`/`auth.spec.ts`/`password-reset.spec.ts`/`realtime-auth.spec.ts`/`nav-sidebar.spec.ts`/`workspace-switcher.spec.ts` full regression re-verified green desktop+mobile (127 tests), zero regressions; `tsc --noEmit` + `pnpm --filter @next-lane/web build` clean. **MCP:** deliberately **not exposed** — instance SSO secrets are not agent-appropriate (same shape of risk as the already-skipped GitHub-integration-config tools); noted in `apps/mcp/README.md`'s "Not exposed over MCP" table. SAML, multi-provider, and JIT workspace/role provisioning remain the tracked Phase 2 follow-up below; per-project role override (next two items) has since shipped both its schema+backend AND frontend UI slices, same day — the "Admin controls" gap is now fully closed. [product-auditor Pass-12 Backlog-Groomer Ingest #4; VISION.md § Better-than-Jira scorecard, Admin controls row]
- [x] (P1, M) **Per-project role override — schema + backend (Phase 1 of 2)** ✅ shipped 2026-07-02 — a `ProjectMembership` layer above the workspace-wide `Membership` so a user can be Viewer on Project A and Admin on Project B in the same workspace; closes the second (and, pending the frontend UI slice below, final) "Admin controls" blocker (schema-confirmed absent since Pass 9). **Schema (additive):** `ProjectMembership { id, projectId, userId, role, createdAt, updatedAt }` (migration `20260703010000_add_project_membership`, hand-authored per the established `GithubIntegration`/`OidcConfig` precedent — `prisma migrate dev` is non-interactive-unsupported in this environment), `@@unique([projectId, userId])`. Sparse by design: NO backfill for existing members — absence of a row means "inherit the workspace role." **Resolution rule (single source of truth):** a new `getEffectiveProjectRole(prisma, userId, workspaceId, projectId)` in `apps/api/src/common/membership.util.ts` — (1) the user MUST have a workspace `Membership` row at all, else `null` (a stray `ProjectMembership` row never grants access on its own — tenant isolation); (2) a workspace ADMIN always resolves to ADMIN, ignoring any override row (belt-and-braces: the write path also refuses to create one for an admin); (3) otherwise an existing override row's role WINS over the workspace role — both ELEVATING (MEMBER → project ADMIN) and RESTRICTING (MEMBER → project VIEWER, still read-only, never locked out of viewing); (4) absent an override, the workspace role applies unchanged. **Zero call-site changes needed:** `assertProjectRole` — already the single chokepoint every project-scoped write/role check in the codebase calls (labels, statuses, sprints, board, workflows, webhooks, versions, components, dashboards, share-tokens, GitHub integration, issue-templates, custom-fields, work-logs, automations, checklist, issue-links, poker, standups, saved-filters, attachments, issues) — now routes its internal role lookup through `getEffectiveProjectRole` instead of the raw workspace `Membership.role`; every one of those ~140 call sites gets override support for free with a one-function change. `assertProjectMember` (membership-only, no role check — used for read access) is deliberately UNCHANGED: it stays workspace-membership-based, so a VIEWER-restricted user still keeps read access to the project (confirmed live: restricted user got `GET /projects/:id` → 200, `POST .../labels` → 403). **REST** (`apps/api/src/project-memberships/`, new `ProjectMembershipsModule`/`Controller`/`Service`): `GET /projects/:id/members` (every effective member — workspace role, effective role, `isOverride` flag; any workspace member may list); `PUT /projects/:id/members/:userId/role` (set/replace an override; requires EFFECTIVE project ADMIN — a project-level override can itself grant this, not just workspace ADMIN, confirmed live; 404 if the target isn't a workspace member; 400 refuses to override a workspace ADMIN); `DELETE .../role` (clear an override, revert to inherited; 404 if none exists). Both mutations audit-logged (`project_membership.override_set`/`_clear`, mirroring `membership.add`/`.role_change`/`.remove`); no realtime socket event added (mirrors the workspace-membership precedent, which is audit-only too). **Shared types:** `ProjectMemberDto`/`SetProjectRoleOverrideDto` in `packages/shared` (no `Role` redefinition). **Tests:** 9 new `membership.util.spec.ts` cases (elevate, restrict, admin-override-ignored-and-short-circuited, tenant-isolation-without-workspace-membership) + 13 new `project-memberships.service.spec.ts` cases — 1575 API tests green (was 1562; the jump also reflects `projectMembership: { findUnique: ... }` added to ~20 existing service-spec Prisma mocks whose `assertProjectRole` call path now reaches the new query). 3 new tenant-isolation-matrix rows (`GET .../members`, `PUT`/`DELETE .../members/:userId/role`, all cross-tenant BLOCKED) — 96 rows total. `tsc --noEmit` + `pnpm --filter @next-lane/api build` clean. **Live end-to-end verification** (not just unit tests): registered two users in one workspace, confirmed a plain MEMBER gets 403 archiving a project, set an ADMIN override → 200 archiving that same project; set a VIEWER override → 403 creating a label but still 200 reading the project; confirmed a workspace ADMIN cannot be overridden (400) and a cleared override correctly reverts access. **MCP:** `list_project_role_overrides` (read) / `set_project_role_override` / `remove_project_role_override` (write) — 88 tools total (was 85); role management is agent-appropriate, not secret-bearing, so unlike the GitHub/SSO config surfaces this one IS wired up. **Frontend UI (Phase 2 of 2) — ✅ shipped 2026-07-02, see the ticked entry below.** [product-auditor Pass 9/10/11/12 carryover, rated 3 vs 5; VISION.md § Better-than-Jira gaps #2]
- [x] (P2, M) **Per-project role override — frontend UI (Phase 2 of 2)** ✅ shipped 2026-07-02 — depended on the schema+backend slice above (`GET`/`PUT`/`DELETE /projects/:id/members[/:userId/role]`, `ProjectMemberDto` in `packages/shared`), consumed as-is with zero backend changes. A new "Members" section on project Settings (`apps/web/src/components/settings/MembersSection.tsx`, between Project details and Columns, matching every neighboring settings-section convention — `Section`-card shell, `LoadingState`/`ErrorState`, dashed-border empty state) lists every effective member (avatar/name/email, workspace-role chip, effective-role control, "Inherited" vs "Override" badge — amber for override, matches the design system's amber scale rather than a new color). An effective-project-ADMIN viewer gets a role `<select>` (ADMIN/MEMBER/VIEWER, no-op skipped when the picked role already matches) per row plus a "Revert to inherited" action behind a `ConfirmDialog` (satisfies the "ConfirmDialog on remove" acceptance criterion) on overridden rows; a workspace ADMIN's row is a disabled fixed-ADMIN control with a tooltip ("Workspace admins always have full access to every project") mirroring the server's 400; the viewer's own row is never self-editable (mirrors `WorkspaceMembersPage`'s existing "isMe" convention, preventing an accidental project-access self-lockout); a solo-member project shows a "No other members" empty state. **Non-admin read-only decision:** a non-project-admin viewer sees the identical member list with zero controls (no `<select>`, no revert) — matches the existing project-settings convention (`SettingsPage.tsx`'s `editable`/`readonly-hint` pattern) rather than hiding the section outright, since `GET /projects/:id/members` is itself readable by any workspace member. New `apps/web/src/api/projectMembers.ts` (`useProjectMembers`/`useSetProjectRoleOverride`/`useClearProjectRoleOverride`) + `qk.projectMembers(projectId)` in `apps/web/src/api/keys.ts`, invalidate-on-settle (mirrors `ComponentsSection`'s mutation-hook convention — no optimistic update, since a role change isn't latency-sensitive). **e2e** (`apps/web/e2e/project-members.spec.ts`, 4 scenarios × desktop 1280 + mobile 393, 8/8 green): admin sees the effective list with inherited roles; setting a VIEWER override flips the badge AND is verified as a **real** access change, not a cosmetic one — a second, independent Playwright browser context logged in as the target member creates an issue successfully through the real board UI *before* the override (positive-control baseline), then, after the admin sets the override, the identical real-UI create-issue attempt (per-keystroke title) gets a genuine 403 error toast ("Requires MEMBER role in this project") with no issue created (confirmed via reload); "Revert to inherited" restores both the badge and write access (re-verified the same way); a workspace-ADMIN row's control is confirmed disabled with the tooltip; a non-project-admin viewer gets a fully read-only list (no comboboxes, no revert link anywhere in the section). **Mobile fix caught during this pass's own screenshot review** (not shipped un-reviewed, per CLAUDE.md's design-elevation directive to surface screenshots during progression): the row's `flex-wrap` layout crushed the name/email column against the fixed-width role controls at 393px, truncating names to 1-2 characters — fixed by switching the row to `flex-col` below `sm` and `sm:flex-row` at/above it, so identity and controls stack cleanly on narrow viewports. `tsc --noEmit` + `pnpm --filter @next-lane/web build` clean; regression re-verified green desktop+mobile: `settings-robustness.spec.ts` (18/18), `components.spec.ts` (10/10), `viewer-aware-ui.spec.ts` (6/6), `board.spec.ts` (8/8) — 42/42, zero regressions. Screenshots: `/tmp/nav-shots/members-desktop-before.png`/`-after.png`, `/tmp/nav-shots/members-mobile-before.png`/`-after.png`. **MCP:** already covered by the Phase 1 backend batch (`list_project_role_overrides`/`set_project_role_override`/`remove_project_role_override`) — no new tools needed for this UI-only slice. **Closes the "Admin controls" Better-than-Jira gap** (alongside the already-shipped in-app SSO/OIDC config screen) — see `docs/VISION.md` § Better-than-Jira scorecard for the next vision-steward pass to re-score. [product-auditor Pass 9/10/11/12 carryover; VISION.md § Better-than-Jira gaps #2]
- [x] (P1, M) **GitLab integration v1 — two-way link (Gitea follow-up noted)** ✅ shipped 2026-07-03 — same two-way linking shape as the shipped GitHub v1, GitLab semantics, implemented as a **parallel** `GitlabIntegration`/`IssueGitlabLink` table pair (deliberately NOT a provider-tagged generalization of the GitHub tables — additive/zero-risk to the already-shipped GitHub rows; a future "unify behind `ScmIntegration`" migration is an option once Gitea makes the duplication cost outweigh the migration risk, not attempted this pass). **Schema (additive):** `GitlabIntegration { projectId (unique), gitlabBaseUrl (default "https://gitlab.com" — self-hosted GitLab is first-class, unlike GitHub's hardcoded host), projectPath ("namespace/project", nested subgroups allowed), tokenEncrypted (AES-256-GCM via the shared `common/crypto/secret-crypto.util.ts` primitive, `GITLAB_TOKEN_ENCRYPTION_KEY`→`JWT_SECRET` fallback), webhookSecret }` + `IssueGitlabLink { kind MR/COMMIT/BRANCH, externalId, title, url, state, authorLogin }` (unique `[issueId, kind, externalId]` for idempotent re-delivery), migration `20260703060000_add_gitlab_integration` (verified against `schema.prisma` via `prisma migrate diff` — zero drift on the new tables; `prisma migrate status` reports "up to date"). **`GitlabModule`** (`apps/api/src/gitlab/`): ADMIN-gated `PUT/GET/DELETE /projects/:projectId/gitlab` (GET is role-shaped via `getEffectiveProjectRole` — full config incl. `webhookSecret` for the caller's EFFECTIVE project ADMIN, workspace ADMIN or per-project-override ADMIN alike, `{projectPath, gitlabBaseUrl, hasToken}` summary otherwise); public `POST /gitlab/webhook/:projectId` verifies the literal `X-Gitlab-Token` header via `crypto.timingSafeEqual` (GitLab does NOT HMAC-sign its payloads, unlike GitHub — this is a plain constant-time equality check, length-guarded before compare) before processing `Push Hook` (per-commit + pushed-branch-name key scan → COMMIT/BRANCH links) and `Merge Request Hook` (title + description + source-branch key scan → MR links, state normalized `opened→open`/`merged`/`closed`/`locked`) events. Issue-key extraction was extracted from `github/github-issue-key.util.ts` into a shared `common/issue-key.util.ts` (GitHub's own file now re-exports it, byte-for-byte behavior-preserving) so both providers share one scoping implementation. `GitlabClient` — the single seam for any future outbound GitLab REST call (self-hosted-base-URL-aware, unlike `GithubClient`'s hardcoded host) — exists, unit-tested/mocked, unused by v1 (webhook payloads carry everything v1 needs), zero gitlab.com egress. `GET /issues/:issueId/gitlab-links`. Two new PAT scopes (`gitlab:read`/`gitlab:write`) added to `PAT_SCOPES` in `packages/shared`; every route `@RequireScope`-gated (no ungated routes, a past review finding). **Frontend:** Settings "GitLab" section (`GitlabSection.tsx`, mirrors `GithubSection.tsx` — ADMIN form with project path + optional self-hosted instance URL + PAT, generated webhook URL/"Secret Token" with copy buttons; MEMBER/VIEWER read-only "Connected to namespace/project" summary, hidden when unconfigured) and a `GitlabLinksSection.tsx` ("GitLab" sub-heading, tanuki-glyph icons, MR/commit/branch rows + state badges) rendered beside `GithubLinksSection` in the issue drawer's Development area — both providers' links visible side-by-side when both are configured. Realtime: webhook upserts reuse the existing `issue.updated` socket event; `apps/web/src/api/socket.ts` also invalidates the new `qk.gitlabLinks(id)` cache key. **Tests:** 5 new spec files mirroring the GitHub coverage depth (`gitlab-token-verify.util.spec.ts` — valid/tampered/missing/empty/case-sensitive/length-mismatch, never throws; `gitlab-crypto.util.spec.ts` — AES round-trip, distinct-ciphertext-per-call, wrong-key failure, independent of the GitHub key's env var; `gitlab-client.service.spec.ts` — mocked `fetch`, self-hosted URL + nested-subgroup path encoding, never throws; `gitlab.service.spec.ts` — 667 lines, ADMIN/effective-ADMIN-override gating, tenant isolation, real Push-Hook/Merge-Request-Hook payload shapes, idempotent re-delivery, wrong-project-key ignored) — **1665 total API tests green**; real-DB `tenant-isolation.integration.spec.ts` still green, **102/102 endpoints blocked, 0 issues** (4 new GitLab rows: GET/PUT/DELETE integration + GET issue links); `apps/web/e2e/gitlab-integration.spec.ts` (5 scenarios × desktop+mobile, 10/10 green — settings save + webhook URL/secret display; a **locally token-verified fake GitLab webhook POST** links the issue and the MR row appears in the drawer; invalid/missing token → 401 + no link created; MEMBER sees the read-only summary, no form); `github-integration.spec.ts`/`issue-detail.spec.ts`/`issue-drawer-overlay.spec.ts`/`settings-robustness.spec.ts` regression re-verified green (40/40, zero regressions); `tsc --noEmit` + both apps' builds clean. **Live round-trip verification** (real running API, not just unit/e2e): valid MR webhook → `200 {linksUpserted:1}`; tampered token → `401`; identical replay → `200 {linksUpserted:1}` with the SAME link row id (idempotent, no duplicate); missing-header Push Hook → `401`; a PAT scoped to `gitlab:read` → `200` on `GET /issues/:id/gitlab-links`, a PAT without that scope → `403 "This token does not have the required scope: gitlab:read"`. **MCP:** `list_issue_gitlab_links` (read-only, `gitlab:read`-scoped, following the compact/paged envelope) — live-verified end-to-end against the running API via a scripted tool invocation, including the 403 scope-denial path returning as an MCP tool error; **92 tools total** (was 91); `apps/mcp/README.md` counts/tables updated (also fixed a pre-existing duplicated `update_project_context` table row found by MCP-QA pass 1, finding 9); configuring the integration itself deliberately NOT exposed (admin-only, secret-bearing, mirrors the GitHub decision) — noted in the "Not exposed over MCP" table. **Gitea follow-up (explicitly deferred, not this pass):** Gitea uses HMAC-SHA256 like GitHub (not GitLab's shared-secret-token scheme) and its own webhook payload shapes (`X-Gitea-Signature`, `push`/`pull_request` event names) — filed as a new Next item below rather than attempted speculatively against an unverified payload shape. [ROADMAP Phase 9; VISION.md § Better-than-Jira gaps #3]

- [x] (P1, M) **Route-derived `<WorkspaceScopedLayout>` / `<ProjectScopedLayout>`** ✅ shipped 2026-07-02 — `apps/web/src/layouts/ScopedLayouts.tsx`: all `/projects/:projectId/*` routes nest under `ProjectScopedLayout` (workspace resolved via existing `useProject`), all `/workspaces/:workspaceId/*` under `WorkspaceScopedLayout` (params-derived); leaf URLs unchanged; the 15 per-page `useSyncActiveWorkspace` calls removed — sync is correct by construction for any future scoped route. New parameterized class-guard e2e sweeps all 14 scoped routes asserting the chip matches the URL's workspace (workspace-switcher suite 18/18 desktop+mobile); board/auth/pulse/personal-board smoke 62/62; tsc clean. Deferred (filed below as P3): hoisting `AppHeader` above the per-page remount boundary for QuickLinksMenu collapse-state persistence (eng-audit P3-2). — Original scope: retire the cross-page tenant-context coherence bug class structurally instead of chasing it page-by-page. Today `useSyncActiveWorkspace` is an opt-in call every project/workspace-scoped page must individually remember — already missed on 7 of 15 pages once (both Pass-11 audits, same day, independently), and the underlying `WorkspaceContext` healing pattern is exactly the "duplicated/shadow state, opt-in sync" shape both audits warn will recur on route #16 if not made structural. Build a shared layout wrapper that derives active workspace/project purely from route params + a fetch and hosts `AppHeader` above the per-page remount boundary. **Acceptance:** every `/projects/:id/*` and `/workspaces/:id/*` route renders through the shared layout; the 15 individual `useSyncActiveWorkspace` call sites are removed once migrated; a parameterized e2e test asserts `data-testid="workspace-chip"` text matches the URL's resolved workspace on *every* scoped route (closes the "class not instance" regression-guard gap called out twice); `QuickLinksMenu`'s group-collapse state survives navigation as a side benefit (no more per-page `AppHeader` remount — eng-audit Pass-11 P3-2); `tsc --noEmit` + full e2e green desktop+mobile. **Priority tension:** see the note above the Ready queue — product-auditor rated this P2/M, engineering-auditor's ideation rated it P1/M; resolved P1. [product-auditor Pass-11 Ideation #1, engineering-auditor Pass-11 Ideation #1, VISION.md § Better-than-Jira gaps #1]
- [x] (P1, S) **`project.updated` realtime event + realtime-coverage inventory** — `Project` was the only top-level entity (vs. Issue/Comment/Sprint/Notification, all covered) with zero realtime events; a rename never propagated to a second open tab/viewer without a manual reload. Fixed: `SocketEvents.ProjectUpdated = 'project.updated'` added to `packages/shared`; emitted from `ProjectsService.update()` **and** `.archive()` (payload: mapped `ProjectDto`, same `emitToProject(project.id, …)` room pattern as issue/sprint events); `useBoardRealtime` (`apps/web/src/api/socket.ts`) now invalidates `qk.project`, `qk.projects(workspaceId)`, `qk.boards`, and the board-view family via `invalidateBoardFamily` on receipt — no new page-level wiring required; new `projects.service.spec.ts` (4 tests: emits on update, room is projectId not workspaceId, emits `archived: true` on archive, ADMIN-gate delegation) — 1342 API tests green; `tsc --noEmit` clean both apps. **Coverage inventory (acceptance criterion):** Board, Workflow, CustomFieldDefinition, Component, and Version services emit **zero** realtime events on mutation today — same stale-tab gap as Project had; filed as a new follow-up item below rather than silently expanding this ticket's scope. [engineering-auditor Pass-11 P3-1 / Ideation #3] ✅ shipped 2026-07-02
- [x] (P1, M) **e2e coverage: `QuickLinksMenu` + workspace rename/delete flows** ✅ shipped 2026-07-02 — `apps/web/e2e/quick-links.spec.ts` (7 scenarios: empty state, add with spaces via per-keystroke typing, javascript:/malformed URL rejected with inline error, edit persists across reload, delete stays gone after reload, group+color renders under group header, 393px usability) + `apps/web/e2e/workspace-settings.spec.ts` (rename updates chip AND dashboard selector and survives navigation+reload, empty-name rejected, wrong confirm text blocks delete / correct text deletes and heals to a remaining workspace, MEMBER sees read-only). 24/24 green desktop+mobile. — Original scope:  flagged unfixed for **two consecutive audit passes** (Pass 10, Pass 11) as the exact "feature ships, no browser test exists for the new surface" failure mode that let the founder's original workspace-selector bug reach production undetected. Workspace delete is the app's only irreversible hard-cascade-delete UI action; QuickLinksMenu is a header-level feature every user touches. **Acceptance:** new `apps/web/e2e/quick-links.spec.ts` (add/edit/delete/group/collapse/color, desktop+mobile) and `apps/web/e2e/workspace-settings.spec.ts` (rename persists; delete-confirm dialog requires an exact name match and stays disabled otherwise; a successful delete navigates home and heals the active-workspace context; ADMIN-gated, non-admin sees read-only) both green desktop+mobile; wired into the existing CI e2e job. [engineering-auditor Pass-10 Risk #2 (M) / Pass-11 P1-2 — "treat as blocking for the next build-loop batch rather than backlog"]
- [x] (P1, M) **SSO/OIDC — Phase 1: generic OIDC login provider** ✅ shipped 2026-07-02 — env-configured single provider (`OIDC_ISSUER_URL`/`OIDC_CLIENT_ID`/`OIDC_CLIENT_SECRET`, optional `OIDC_BUTTON_LABEL`/`OIDC_REDIRECT_URI`; documented in `.env.example` + `docs-site/guide/configuration.md` + `DEPLOY-KUBERNETES.md` `api.extraEnv`); OFF unless all three required vars are set — zero-config self-host path unaffected. `OidcModule` (`apps/api/src/auth/oidc/`): `GET /auth/oidc/login` (PKCE + state/nonce, signed short-lived httpOnly state cookie via `openid-client@5` + `Issuer.discover`, redirects to the provider) and `GET /auth/oidc/callback` (state/nonce CSRF guard, token exchange, rejects `email_verified: false`, JIT-provisions by email — existing user reused, unknown email creates one with a random unusable argon2 password hash — then issues the SAME JWT `AuthService.login`/`register` issue via new `AuthService.issueSession()`); browser lands on a new SPA route `/login/sso-complete` that reads the token from the URL **fragment** (never a query param/server log) and persists it exactly like password login. Both endpoints 404 when unconfigured (feature fully absent). Public `GET /auth/providers` → `{ oidc: { enabled, label } }` capability probe; `LoginPage` renders "Continue with `<label>`" only when the probe reports enabled, below a divider under the password form — all existing login test hooks/roles intact. 41 new unit tests (`oidc.config.spec.ts`, `oidc.service.spec.ts` — CSRF/nonce/unverified-email/token-exchange-failure guards + JIT-provisioning existing-vs-new-user, `openid-client` fully mocked — `oidc.controller.spec.ts`, `auth.controller.spec.ts` providers-endpoint shape); 1375 total API tests green; `tsc --noEmit` clean both apps; `apps/web/e2e/sso.spec.ts` (button absent + endpoints 404 when the dev API has no OIDC env set — the honest testable slice without a real IdP) green desktop+mobile alongside `auth.spec.ts`. SAML, multi-provider, and per-workspace/role JIT provisioning remain the tracked Phase 2 follow-up below. [product-auditor Pass 9/10/11 admin-controls rating 3 vs 5; VISION.md § Better-than-Jira gaps #2]
- [x] (P1, M) **GitHub integration kickoff — v1 two-way link (Phase 9 start)** ✅ shipped 2026-07-02 — per-project `GithubIntegration` (repoFullName + AES-256-GCM-encrypted PAT + generated HMAC webhook secret, hand-authored migration `20260702000000_add_github_integration`) and `IssueGithubLink` (kind PR/COMMIT/BRANCH, unique `[issueId, kind, externalId]` for idempotent re-delivery). `GithubModule` (`apps/api/src/github/`): ADMIN-gated `PUT/GET/DELETE /projects/:projectId/github` (GET is role-shaped — full config incl. webhook secret for ADMIN, `{repoFullName, hasToken}` summary for MEMBER/VIEWER, never the token); public `POST /github/webhook/:projectId` verifies `X-Hub-Signature-256` (constant-time HMAC compare) against the RAW request body (`main.ts` `rawBody: true`) before processing `push` (per-commit + pushed-branch-name key scan → COMMIT/BRANCH links) and `pull_request` (title + head-branch key scan → PR links, state open/closed/merged) events; `extractIssueNumbers()` is regex-scoped to the target project's own key (word-boundary, case-insensitive) so a different project's key never matches. `GithubClient` — the single injectable seam for any future outbound GitHub API call — exists and is unit-tested/mocked but is unused by every v1 endpoint (webhook payloads already carry everything v1 needs), keeping the module fully buildable/testable with zero github.com egress, per the environment constraint. `GET /issues/:issueId/github-links` (MEMBER+). Frontend: Settings "GitHub" section (`GithubSection.tsx`, ADMIN form + generated webhook URL/secret with copy buttons + paste-into-GitHub instructions; read-only "Connected to owner/repo" summary for members, hidden entirely when unconfigured) and a "Development" section in the issue drawer (`GithubLinksSection.tsx`; PR/commit/branch rows with state badges + external links; hidden when no links) wired into `IssueDetailDrawer` between Time Tracking and Comments. Realtime: webhook-driven upserts reuse the existing `issue.updated` socket event (minimal `{id}` payload, no new event type) so an already-open drawer refreshes live — `apps/web/src/api/socket.ts` extended to also invalidate the new `qk.githubLinks(id)` cache key. Two new PAT scopes (`github:read`/`github:write`) added to `PAT_SCOPES`. Deferred to the next Phase-9 slice (not v1 scope): auto-transition-on-merge, live PR/CI status badge on the board card, smart-commit `#done` syntax, GitLab/Gitea. **Tests:** 63 new unit tests across 5 spec files (`github-signature.util.spec.ts` — valid/invalid/missing/tampered/malformed signatures; `github-issue-key.util.spec.ts` — multi-key commits, wrong-project-prefix ignored, case-insensitive, word-boundary substring guard; `github-crypto.util.spec.ts` — AES round-trip, wrong-key failure, malformed-input guard; `github-client.service.spec.ts` — mocked `fetch`, never throws; `github.service.spec.ts` — ADMIN gating on upsert/delete, tenant isolation via `assertProjectMember`/`assertProjectRole` rejection, webhook idempotency, push/PR event parsing including BRANCH links and merged/closed/open PR state) — 1440 total API tests green (was 1375); real-DB `tenant-isolation.integration.spec.ts` still green (7/7); `apps/web/e2e/github-integration.spec.ts` (4 scenarios × desktop + mobile — settings save + webhook URL/secret display; a **locally HMAC-signed fake GitHub webhook POST** links the issue and the PR row appears in the drawer, fully local with zero GitHub egress; invalid signature → 401 + no link created; MEMBER sees the read-only summary, no form) all green; `tsc --noEmit` clean both apps. [VISION.md § Better-than-Jira gaps #3; ROADMAP Phase 9]
- [x] (P1, S) **CSV export completeness — add missing columns** ✅ shipped 2026-07-02 (Description, Component, Fix Versions, Parent key, Original Estimate (minutes), one "CF: <name>" column per custom-field definition, id-keyed with key fallback; importer-shared column names keep the export round-trippable; 41 CSV unit tests green incl. new value-level completeness cases) — the shipped 13-column export (`GET /projects/:id/issues.csv`) is missing data any real export/reporting/migration workflow needs: **Description**, **Component** (name), **Fix Versions** (Version M:N, joined like Labels), **Custom Fields** (all project-defined fields as their own columns, keyed by field label), **Time estimates** (`originalEstimateMinutes` → friendly duration + `timeSpentMinutes` → friendly duration), and **Parent/Epic** (parent issue key, if any). All source data already exists on `IssueDto`/relations — this extends `csv.util.ts`'s column mapping, no schema change. **Acceptance:** new columns appended (not reordered, to avoid breaking existing consumers' column-index assumptions) to the CSV header + row mapper; custom-field columns dynamically generated per-project from `CustomFieldsService` (empty cell when a field doesn't apply to that issue's type); formula-injection guard (`csvCell()`) applied to all new text-bearing columns; CSV import (`issues-import.sources.ts`) header-alias map updated so round-tripping an exported file back through import still works; unit tests for each new column (present/absent/multi-value) + an import-export round-trip test; `tsc --noEmit` clean. [founder-flagged 2026-07-02]
- [x] (P1, M) **Settings robustness pass** ✅ shipped 2026-07-02 — `qa-tester`-driven sweep (`docs/UI-REVIEW.md` "Settings robustness sweep — 2026-07-02") of every project-settings section and every workspace-settings page found 4 confirmed defects (SETTINGS-1..4), all fixed same day in a dedicated fix batch: **SETTINGS-1 (P1, admin lockout)** — `workspaces.service.ts#addMember` used to upsert-by-email with no last-admin guard, so a solo admin re-typing their own (or another admin's) email into the free-text Invite form silently demoted them with zero recovery path. Fixed: `addMember` (`POST /workspaces/:id/members`) now only creates a brand-new membership — an email that already belongs to a member returns a friendly 409 pointing at the per-row role Select instead of upsert-demoting; role changes for existing members moved to a new `PATCH /workspaces/:id/members/:membershipId`; that endpoint and `removeMember` both enforce a last-admin invariant (400, friendly message) covering self-demotion/self-removal by the sole admin, while a non-last-admin demote/remove still works. **SETTINGS-2 (P2)** — the branding hex input accepted CSS 3-digit shorthand (`#fff`) client-side but the server DTO only accepted 6-digit, round-tripping a raw technical 400 on Save; the client now normalizes 3-digit → 6-digit before submit. **SETTINGS-3 (P2)** — `statuses.service` gained a case-insensitive same-project duplicate-name guard (409, friendly), mirroring Labels/Components/Versions; deliberately a service-level check only, no DB `@@unique`, since existing self-hosted installs may already have duplicate column names. **SETTINGS-4 (P2)** — `labels.service` now catches Prisma P2002 on create/update and throws a friendly `A label named "X" already exists in this project` instead of the generic fallback. Plus a P3 polish pass rewording leaked DTO/enum-name validation messages (GitHub `repoFullName`, custom-field type name, branding hex) into user voice. All 4 `test.fixme()` tests in `apps/web/e2e/settings-robustness.spec.ts` un-fixme'd, 20/20 green desktop+mobile; 23 new API unit tests (every guard branch); 1461 API unit tests green; `tsc --noEmit` clean both apps. [founder directive 2026-07-02]
- [x] (P1, M) **Workflows robustness pass** ✅ shipped 2026-07-03 — `qa-tester`-driven sweep (`docs/UI-REVIEW.md` "Workflows robustness sweep — 2026-07-02") of the named-workflow manager, the transition/gate editor, the visual graph builder, board workflow assignment, and — the headline finding — enforcement as felt across every status-change surface, found 5 confirmed defects (WF-1..WF-5), all fixed same batch: **WF-1 (P1, enforcement bypass)** — Triage's `s` picker, the issue drawer's Status `<select>`, and bulk edit all silently bypassed a board's enforced named workflow (only board drag/the card status picker enforced it, via `IssuesService.move()`'s board-aware path). Fixed: a single shared `IssuesService#enforceStatusChange()` now backs `move()`, `update()`, and (transitively) `bulkUpdate()` — when no explicit `boardId` is present it resolves the project's enforced board-assigned workflow via `resolveEnforcedWorkflowId()` (mirrors `BoardService`'s own KANBAN/SCRUM visibility rule; ties break to the default board), falling back to the legacy project-level path only when none resolves; automation bypass preserved on all paths. **WF-2 (P2)** — the REQUIRE_FIELD gate's custom-field mode could never actually succeed (`Issue.customFields` is stored keyed by the definition's opaque id, but the gate editor's placeholder steered admins toward typing the human key). Fixed: `WorkflowService.evaluateGate` now resolves the gate's `field` against `CustomFieldDefinition.key`/`name` (case-insensitive) to the definition id before the value lookup, with backward-compatible direct-match short-circuit; the gate editor's field input is now a `<select>` of core fields + the project's real custom fields instead of freeform text. **WF-3 (P2)** — a REQUIRE_FIELD/REQUIRE_LINK gate could be saved with a blank param and silently never gate anything. Fixed: `WorkflowGateDtoClass` now rejects a blank `field`/`linkType` with 400 (`@MinLength(1)`); the gate editor disables Save until a field is chosen; any already-stored blank-key gate renders a visible "misconfigured" warning chip. **WF-4 (P2)** — no rename affordance for a named workflow; added inline rename (pencil → per-keystroke input, Enter/blur saves, Escape cancels, 409 handled friendly) in `WorkflowsManager`. **WF-5 (P3)** — the two independent workflow systems (legacy project-wide vs. named per-board) were unlabeled and their "+ Add transition" buttons collided by accessible name; the legacy section is now headed "Legacy project-wide transitions" with an explainer + link to the Named Workflows manager, and the two buttons have distinct accessible names. All 12 `test.fixme()` tests in `apps/web/e2e/workflow-robustness.spec.ts` un-fixme'd, 42/42 green desktop+mobile; `board-workflows.spec.ts`/`board.spec.ts`/`workflow.spec.ts`/`workflow-graph.spec.ts` regression green; 1471 API unit tests green (new coverage for the WF-1 resolution semantics and WF-2 definition-key resolution); `tsc --noEmit` clean both apps. [founder directive 2026-07-02]
- [x] (P1, L) **Navigation & IA overhaul — Phase 1: persistent left sidebar framework** ✅ shipped 2026-07-02 — App.tsx-level `AppShellFrame` (a sibling of `<Routes>`, wrapping `SidebarProvider`) renders `<AppSidebar>` on desktop (`lg`+, collapsible to a 56px icon rail) and `<MobileSidebarDrawer>` as a below-`lg` overlay opened from a new header hamburger (`nav-sidebar-drawer-toggle`) — a single mount for the whole session, never remounted per page (verified: it sits outside every per-page `Shell`). Sections: **workspace** (active workspace + switcher — the header `WorkspaceChip`'s search/recent/list dropdown body was extracted verbatim into `WorkspaceSwitcherMenuContent`, reused by both the header chip and the sidebar's own trigger; both read the one `WorkspaceContext`/`useSwitchWorkspace` hook, no duplicated state); **projects** for the active workspace (active project gets `aria-current="page"` + a signature 3px cobalt "rail tick" flush to the row, project key shown via the existing `.nl-issue-key` mono chip — collapsed rail truncates to 2 chars, matching `ProjectCard`'s key-badge convention, fixing an overflow bug caught in QA screenshots); **personal** (My Work / My Board / Insights / Notifications); **bottom utility** (workspace settings link). Collapse state persisted to `localStorage` (`nl.sidebarCollapsed`) and restored synchronously (read in the `useState` initializer, no flash). `AppHeader`'s My Work/My Board/Insights links hide at `lg`+ (`lg:hidden` added) so the sidebar is the single place they render there — no duplication; the `gotoSection` e2e helper was made viewport-generic (checks every role-matched link for the first visible one instead of only `.first()`) so it keeps working regardless of which surface currently owns a link, and `analytics.spec.ts`'s direct `nav-my-analytics` testid click was swapped for `gotoSection('Insights')` for the same reason. Keyboard-accessible (`aria-current`, visible focus rings, Escape closes the mobile drawer via the shared `useOverlay` hook — same focus-trap/scroll-lock/Escape behavior as `Modal`), `prefers-reduced-motion` respected (global rule already zeroes all animation durations). New `data-testid`s: `nav-sidebar`, `nav-sidebar-toggle`, `nav-sidebar-project`, `nav-sidebar-drawer`, `nav-sidebar-workspace-trigger`, `nav-sidebar-drawer-toggle`. Design: Dispatch tokens throughout (ink-* neutrals, signal-600 cobalt only for the active-row rail tick + accents), no generic filled-pill active state, no new dark-chrome rail (deferred to the upcoming dark-mode item so the token system is touched once). Every existing `data-testid`/role/aria-label preserved. New `apps/web/e2e/nav-sidebar.spec.ts` (12 tests: render/active-project/click-through/collapse-persist-across-reload desktop, hidden-by-default/hamburger-opens/Escape-closes/click-navigates-and-closes mobile) + one sidebar case added to `workspace-switcher.spec.ts` (switch via sidebar → sidebar's own project list re-scopes); full existing e2e suite re-run desktop+mobile; `tsc --noEmit` + `pnpm --filter @next-lane/web build` clean. Before/after screenshots (desktop 1440 + mobile 393, dashboard + board, incl. collapsed rail + mobile drawer states) captured per CLAUDE.md's design-elevation directive. **Full-suite regression hardening (found + fixed by the full-suite QA gate, not theoretical):** (1) the sidebar frame was originally gated on `activeWorkspace` resolving, opening a timing gap right after registration/first-login where `AppHeader`'s equivalent links are already `lg:hidden` but the sidebar hadn't mounted yet — fixed by always rendering the sidebar frame (only the workspace/projects *sections* inside it gate on the workspace), plus `gotoSection` now polls for a visible candidate instead of one snapshot; (2) the sidebar rendering every project's name/key as real page text broke three existing tests' loose, page-wide text/role matchers that happened to collide with seeded/test project names (`backlog-sprint.spec.ts`'s `getByRole('link', {name:'Backlog'})` matched "QA Backlog …" sidebar rows — fixed with `exact: true`; `mobile-breadcrumb.spec.ts` and `share-link.spec.ts` scoped their assertions to `header`/`main` respectively); (3) the sidebar's project-list reused the shared `.nl-scroll` utility class, and since it now sits earlier in the DOM than the board, `qa-adversarial.spec.ts`'s `document.querySelector('.nl-scroll')` started finding the sidebar instead of the board's horizontal-scroll container — fixed by adding a `board-scroll-container` testid to `BoardPage.tsx` and querying that instead; (4) the new mobile hamburger button pushed the header's icon row past 375px, wrapping the avatar onto the breadcrumb's row and growing total header height — fixed by shrinking the hamburger's own footprint and the workspace chip's mobile `max-w` (120px→84px; already-truncating by design, no test asserts its exact rendered width). Two additional full-suite failures were root-caused as pre-existing and NOT caused by this change (confirmed via `git stash` against the pre-sidebar baseline): `csv-export.spec.ts`'s header-row assertion is stale against an unrelated in-flight CSV-column-completeness change (ROADMAP "Current focus" #6), and `sprint-indicator.spec.ts`'s mobile active-sprint-badge assertion fails identically on the pre-sidebar baseline (the badge is `ProjectBreadcrumb`'s `extra` slot, documented as desktop-only/hidden-below-`sm` — the test itself is stale, not this change). `role-enforcement.spec.ts`'s "ADMIN re-add member" 409 is explained by an already-shipped, unrelated backend change (Settings robustness pass's last-admin-invariant work made `addMember` non-idempotent) — a pure API test with zero frontend touchpoints, outside this diff's blast radius. **Phase 2 (below) is next.** [VISION.md § Better-than-Jira scorecard "Cross-cutting finding"; CLAUDE.md "Design elevation — CURRENT TOP PRIORITY"]
- [x] (P1, M) **Navigation & IA overhaul — Phase 2: surface buried features as first-class nav** ✅ shipped 2026-07-02 — builds on Phase 1's sidebar shell (`apps/web/src/components/nav/`), same Dispatch tokens/vocabulary. (1) **Per-project views as direct sidebar links**: the active project's `ProjectRow` now expands a `ProjectViewsSubNav` (Board/Backlog/**Roadmap**/Reports, `data-testid="nav-sidebar-view"`, small icon set added to `sidebarIcons.tsx`) directly beneath it — the Gantt-style Roadmap, previously two clicks deep in `ProjectNav`'s "More" dropdown, is now one visible click away; `ProjectNav` itself is untouched (still serves mobile + tab context), this is purely additive and shared verbatim by both `AppSidebar` and `MobileSidebarDrawer` (one `SidebarNavContent` source, no drift). (2) **Workspace Branding surfaced**: a `Branding` row (`data-testid="nav-sidebar-branding"`) added alongside the existing "Workspace settings" link in the sidebar's bottom utility section, gated on `useMyRole(activeWorkspace.id) === Role.ADMIN` — the exact same admin gate `WorkspaceBrandingPage` itself enforces, so a non-admin never sees a link to a page that would reject them. (3) **Board default-filter discoverability**: `BoardPage`'s toolbar chip (`board-filter-indicator`) changed from a passive `<span>` to a `<button>` that calls `setOpenFilterField(true)`, which `BoardSwitcher` (new `openFilterField`/`onFilterFieldOpened` props, mirroring the existing `openColorsTab` pattern) uses to open `BoardSettingsModal` on the General tab with a new `focusFilterField` prop that scrolls the Default-filter field into view and focuses its input on open; when no filter is set yet, a new dashed-border empty-state affordance (`data-testid="board-filter-chip"`, "+ Default filter", MEMBER+ only via the existing `editable`/`canEdit()` gate) renders in its place, opening the identical editor — so the capability is discoverable even on boards that have never used it. **Tests:** 3 new cases in `apps/web/e2e/nav-sidebar.spec.ts` (Roadmap reachable + navigates from the sidebar; Branding reachable for an admin; filter chip opens Board settings pre-scrolled/focused on the Default-filter input) — 9/9 green desktop+mobile; full regression re-run desktop+mobile: `nav-sidebar.spec.ts` (9), `workspace-switcher.spec.ts` (11), `board.spec.ts` (4), `board-switcher.spec.ts` (9), `roadmap.spec.ts` (1), `board-default-filter.spec.ts` (1) — 35/35 × 2 viewports green; `tsc --noEmit` + `pnpm --filter @next-lane/web build` clean. Before/after screenshots captured (`/tmp/nav-shots/phase2-desktop.png` — sidebar expanded with project views; `phase2-filter-chip.png` — the filled filter-chip affordance; a mobile drawer shot confirming the same additions render there via the shared `SidebarNavContent`). One pre-existing, out-of-scope flake was root-caused during this pass (not fixed, not caused by this diff): `analytics.spec.ts`'s "Analytics tab renders flow, cycle-time and workload" intermittently fails to click `ProjectNav`'s "More → Analytics" menu item under 2-worker parallel load ("element is not stable"/detached-DOM retry) — reproduced identically on the pre-Phase-2 baseline via `git stash`, confirmed unrelated to any file this change touches; filed as a follow-up below rather than silently patched over. — Original scope: moves the three founder-rediscovered features (and their peers) from "buried" to "first-class," without duplicating any backend. [VISION.md § Better-than-Jira scorecard "Cross-cutting finding"]
- [ ] (P3, S) **`analytics.spec.ts` flaky "More → Analytics" click under parallel load** — `Project analytics — desktop › Analytics tab renders flow, cycle-time and workload` intermittently times out clicking `ProjectNav`'s "More" dropdown's Analytics menuitem ("element is not stable" → "element was detached from the DOM, retrying") when run in the same 2-worker batch as other specs; passes reliably in isolation. Reproduced on both the Phase-2-nav baseline and pre-Phase-2 baseline (`git stash` A/B), so it predates and is unrelated to the Navigation & IA overhaul — likely `MoreMenu`'s outside-click/blur handler racing a slow first render under CPU contention from a sibling worker's browser process. Root-cause and fix (e.g. a more robust wait, or hardening `MoreMenu`'s close-on-blur listener) rather than adding a retry/skip. [nav overhaul Phase 2 QA pass, 2026-07-02]
- [x] (P1, M) **Light / dark mode** ✅ shipped 2026-07-02 — token-layer fix, done ONCE so every existing component inherits it for free (no per-component overrides). `tailwind.config.js` adds `darkMode: 'class'` and re-points the `ink`/`slate`/`red`/`amber`/`emerald`/`green`/`blue`/`gray`/`orange`/`signal`/`brand` Tailwind color scales at CSS custom properties via a `withOpacity()` helper (`color-mix()`-based, so `/NN` opacity modifiers keep working — this also fixed a pre-existing latent bug where `bg-signal-50/60`-style opacity modifiers on var-backed colors silently produced no rule at all). The vars themselves stay plain hex strings (not RGB triplets), so `getComputedStyle(...).getPropertyValue('--nl-signal-600')` is unaffected — `workspace-branding.spec.ts`'s existing hex-string assertions keep passing untouched. `index.css`: light values byte-identical to the previous static palette; a new `.dark {}` block redefines every var — ink-scale shade ROLES stay fixed across modes (50/100 wash, 200/300 border, 400-600 muted→secondary text, 700-900 body→primary text) while the literal color flips; canvas `#0d0f14` → surface `#141821` → raised `#181c26` → overlay `#1b202b`; semantic red/amber/emerald/green/blue/orange scales re-derived from their UNCHANGED 600 anchor (mix sub-anchor shades toward the dark canvas, super-anchor shades toward a bright paper tone) — spot-verified ≥4.5:1 contrast for every text-role pairing against both canvas and surface; shadows trade soft ambient blur for a 1px border-tinted shadow in dark (`--nl-shadow-*` vars, per the "reduce/replace with borders in dark" guidance); new `scrim` token (deliberately mode-invariant and decoupled from `ink-900`, which DOES flip to a light text color in dark mode, so modal/drawer backdrops never invert) and `surface`/`canvas` tokens replace ~190 hardcoded `bg-white`/`ring-white`/`border-white` call sites app-wide via a token-layer swap. `applyBrandColor.ts` is dark-mode-aware — detects the `.dark` class and mixes a custom workspace brand color toward the dark canvas/paper endpoints instead of white/black (verified: brand color composes correctly with dark mode). `PulseDashboardPage` (explicitly named) migrated off `slate-*` to the `ink-*` vocabulary; `BulkActionBar`'s always-dark floating command strip pinned to literal mode-invariant hex (it's an intentionally-dark chrome element regardless of app theme, so letting `ink-900` flip under it would have inverted it). Mode control: `ThemeContext` (`nl.theme` localStorage key: light/dark/system, default system — matches `nl.activeWorkspaceId`/`nl.sidebarCollapsed`), a synchronous inline `<script>` in `index.html` applies the `.dark` class before first paint (zero flash), `ThemeToggle` (Light/Dark/System, `data-testid="theme-toggle"`/`theme-toggle-light`/`-dark`/`-system`) in both the sidebar utility area and the header user menu. **Gates:** `tsc --noEmit` + `pnpm --filter @next-lane/web build` clean; new `apps/web/e2e/theme.spec.ts` (10 checks desktop+mobile: default-light with no persisted pref, toggle-to-dark applies instantly + persists across reload + toggle-back, brand-var stays a hex string in dark mode, System mode follows Playwright's emulated `colorScheme`, mobile-reachable via the header menu with no overflow) plus the full `nav-sidebar`/`workspace-switcher`/`board`/`auth`/`pulse-dashboard`/`settings-robustness` suites re-verified green (104/104 desktop+mobile, zero regressions). Screenshots: `/tmp/nav-shots/dark-dashboard.png`, `dark-board.png`, `dark-issue-drawer.png`, `dark-mobile.png` (393px), `light-board-no-regression.png`. **MCP exposure:** not agent-appropriate — theme is a client-only display preference (never persisted server-side), so there is no corresponding `@next-lane/mcp` tool. [VISION.md § Better-than-Jira scorecard "Cross-cutting finding"; CLAUDE.md "Design elevation — CURRENT TOP PRIORITY"]
- [x] (P1, M) **Configurable dashboards — Phase 1: NLQL-native gadget framework** ✅ shipped 2026-07-02 (rescoped 2026-07-02 per founder directive — "custom dashboards based on the query language") — closes the flat-3 reporting/admin parity gap flagged across **four consecutive audit passes** (8/9/10/11). Every gadget is defined the same way: an **NLQL query** (the exact shared `validateQuery`/`filterIssues` engine + `NlqlInput` autocomplete end-to-end, no parallel query path) + a **visualization type** (STAT/TABLE/BREAKDOWN/BURNDOWN) — not a bespoke widget type per gadget. **Scope delta from the original write-up above (per the build task's explicit direction):** dashboards are **per-project** (`Dashboard.projectId`, mirroring how Boards/Reports are project-scoped — not per-user on `/`), reordering is **up/down buttons writing `config.position`** (no drag in v1, config-driven per the acceptance criteria given), and there are **no pre-built default gadgets** (every gadget — including a project's first one — is created through the same "add a custom gadget" composer; a founder/product call on whether to seed defaults is deferred to Phase 2). **Schema (additive):** `Dashboard` (`id`, `projectId`, `name`, `order`, timestamps) + `DashboardGadget` (`id`, `dashboardId`, `title`, `query`, `visualization` enum, `config Json` — holds `field`/`columns`/`limit`/`position`/`size`, timestamps), both `onDelete: Cascade`; migration `20260702010000_add_dashboards`. **Backend** `apps/api/src/dashboards/` (`DashboardsModule`/`Controller`/`Service`): `GET`/`POST /projects/:id/dashboards` (VIEWER read / MEMBER+ write), `GET`/`PATCH`/`DELETE /dashboards/:id`, `POST /dashboards/:id/gadgets`, `PATCH`/`DELETE /gadgets/:id` (config PATCHes merge, not replace), `GET /dashboards/:id/data` — loads the project's issues once (capped `DASHBOARD_ISSUES_CAP=2000`), validates + evaluates every gadget's query via the shared NLQL engine, and shapes per visualization (STAT → count; TABLE → capped key/title/status/assignee/points rows; BREAKDOWN → counts grouped by a standard or custom SELECT/MULTI_SELECT field; BURNDOWN → resolves the single sprint the filtered issues belong to and reuses `ReportsService.burndown()` verbatim). A gadget with invalid/now-unresolvable NLQL (e.g. its referenced custom field was later deleted) returns a per-gadget `{ error }` instead of ever 500ing the whole dashboard. 40 new unit tests (18 pure-function evaluator + 22 service, incl. tenant isolation via `assertProjectMember`/`assertProjectRole`) — 1551 API tests green. **Shared types + MCP:** `DashboardDto`/`DashboardGadgetDto`/`DashboardDataDto` family in `packages/shared`; **9 new MCP tools** (`list_dashboards`, `get_dashboard`, `get_dashboard_data`, `create_dashboard`, `update_dashboard`, `delete_dashboard`, `create_dashboard_gadget`, `update_dashboard_gadget`, `delete_dashboard_gadget`) — server now at **85 tools** (36 read / 49 write); `apps/mcp/README.md` tables updated; 59 MCP tests green. **Frontend:** "Dashboards" in the sidebar `ProjectViewsSubNav` + `ProjectNav`'s "More" menu; route `/projects/:id/dashboards` (`DashboardsPage`); dashboard tabs + a CSS-grid gadget board (`config.size` for 1/2-column span); create/edit gadget modal reuses `NlqlInput` + a visualization select + per-viz config fields; STAT/TABLE/BREAKDOWN/BURNDOWN renderers (BURNDOWN reuses the existing `BurndownChart` SVG); VIEWER read-only (no add/edit/delete/reorder). testids: `dashboard-page`, `dashboard-gadget`, `gadget-add`, `gadget-query-input`, `gadget-save`. Dispatch tokens only, light+dark screenshot-verified. **e2e** (`apps/web/e2e/dashboards.spec.ts`, 10 tests desktop+mobile): create dashboard → STAT gadget with a per-keystroke-typed NLQL query → value matches seeded data; BREAKDOWN-by-status bars; a gadget whose query references a deleted custom field shows a friendly per-gadget error; VIEWER read-only; 393px no overflow — all green; `nav-sidebar`/`workspace-switcher`/`board` regression suites re-verified green; `tsc --noEmit` + build clean both apps. Phase 2 (more visualization types, cross-workspace gadget scoping, and revisiting drag-reorder/pre-built defaults) is the tracked follow-up below. [product-auditor Pass 8/9/10/11 flat-3 finding; VISION.md § Better-than-Jira gaps #4; founder directive 2026-07-02]
- [x] (P2, M) **MCP coverage parity sweep** ✅ shipped 2026-07-02 (founder-flagged — "most should be wired into the mcp") — `@next-lane/mcp` grew from the 18 tools first shipped (2026-06-29) to 55 organically, feature-by-feature, with no standing audit keeping it in lockstep with the product surface. Audited the 55 tools against the shipped feature surface and closed every gap the founder directive scoped (parity table below); shipped **21 new tools** (12 read / 9 write) plus a parity fix to the two existing issue tools, bringing the total to **76** (33 read / 43 write). New read tools: `list_issue_github_links`, `list_quick_links`, `get_personal_board`, `list_issue_templates`, `get_project_analytics`, `get_my_analytics`, `get_velocity_report`, `get_burndown_report`, `get_cfd_report`, `list_notifications`, `get_unread_notification_count`, `get_project_csv` (returns raw CSV text, not JSON-wrapped). New write tools: `create_quick_link`/`update_quick_link`/`delete_quick_link`, `create_personal_card`/`update_personal_card` (move via `columnId`+`beforeId`/`afterId`), `create_issue_from_template`, `bulk_update_issues`, `mark_notification_read`/`mark_all_notifications_read`. Parity fix: `create_issue`/`update_issue` now also accept `originalEstimateMinutes` (time-tracking estimate) and `create_issue` accepts `customFields` (already on `update_issue`). Deliberately **not** wired, as secret-bearing/admin-only or destructive-without-confirmation (noted per the acceptance criteria rather than silently skipped): configuring the GitHub integration (`PUT`/`DELETE /projects/:id/github` — sets/rotates the webhook secret, and even the `GET` leaks the plaintext secret to project admins) — only the secret-free `list_issue_github_links` read is exposed; workspace/project deletion (already out of scope from prior MCP work); planning poker (stateful realtime session, not a natural stdio tool call); attachments (binary upload, not a natural JSON tool-call payload); webhooks/API-token management (self-referential/secret-bearing, consistent with the existing skip). Deferred as follow-ups (not in this sweep's explicit scope, no existing tools to build on yet): async standups, audit log, workspace membership/role admin, roadmap-timeline read, and configurable dashboards (tracked in its own "Configurable dashboards — Phase 1" item above — its acceptance criteria already calls for dashboard CRUD as MCP tools once that ships). Full gap table below. New tools follow the exact existing `client.ts`/PAT-auth pattern (`jsonResult`/`ToolDef` shape, snake_case names, read-vs-write grouping); 33 new/changed vitest unit tests mirroring the existing per-tool style (URL/method/body assertions, `tools/index.test.ts`, 53 tests total, all green); live-smoked every new tool end-to-end against the running API (`:4000`) with a fresh demo-user PAT before committing (personal board create+move, quick-link CRUD, issue-template create-from-template, bulk update, notifications, CSV export, all four report endpoints) — PAT revoked after the smoke. `apps/mcp/README.md` tool tables + a new "Not exposed over MCP (by design)" section updated; `pnpm --filter @next-lane/mcp build`/`lint`/`test` clean. **Standing rule (recorded in `docs/ROADMAP.md` § Current focus):** every new feature's definition of done includes MCP exposure where it makes sense, so this gap doesn't reopen.

  **Parity table (feature → MCP tool?):**

  | Feature (already-shipped surface) | Has MCP tool? |
  | --- | --- |
  | Issues (CRUD/list/get/search/move/delete) | Y — `list_issues`, `get_issue`, `create_issue`, `update_issue`, `move_issue`, `delete_issue`, `search_issues` |
  | Issue links | Y — `list_issue_links`, `link_issues`, `unlink_issues` |
  | Sprints | Y — `list_sprints`, `create_sprint`, `update_sprint` |
  | Comments | Y — `list_comments`, `add_comment` |
  | Labels | Y — `list_labels`, `create_label`, `add_issue_label`, `remove_issue_label` |
  | Statuses | Y — `list_statuses`, `create_status`, `update_status` |
  | Workflows / transitions / gates | Y — `list_workflows`, `get_workflow`, `create_workflow(_from_template)`, `update_workflow`, `delete_workflow`, `add/update/delete_workflow_transition`, `assign_board_workflow` |
  | Boards | Y — `list_boards`, `create_board`, `update_board` |
  | Automations | Y — `list_automations`, `create_automation` |
  | Saved filters (NLQL) | Y — `list_saved_filters`, `create_saved_filter` |
  | Components | Y — `list_components`, `create_component` |
  | Versions/releases | Y — `list_versions`, `create_version`, `set_issue_versions` |
  | Checklists | Y — `list_checklist`, `add_checklist_item`, `update_checklist_item` |
  | Worklogs | Y — `list_worklogs`, `add_worklog` |
  | Custom fields | Y — `list_custom_fields`, `create_custom_field` |
  | **GitHub links (read)** | **Y (new)** — `list_issue_github_links` |
  | **GitHub integration config (write/PUT/DELETE + admin GET)** | **N — not agent-appropriate**: admin-only and leaks the plaintext webhook secret to admins on GET; manage from Settings in the web app |
  | **Quick links (per-user CRUD)** | **Y (new)** — `list_quick_links`, `create_quick_link`, `update_quick_link`, `delete_quick_link` |
  | **Personal boards (list/create/move cards)** | **Y (new)** — `get_personal_board`, `create_personal_card`, `update_personal_card` |
  | **Issue templates (list + create-issue-from-template)** | **Y (new)** — `list_issue_templates`, `create_issue_from_template` |
  | **Time-tracking estimate field** | **Y (new)** — `create_issue`/`update_issue` now accept `originalEstimateMinutes` |
  | **CSV export trigger** | **Y (new)** — `get_project_csv` (raw CSV text result) |
  | **Bulk update** | **Y (new)** — `bulk_update_issues` |
  | **Project analytics/reports (read)** | **Y (new)** — `get_project_analytics`, `get_my_analytics`, `get_velocity_report`, `get_burndown_report`, `get_cfd_report` |
  | **Notifications (list + mark read)** | **Y (new)** — `list_notifications`, `get_unread_notification_count`, `mark_notification_read`, `mark_all_notifications_read` |
  | Dashboards/gadgets | **Y (shipped 2026-07-02)** — `list_dashboards`, `get_dashboard`, `get_dashboard_data`, `create_dashboard`, `update_dashboard`, `delete_dashboard`, `create_dashboard_gadget`, `update_dashboard_gadget`, `delete_dashboard_gadget` (see "Configurable dashboards — Phase 1" above) |
  | Planning poker | N — not agent-appropriate: stateful realtime collaborative session, not a natural stateless tool call |
  | Attachments | N — not agent-appropriate: binary file upload, not a natural JSON stdio tool-call payload |
  | Webhooks / API tokens (self) | N — not agent-appropriate: secret-bearing / self-referential (a PAT managing PATs), consistent with the pre-existing skip |
  | Async standups, audit log, workspace membership/role admin, roadmap-timeline read | N — out of this sweep's explicit scope; deferred as follow-ups, no directive coverage yet |

  [founder directive 2026-07-02]
- [x] (P1, M) **CSP blocks the dark-mode inline bootstrap in the Docker artifact** ✅ shipped 2026-07-02 — `apps/web/index.html:24-41` shipped a synchronous inline `<script>` (the no-FOUC theme bootstrap) while `apps/web/nginx.conf`'s CSP sets `script-src 'self'` with no `unsafe-inline`/nonce/hash — deterministically blocked in every real Docker deployment (dark mode still worked via React's `ThemeProvider` `useEffect` fallback, but every reload with a dark preference flashed light first + logged a CSP violation), invisible to `theme.spec.ts` because `playwright.config.ts`'s `webServer` runs `vite preview`, which serves no CSP header at all — the exact "green tests, broken shipped artifact" failure class this project already paid to learn once (AUDIT-ENGINEERING.md Pass 12, P1-1). Fixed by moving the bootstrap to `apps/web/public/theme-init.js` (byte-identical logic), loaded via a normal BLOCKING `<script src="/theme-init.js">` in `<head>` before the app bundle — an external, self-hosted script satisfies `script-src 'self'` outright, no hash/nonce plumbing needed, same no-flash guarantee since it's still synchronous and pre-bundle. Hardened the regression guard past the single instance: `scripts/smoke-web-csp.sh` gained a mode-3 check that fetches the real served `index.html` + `Content-Security-Policy` header and FAILS if any inline `<script>` (no `src=` attribute) exists while `script-src` lacks `unsafe-inline`/a `nonce-`/a `sha256-` hash — verified it correctly flags the old inline-script code and passes on the fix (isolated logic test, both directions). Also added `apps/web/e2e/csp-artifact.spec.ts` — serves the real built `dist/` bundle via a tiny Node static server that sets the production CSP header, seeds `localStorage['nl.theme']='dark'` via `context.addInitScript` (runs before any page script), and asserts zero `securitypolicyviolation` events with `script-src` in the directive plus `document.documentElement.classList.contains('dark')` true — verified it fails when the old inline-script shape is restored and passes on the fix, desktop + mobile. `theme.spec.ts` stays green unmodified against `vite preview`. [engineering-auditor Pass-12 P1-1]
- [x] (P1, M) **Configurable dashboards — zero realtime coverage** ✅ shipped 2026-07-02 — Dashboards (this wave's flagship feature) never subscribed to the project's socket room and never emitted any event on CRUD, so gadget data (e.g. a STAT count of open issues) could go stale indefinitely with the tab open — no `staleTime` refetch, no refocus refetch, no socket path — worse than any entity Pass 11 flagged for the same gap (AUDIT-ENGINEERING.md Pass 12, P1-2). Fixed, mirroring the `project.updated` pattern that shipped cleanly the pass before: `SocketEvents.DashboardUpdated = 'dashboard.updated'` added to `packages/shared` (payload `{ dashboardId }`), emitted from `DashboardsService` on every dashboard/gadget CRUD mutation (`createDashboard`/`updateDashboard`/`deleteDashboard`/`createGadget`/`updateGadget`/`deleteGadget`) to the project room via the newly-injected `RealtimeService`. `DashboardsPage` now calls `useBoardRealtime(projectId)`; `apps/web/src/api/socket.ts`'s central event handler invalidates the whole `['dashboardData']` query-key family (new `invalidateDashboardDataFamily()` helper, mirroring `invalidateBoardFamily`'s "invalidate the family since the mutation doesn't know which dashboards are open" shape) on any `issue.*` event for the project, and invalidates `qk.dashboards(projectId)` + the specific `qk.dashboard`/`qk.dashboardData` on `dashboard.updated`. 6 new unit tests assert the emission (dashboard/gadget CRUD → `emitToProject(projectId, 'dashboard.updated', { dashboardId })`, exactly once per mutation) — 1521 API tests green. New e2e case in `apps/web/e2e/dashboards.spec.ts`: create a matching issue via the API while a STAT gadget's dashboard is open (no `page.reload()` anywhere in the test) → the gadget's count updates from 1 to 2 on its own within a 20s budget via the socket path — green desktop + mobile alongside the other 5 dashboards specs. Still open, not part of this fix: a per-project/per-dashboard gadget cap (engineering P2-2, currently unbounded) — tracked separately. [engineering-auditor Pass-12 P1-2]
- [x] (P2, S) **Mobile breadcrumb: truncate the workspace name, not the project name** ✅ shipped 2026-07-02 — new shared `<ProjectBreadcrumb>` component (`apps/web/src/components/project/ProjectBreadcrumb.tsx`) replaces the duplicated "Projects / {name}" markup on all 11 project-scoped page shells; `AppHeader`'s breadcrumb slot now wraps to its own full-width row below the icon row at mobile widths (`flex-wrap` + `order-last w-full` below `sm:`) instead of competing inline with the workspace chip + action icons, and "Projects" collapses to a back-chevron icon with badges hidden below `sm:` so the project name gets the room; `apps/web/e2e/mobile-breadcrumb.spec.ts` asserts the full project name renders verbatim at 393px (`project-breadcrumb-name` testid) plus a desktop no-regression check (both green desktop+mobile); `tsc --noEmit` + build clean. — Original scope: at 393px the header collapsed "Bravo Project" to unreadable "Pro..." while the chip is also truncated, leaving a mobile user with no legible "where am I" signal. **Acceptance:** at 393px, the project name in the breadcrumb (the primary "where am I" signal on a project-scoped page) is never truncated below ~15 visible characters — the workspace chip absorbs the truncation instead, or collapses to an icon + tap/long-press tooltip; a Playwright text-content assertion at 393px confirms; no desktop regression. [product-auditor Pass-11 rank #4, P2/S; VISION.md § Better-than-Jira gaps #5]

- [x] (P2, S) **Workspace `uniqueSlug` read-then-write race** — `workspaces.service.ts` `uniqueSlug()` did findUnique-then-create; concurrent same-name creates raced to a spurious 409/500. Fixed: `create()` catches Prisma P2002 and retries with a fresh suffix (bounded at 3 retries); 3 unit tests (retry uses a different slug, bounded give-up, non-P2002 not retried); 1338 API tests green. ✅ shipped 2026-07-02 [pass-11 gate-hardening finding]
- [x] (P1, S) **Personal board: spaces unusable in edit modal** (founder-reported) — dnd-kit KeyboardSensor preventDefaulted Space (drag activator) for keydowns bubbling from the edit modal through the sortable card wrapper (portal events bubble via the React tree). Fixed with shared `EditableSafeKeyboardSensor` (`apps/web/src/lib/dndSensors.ts`) ignoring editable/dialog targets, applied to PersonalBoardPage + BoardPage + BoardSwimlanesView; per-keystroke regression e2e added (`.fill()` was masking it); 22/22 personal-board e2e green desktop+mobile. ✅ shipped 2026-07-02
- [x] (P1, M) **Workspace chip sync — remaining seven pages** — Both independent Pass-11 audits found the same gap: `useSyncActiveWorkspace` had only been wired into 8 of 15 workspace/project-scoped pages, so deep-link/bookmark/back-forward to Reports, Roadmap, Project Analytics, Workspace Members, Workspace Audit Log, Workspace Settings, or Workspace Branding left the header chip misreporting the active workspace. Wired the hook into all seven, top-level of each component (project-scoped pages read `boardQuery.data?.project.workspaceId`; workspace-scoped pages read `workspaceId` straight off `useParams`, which is authoritative for their route). Also removed a surprising side effect in `WorkspaceBrandingPage`'s `ColorSection`: saving/resetting the brand color previously called `setActiveWorkspaceId(workspaceId)` as a byproduct of the mutation — redundant now that the page syncs on load, so it's gone. Deleted `DashboardPage.tsx` (confirmed dead: unreferenced anywhere, re-implemented the original unsynced-selector bug). Extended `workspace-switcher.spec.ts` with a deep-link-to-`/workspaces/:id/settings` regression case; 17/17 green desktop + mobile (was 16/16); `tsc --noEmit` clean. ✅ shipped 2026-07-02 [product-auditor + engineering-auditor Pass 11]
- [x] (P1, M) **Cross-page state-coherence QA gate + workspace-selector correctness fix** — the founder-reported root bug (header chip and the Pulse dashboard's own `<select>` were two independent, unsynced "active workspace" copies; no persistence across reload; chip misreported the workspace on a project board). `WorkspaceContext` made the single source of truth, persisted to `localStorage` (`nl.activeWorkspaceId`) and restored synchronously on load, healing to the first workspace only when the stored one is gone (covers delete-active-workspace and delete-last-workspace); switching via the chip navigates home so content re-scopes; reusable `useSyncActiveWorkspace(workspaceId)` hook introduced. Closed the process gap that let it through: permanent regression suite `apps/web/e2e/workspace-switcher.spec.ts` (7 scenarios × desktop + mobile = 14 checks — chip/dashboard agreement, switch-via-either-surface re-scopes content, reload persistence, deep-link chip sync, delete-active-workspace heal, single-workspace chip-as-link); `qa-tester`/`playwright-qa` now mandate a cross-page coherence matrix for any global/stateful UI. ✅ shipped 2026-07-01 [founder QA report; commits `c8bf9c8`, `12c7d5a`]
- [x] (P2, S) **Blocked-badge clears when the blocker resolves** — the board's `blockedByCount` counted every `BLOCKS` link regardless of the blocker's status, so the red "Blocked" badge stayed forever even after the blocking issue moved to Done. The filtered Prisma `_count` now adds `source: { status: { category: { not: DONE } } }`. Board e2e extended: after moving the blocker to Done the badge disappears on reload. 43 board unit tests + 66 board/workspace-switcher e2e green desktop + mobile. ✅ shipped 2026-07-02 [engineering-auditor Pass-11 P2-3]
- [x] (P2, S) **`qk.boardView` cache-family invalidation helper (`invalidateBoardFamily`)** — `useUpdateIssue`, `useBulkUpdateIssues`, `useAssignIssueToSprint` (`issues.ts`), `useUpdateIssueCustomFields` (`custom-fields.ts`), and all three sprint mutations (`sprints.ts`) only invalidated the legacy `qk.board(projectId)` key — a cache nothing renders from once a non-default board is selected — leaving the visible board stale for up to the global 30s `staleTime` after issue updates, bulk edits, sprint start/complete, and custom-field value changes. New `invalidateBoardFamily(qc, projectId)` helper in `apps/web/src/api/keys.ts` invalidates the legacy key AND every board-id-keyed `boardView` entry; applied to all six mutation sites. 66 e2e green desktop+mobile; `tsc --noEmit` clean. ✅ shipped 2026-07-02 [engineering-auditor Pass-11 P2-2 / Ideation #2]

- [x] (P1, L) **Configurable workflows — schema (Phase 1)** — `Project.workflowEnforced Boolean @default(false)`, `WorkflowTransition` model (id/projectId/fromStatusId?/toStatusId/issueType?/name?/gates Json/createdAt/updatedAt); relations on Project + Status; unique `(projectId, fromStatusId, toStatusId, issueType)` NULLS NOT DISTINCT; Prisma client generated; shared contract final in `@next-lane/shared`: `WorkflowDto`, `WorkflowTransitionDto`, `WorkflowGateDto`, `WorkflowGateType` enum, `WORKFLOW_GATE_TYPES`, `ProjectDto.workflowEnforced`. ✅ shipped 2026-06-28
- [x] (P1, L) **Configurable workflows — backend (Phase 2)** — `WorkflowModule` (`apps/api/src/workflows/`): `GET /projects/:projectId/workflow` → `WorkflowDto`; `PATCH /projects/:projectId/workflow` (Admin; auto-seeds permissive default set when enabling with 0 transitions); `POST /projects/:projectId/workflow/transitions` (Admin; validates status ownership, issueType, gate params; 409 on duplicate); `PATCH /workflow/transitions/:id` (Admin; partial update + re-validate); `DELETE /workflow/transitions/:id` (Admin; 204). Enforcement: `WorkflowService.enforceTransition()` called from `IssuesService.move()` + `update()` BEFORE the status write; automation-originated moves bypass enforcement (`opts.automated === true`); gate semantics: REQUIRE_ASSIGNEE / REQUIRE_DESCRIPTION / REQUIRE_FIELD (core + custom-field key) / REQUIRE_LINK (linksFrom+linksTo) / REQUIRE_NO_OPEN_BLOCKERS (linksTo where type=BLOCKS and source status != DONE); 422 on gate failure with descriptive message; `ProjectDto.workflowEnforced` added to `toProjectDto` mapper; `WorkflowModule` wired into `AppModule`; `IssuesModule` imports `WorkflowModule` via `forwardRef`; 47 new unit tests (workflow.service.spec + workflow.enforcement.spec); existing 670 issue tests updated with `noOpWorkflow` stub; 762 total tests green; build clean. ✅ shipped 2026-06-28
- [x] (P1, M) **Configurable workflows — frontend UI (Phase 3)** — `apps/web/src/api/workflow.ts`: `useWorkflow`, `useSetWorkflowEnforced`, `useCreateWorkflowTransition`, `useUpdateWorkflowTransition`, `useDeleteWorkflowTransition`; `WorkflowSection` component (`apps/web/src/components/settings/WorkflowSection.tsx`) wired into Settings page (ADMIN-only edit; MEMBER/VIEWER read-only): enforcement toggle (`data-testid="workflow-enforce-toggle"`), transition graph grouped by from-status, add/edit/delete transitions modal (`data-testid="workflow-add-transition"`, `workflow-transition-row`, `workflow-gate-add`, `workflow-save-transition`); gate editor with per-type param inputs (REQUIRE_FIELD → field key input, REQUIRE_LINK → link-type input); 409 duplicate → friendly toast; 422 workflow-enforcement + gate-failure messages forwarded verbatim via existing `errorMessage(err)` in all move/update error handlers (board drag-drop, card status picker, triage s-picker, IssueDetailDrawer); optimistic rollbacks already present in `useMoveIssue`; build clean. ✅ shipped 2026-06-28
- [x] (P2, S) **Filter-state URL persistence** — board filter state (NLQL `?q=`, title search `?s=`, assignee `?assignee=`, labels `?labels=`, types `?types=`, priorities `?priorities=`, quick-filter presets `?presets=`) synced to `useSearchParams`; URL is single source of truth; existing `?issue=`/`?new=` preserved; clean URLs (empty params omitted); back/forward navigates filter history; `replace:true` for typing, `replace:false` for toggles. Build clean; 17 e2e green. ✅ shipped 2026-06-28
- [x] (P1, M) **Board swimlanes (group-by)** — horizontal swimlane grouping by Assignee / Priority / Issue type / Epic; URL `?group=` param (same `setFilterParam` pattern); `computeLanes()` pure function; each lane is an independent `DndContext` (cross-lane DnD out of scope v1); lane header: group label + count badge + Avatar for Assignee lanes; only non-empty lanes rendered; Dispatch ink-50/ink-200 lane header band; `GroupBySelector` toolbar dropdown (`data-testid="swimlane-groupby"`); `data-testid`: `swimlane-lane`, `swimlane-lane-header`; mobile: lanes stack, each horizontally scrollable; `apps/web/src/components/board/BoardSwimlanesView.tsx`; build clean; 27/28 e2e green. ✅ shipped 2026-06-28
- [x] (P2, S) **Notifications center page (`/notifications`)** — full notification history at a dedicated route (previously only the bell dropdown's recent slice was reachable). Lists up to the 50 most-recent notifications (server cap; pagination is a backend follow-up), client-side type filter pills (All / Assigned / Mentioned / Commented / Watching) with per-type counts, bulk "Mark all read", per-row open-and-mark-read deep-link to the issue. Reachable via a new "See all notifications" footer link in `NotificationBell`. Dispatch tokens, accessible list/radio semantics, visible focus, responsive (mobile overflow clipped via root `overflow-x-clip`, matching Board/Settings). `apps/web/src/pages/NotificationsPage.tsx`, `App.tsx` route, `NotificationBell` link; 3 e2e (desktop list/filter/mark-all + bell nav + mobile no-overflow) green; build clean. ✅ shipped 2026-06-28 [product-auditor Pass 8 gap #8]
- [x] (P1, M) **Components backend** (2026-06-28) — `ComponentsModule` (`apps/api/src/components/`): `GET /projects/:projectId/components` (VIEWER+, ordered by name asc), `POST /projects/:projectId/components` (ADMIN; 409 on duplicate name within project; `defaultAssigneeId` workspace-member check or 400), `PATCH /components/:id` (ADMIN; re-validate uniqueness + assignee membership; 404 if not found/foreign), `DELETE /components/:id` (ADMIN; 204; onDelete SetNull keeps issues coherent). Shared types: `ComponentDto` / `CreateComponentDto` / `UpdateComponentDto` added to `@next-lane/shared`. Issue integration: `IssueDto.componentId` (required, nullable) + `IssueDto.component?: { id, name } | null` in shared types + mapper; `CreateIssueDto.componentId` (string?) + `UpdateIssueDto.componentId` (string | null?); `assertSameProject` extended with `componentId`; default-assignee auto-assignment when `componentId` set + no `assigneeId` on create. 40 new unit tests (36 ComponentsService/DTO + 4 IssuesService); 849 total green; `tsc --noEmit` + build clean. [product-audit P1 #4]
- [x] (P1, M) **Components frontend** (2026-06-28) — `apps/web/src/api/components.ts` (`useComponents`/`useCreateComponent`/`useUpdateComponent`/`useDeleteComponent`; `qk.components(projectId)` key); `ComponentsSection` Settings card (ADMIN create/edit/delete, MEMBER/VIEWER read-only list, 409 duplicate friendly toast, ConfirmDialog delete, `data-testid`: `components-section`, `component-add`, `component-row`, `component-name-input`, `component-save`); Component picker in `IssueDetailDrawer` sidebar (`data-testid="issue-component-picker"`, select with None + project components, calls `useUpdateIssue` with `componentId`); `componentId` added to `UpdateIssueInput`; `useWorkspaceMembers` wired into SettingsPage for default-assignee picker; 5 Playwright e2e (desktop: create/pick/duplicate-error/delete + mobile: no overflow) all green; tsc + build clean. [product-audit P1 #4]
- [x] (P1, M) **Versions / Releases backend** (2026-06-28) — `VersionsModule` (`apps/api/src/versions/`): `GET /projects/:projectId/versions` (VIEWER+, ordered by createdAt asc, `issueCount` via `_count`), `POST /projects/:projectId/versions` (ADMIN; 409 on duplicate name within project), `PATCH /versions/:id` (ADMIN; 404 if not found/foreign; 409 on duplicate name; auto-sets `releaseDate` to now when transitioning to RELEASED with no existing date), `DELETE /versions/:id` (ADMIN; 204; IssueVersion rows cascade). Issue↔version M:N: `PUT /issues/:issueId/versions { versionIds }` atomically replaces the set (MEMBER+; cross-project or missing IDs → 400). `VersionState` enum (UNRELEASED/RELEASED/ARCHIVED) + label map in `@next-lane/shared/enums`; `VersionDto`/`CreateVersionDto`/`UpdateVersionDto` + `IssueDto.versions` added to `@next-lane/shared/types`; mapper + `listInclude` in `IssuesService` extended. 55 new unit tests; 904 total green; tsc clean. ✅ shipped 2026-06-28
- [x] (P2, M) **Versions / Releases frontend** (2026-06-28) — `apps/web/src/api/versions.ts` (`useVersions`/`useCreateVersion`/`useUpdateVersion`/`useDeleteVersion`/`useSetIssueVersions`; `qk.versions(projectId)` key); `VersionsSection` Settings card (ADMIN create/edit/delete modal with name+description+releaseDate; state badge UNRELEASED neutral/RELEASED emerald/ARCHIVED muted; issue count + releaseDate per row; Release + Archive state-transition actions; MEMBER/VIEWER read-only; 409 duplicate friendly toast; ConfirmDialog delete; `data-testid`: `versions-section`, `version-add`, `version-row`, `version-save`, `version-release`); `VersionsField` multi-select in `IssueDetailDrawer` sidebar (`data-testid="issue-versions-picker"`, chip display with state badge, dropdown listbox, `useSetIssueVersions` PUT mutation); `VersionState`/`VERSION_STATE_LABELS` from `@next-lane/shared`; 6 Playwright e2e (desktop: create/release/assign/duplicate-error/delete + mobile no-overflow) all green; tsc + build clean. ✅ shipped 2026-06-28
- [x] (P1, S) **WIP limits — schema + backend** (2026-06-28) — `wipLimit Int?` added to `Status` model; migration `20260628120000_add_status_wip_limit` applied and verified against live DB; Prisma client regenerated; `wipLimit: number | null` in `StatusDto` (shared types); `CreateStatusDto`/`UpdateStatusDto` wired with `@IsOptional @IsInt @Min(1)` + null-clear via `@ValidateIf`; `toStatusDto` updated in all mappers; 14 new unit tests (918 total); tsc clean. ✅ shipped 2026-06-28 [product-audit #7]
- [x] (P2, M) **Checklists backend** (2026-06-28) — `ChecklistModule` (`apps/api/src/checklist/`): `POST /issues/:issueId/checklist` (MEMBER+; order = max+1, or 0 for first item), `PATCH /checklist/:itemId` (MEMBER+; edit text / toggle done / set order; tenant check: item→issue→project), `DELETE /checklist/:itemId` (MEMBER+; 204), `PUT /issues/:issueId/checklist/reorder { itemIds }` (MEMBER+; validates all ids belong to issue); `ChecklistItem` Prisma model (id/issueId/text/done/order/timestamps; `@@index([issueId])`; onDelete:Cascade); migration `20260628130000_add_checklist_items` — table verified live (count/create/read/delete round-trip). `toChecklistItemDto` mapper; `checklistItems` added to `listInclude` in `IssuesService` (orderBy order asc); `IssueDto.checklist?: ChecklistItemDto[]` + `IssueDto.checklistProgress?: { done, total }` added to `@next-lane/shared/types` + mapper. `ChecklistItemDto`/`CreateChecklistItemDto`/`UpdateChecklistItemDto` in shared types. `ChecklistModule` wired into `AppModule`. 40 new unit tests (checklist.service.spec.ts × 26 + checklist.dto.spec.ts × 14); 958 total green; `tsc --noEmit` clean. ✅ shipped 2026-06-28 [Phase 5 parity]
- [x] (P2, M) **Checklists frontend** (2026-06-28) — `ChecklistSection` in `IssueDetailDrawer` main column (between AttachmentsPanel and CommentsPanel): progress indicator (`done/total` text + slim emerald `role=progressbar`/aria progress bar, hidden when total=0); per-item `<input type=checkbox>` (optimistic done toggle), text label (strikethrough+muted when done), delete button (hover/focus-visible, MEMBER+ only); empty state placeholder; "Add item" controlled input (per-keystroke safe, Enter or Add button submits, clears on success, focus preserved); `apps/web/src/api/checklist.ts` (`useChecklist`, `useAddChecklistItem`, `useUpdateChecklistItem`, `useDeleteChecklistItem`, `useReorderChecklist`); `qk.checklist(issueId)` added to `keys.ts`; `data-testid` hooks: `checklist-section`, `checklist-progress`, `checklist-add-input`, `checklist-item`, `checklist-item-checkbox`, `checklist-item-delete`; `apps/web/e2e/checklist.spec.ts` (3 tests: desktop add+toggle+progress+delete, viewer read-only, mobile no-overflow — all green); tsc clean; build clean. ✅ shipped 2026-06-28 [Phase 5 parity follow-up]
- [x] (P2, S) **WIP limits — UI indicator** — `BoardColumn.tsx` renders `count / limit` chip (`data-testid="column-wip-indicator"`) with `bg-red-50 text-red-700 ring-red-200` over-limit danger tokens and `aria-label="N of M, over limit"` for a11y; no chip when `wipLimit` is null. `ColumnFormModal.tsx` adds "WIP limit (optional)" numeric input (`data-testid="column-wip-limit-input"`); `UpdateStatusInput`/`CreateStatusInput` extended with `wipLimit: number | null`; client-side validation guards < 1. 5 Playwright e2e tests (desktop + mobile) all green; tsc + build clean. ✅ shipped 2026-06-28 [product-audit #7]
- [x] (P2, M) **Issue templates backend** (2026-06-29) — `IssueTemplatesModule` (`apps/api/src/issue-templates/`): `GET /projects/:projectId/issue-templates` (MEMBER+; ordered by name asc), `POST /projects/:projectId/issue-templates` (ADMIN; 409 on duplicate name; `defaultAssigneeId` workspace-member check or 400; `componentId` project-ownership check or 400; `labelIds` project-ownership check or 400), `PATCH /issue-templates/:id` (ADMIN; same validations re-run; 404 if not found or tenant-foreign), `DELETE /issue-templates/:id` (ADMIN; 204), `POST /issue-templates/:id/create-issue` (MEMBER+; title resolution: dto.title > titleTemplate > 400; per-field overrides beat template; labels attached via `IssueLabel.upsert` after `IssuesService.create`; full `IssueDto` returned via `IssuesService.findOne`). `toIssueTemplateDto` mapper: `toUserDto` for `defaultAssignee`, `parseLabelIds` (Prisma Json→string[]). `IssueTemplatesModule` wired into `AppModule`; imports `IssuesModule` (already exports `IssuesService`). 72 new unit tests (35 service + 37 DTO); 1030 total green; `tsc --noEmit` clean (no new errors). ✅ shipped 2026-06-29
- [x] (P1, L) **Per-board named workflows backend** (2026-06-29) — Named `Workflow` entity CRUD (`GET/POST /projects/:projectId/workflows`, `GET/PATCH/DELETE /workflows/:id`) with `transitionCount`/`boardCount` rollup counts (ADMIN write, VIEWER+ read); workflow-scoped transition CRUD (`POST /workflows/:id/transitions`, `PATCH/DELETE /workflow-transitions/:id`) on non-colliding paths (hyphen) vs legacy project-level endpoints; seed-from-template (`POST /projects/:projectId/workflows/from-template`) for four named templates: `simple` (2 transitions, linear), `kanban` (any-to-any), `scrum` (4 transitions, linear + back-transitions), `bug-triage` (3 transitions including DONE→TODO reopen); templates gracefully skip transitions whose statuses don't exist in the project. Board workflow assignment: `workflowId?: string | null` added to `UpdateBoardDto` (`@ValidateIf` null-passthrough), `BoardService.updateBoard` validates workflow belongs to the same project or throws `BadRequestException`, uses Prisma `{ connect }` / `{ disconnect: true }` relation syntax; `toBoardSummaryDto` returns `workflowId: string | null`. Board-aware enforcement: `boardId?` added to `MoveIssueDto`; `enforceMove` private method in `IssuesService.move` resolves three branches — (1) board has enforced named workflow → `enforceTransitionForWorkflow`; (2) board has no workflow or non-enforced → project-level `enforceTransition`; (3) no boardId → project-level `enforceTransition`; `opts.automated` bypass preserved on all paths. `WorkflowService.enforceTransitionForWorkflow(workflowId, issueId, targetStatusId)` mirrors project-level enforcement scoped to the workflow entity's transitions (same-status no-op, gate checks, 422 on blocked). 3 new Jest spec files: `named-workflow.service.spec.ts` (CRUD + templates + enforcement; 32 tests), `board-workflow-assignment.spec.ts` (assign/clear/cross-project/DTO serialization; 8 tests), `issues-board-enforcement.spec.ts` (all 3 routing branches + automation bypass + same-status; 7 tests); 1085 unit tests total; `tsc --noEmit` clean. Satisfies the founder's "workflows for the progression of a ticket / editable per board" request. ✅ shipped 2026-06-29
- [x] (P2, M) **Time tracking backend** (2026-06-29) — `WorkLogsModule` (`apps/api/src/work-logs/`): `GET /issues/:issueId/worklogs` (VIEWER+; ordered workedAt desc), `POST /issues/:issueId/worklogs` (MEMBER+; `minutes ≥ 1`, userId=current user, optional note+workedAt), `PATCH /worklogs/:id` (author OR project ADMIN; ForbiddenException otherwise), `DELETE /worklogs/:id` (author OR project ADMIN; 204); tenant isolation: worklog→issue→project membership chain; `toWorkLogDto` mapper with `toUserDto` for user. Issue integration: `originalEstimateMinutes?: number` on `CreateIssueDto` (`@IsInt @Min(0)`); `originalEstimateMinutes?: number | null` on `UpdateIssueDto` (null clears; `@ValidateIf` non-null guard; omit from `OmitType` base to allow null widening); both persisted in `IssuesService.create`/`update`. Time-spent rollup: `workLogs: { select: { minutes: true } }` added to `listInclude` in `IssuesService`; `toIssueDto` mapper already sums `workLogs[].minutes` when relation present → `IssueDto.timeSpentMinutes`. `WorkLogsModule` wired into `AppModule`. 56 new unit tests (work-logs.service.spec × 28 + work-log.dto.spec × 18 + issues.service.spec estimate/rollup × 10); 1141 total green; `tsc --noEmit` clean. ✅ shipped 2026-06-29
- [x] (P2, M) **Time tracking frontend** (2026-06-29) — `TimeTrackingSection` in `IssueDetailDrawer` main column (after ChecklistSection): original estimate inline edit field (`data-testid="estimate-input"`; friendly duration parse/format "2h 30m"/"90m"/"1.5h" via `src/lib/duration.ts`; saves null to clear; on-blur + Enter commit; inline parse-error hint); `role=progressbar` bar (`data-testid="time-progress"`, `aria-valuenow/min/max`) with time-spent vs estimate label ("1h 15m logged of 2h"); over-estimate red danger token; log-work form (`worklog-add-minutes`, `worklog-add-submit`; per-keystroke, Enter submits, < 1 min inline error, optional note); worklog list (`worklog-row`; author avatar, duration badge, note, relative time); delete button (`worklog-delete`) with `ConfirmDialog` guard (author or ADMIN; VIEWER read-only); `apps/web/src/api/worklogs.ts` (`useWorkLogs`, `useAddWorkLog`, `useUpdateWorkLog`, `useDeleteWorkLog`; invalidates issue + worklogs on success; optimistic delete); `qk.worklogs(issueId)` key; `UpdateIssueInput.patch.originalEstimateMinutes` added; 3 e2e green (desktop: estimate + log 30m+45m + progress check + delete; seeded list; mobile 390px no-overflow); `tsc --noEmit` + build clean. ✅ shipped 2026-06-29
- [x] (P1, L) **Per-board named workflows frontend** (2026-06-29) — `apps/web/src/api/workflows.ts`: `useWorkflows`, `useWorkflowDetail`, `useCreateWorkflow`, `useUpdateWorkflow`, `useDeleteWorkflow`, `useCreateWorkflowFromTemplate`, `useAddWorkflowTransition`, `useUpdateWorkflowTransition`, `useDeleteWorkflowTransition`, `useAssignBoardWorkflow`; `qk.workflows(projectId)` + `qk.workflow(id)` keys in `keys.ts`. `WorkflowsManager` section in project Settings (`data-testid="workflows-manager"`) — list with enforced badge + transition/board counts, create modal (`workflow-create`), from-template modal (`workflow-from-template`), inline detail panel (enforcement toggle `workflow-enforce-toggle-2`, add/edit/delete transitions reusing WorkflowSection UI, delete workflow with ConfirmDialog; ADMIN write / MEMBER read-only). `BoardWorkflowSelector` in board toolbar (`board-workflow-select` admin select; `board-workflow-badge` read-only badge with enforced styling). `MoveIssueInput.boardId` added to `useMoveIssue` API call so drag-drop + card-status-picker pass board context → named workflow enforcement triggers correctly. `BoardPage.tsx` passes `boardId` in both `handleCardStatusChange` and DnD `handleDragEnd`. `apps/web/e2e/board-workflows.spec.ts` (4 tests: desktop create-from-template, assign-to-board+badge, enforced-422-toast, mobile no-overflow — all green). `tsc --noEmit` + build clean. ✅ shipped 2026-06-29
- [x] (P2, M) **Issue templates frontend** (2026-06-29) — `apps/web/src/api/issue-templates.ts` (`useIssueTemplates`/`useCreateIssueTemplate`/`useUpdateIssueTemplate`/`useDeleteIssueTemplate`/`useCreateIssueFromTemplate` hooks; `qk.issueTemplates(projectId)` key in `keys.ts`); `TemplatesManager` Settings section between ComponentsSection and VersionsSection (ADMIN create/edit/delete modal with name, issueType, priority (optional), titleTemplate, descriptionTemplate, default-assignee picker, component select (optional), label multi-select; MEMBER/VIEWER read-only list; type+priority badge per row; 409 duplicate-name friendly toast; ConfirmDialog delete; `data-testid`: `templates-manager`, `template-add`, `template-row`, `template-name-input`, `template-save`, `template-delete`); `FromTemplateMenu` board toolbar dropdown (`data-testid="new-from-template-menu"`, `"new-from-template-option"`; hidden when no templates; calls `POST /issue-templates/:id/create-issue`, invalidates board+projectIssues caches, opens issue drawer + success toast); `apps/web/e2e/issue-templates.spec.ts` (5 tests: desktop create/duplicate-error/delete/from-template-board + mobile no-overflow — all green); tsc + build clean. [Phase 5 parity] ✅ shipped 2026-06-29
- [x] (P1, M) **CSV import backend** (2026-06-29) — `POST /projects/:projectId/issues/import` (MEMBER+; `issues:write` scope); accepts multipart/form-data `file` field (2 MB cap, `memoryStorage`) OR JSON body `{ csv: string, dryRun?: boolean }`; `?dryRun=true` query param overrides body flag; `csv-parse/sync` RFC-4180 parser (columns:true, skip_empty_lines, relax_column_count, BOM stripping); case-insensitive header mapping; 2000-row hard cap; per-row validation: Title required (1–300 chars), Type/Priority enum (case-insensitive), Status name→id (default TODO-category when blank; 400 for unknown), Assignee email→workspace-member-id (regex-extract + fallback; 400 for unknown), Story Points int 0–999, Due Date ISO 8601; Labels comma-or-semicolon split, case-insensitive create-or-match (auto-creates unknown; skips in dryRun); formula-injection apostrophe stripped on import; per-row errors collected without aborting; returns `ImportIssuesResultDto { created, skipped, errors: { row, message }[], dryRun }`; `ImportIssueRowError` + `ImportIssuesResultDto` added to `@next-lane/shared`; `IssuesImportService` + `IssuesImportController` registered in `IssuesModule`; `csv-parse` dep added; 36 new unit tests (1177 total green); `tsc --noEmit` clean. ✅ shipped 2026-06-29
- [x] (P1, M) **CSV import frontend** (2026-06-29) — `apps/web/src/api/import.ts` (`useImportIssues` hook exposing imperative `dryRun(file)` + `importCsv(file)` helpers; bearer-auth multipart POST to `POST /projects/:id/issues/import[?dryRun=true]` matching the `export.ts` token pattern; on real import invalidates `projectIssues`, `board`, and `boardView` queries); `apps/web/src/components/ImportCsvModal.tsx` (`ImportCsvModal` — file picker `data-testid="import-csv-file"` accepting `.csv`; automatic dry-run on every file pick with `aria-live` validating spinner; preview summary `data-testid="import-csv-dryrun-summary"` showing "{N} issues will be created, {M} skipped"; scrollable per-row error list `data-testid="import-csv-error-row"` in red panel; collapsible "Accepted columns" hint with all 9 accepted column names + Title-required note; "Import" submit button `data-testid="import-csv-submit"` enabled only when dry-run found ≥1 creatable row; "Choose a different file" resets state without closing the modal; import success closes modal + toast "Imported N issues"; real-import failure restores the last preview so user can retry; MEMBER+ only — Import button hidden for VIEWERs); "Import CSV" trigger button `data-testid="import-csv"` placed next to "Export CSV" on BacklogPage toolbar (MEMBER+ only) and BoardPage toolbar (MEMBER+ only); `apps/web/e2e/csv-import.spec.ts` (desktop: trigger visible + modal opens + 3-row CSV dry-run shows 2 creatable + 1 error row + Import clicks + success toast + modal closes + issues appear in backlog; mobile 390px: modal renders without horizontal overflow; `test.skip` guard when API not reachable in CI sandbox); `tsc --noEmit` clean; `pnpm --filter @next-lane/web build` clean. ✅ shipped 2026-06-29
- [x] (P1, M) **Configurable workflows — MCP surface (Phase 4)** — ✅ 2026-06-29: new standalone `@next-lane/mcp` package (`apps/mcp/`), an MCP server (stdio, official SDK + zod) exposing 18 PAT-authenticated read/write tools so external agents (Claude Desktop, Claude Code) can introspect AND modify a project's workflows/transitions/gates + core entities via the REST API. Agent-native differentiator; pairs with Phase 6 MCP-native. [founder request 2026-06-28]
- [x] (P1, L) **Configurable workflows — visual graph builder (Phase 5)** — custom SVG graph editor in `WorkflowGraph.tsx` (no heavy lib dependency); nodes = project statuses auto-laid-out by category column (TODO→IN_PROGRESS→DONE); synthetic "Start" node for `fromStatusId=null`; directed bezier edges with arrowheads; connect handles (real `<button>` inside `<foreignObject>`) for click-source→click-target transition creation; edge delete via (×) affordance with ConfirmDialog guard; gate count badge per node; view toggle (List/Graph) in `WorkflowsManager` detail panel (`data-testid="workflow-graph-toggle"`); mobile-safe `overflow-x-auto` containment; 4 Playwright e2e (desktop: node-render, create-transition, delete-edge; mobile: no-overflow) all green; tsc + build clean. ✅ shipped 2026-06-29 [founder request 2026-06-28]

- [x] (P2, M) **UI design elevation polish pass — Settings + Drawer + Board surfaces** (2026-06-29) — 11 files: `WorkflowsManager`/`WorkflowGraph`/`TemplatesManager`/`ComponentsSection`/`VersionsSection`/`NotificationPreferencesSection` migrated from `slate-*` → `ink-*` tokens; empty states elevated to icon+heading+description dashed-card pattern matching the shared `EmptyState` primitive; workflow graph canvas gains dot-grid `radial-gradient` + node `feDropShadow` SVG filter for depth; `VersionsSection` UNRELEASED badge corrected to `bg-ink-50 text-ink-600 ring-ink-200`; enforcement toggle row dynamic background (`signal-50` when enforced); `TimeTrackingSection` + `ChecklistSection` progress bars gain monospace `%` label (9px tabular-nums, `text-ink-400`); `ChecklistSection` fill uses `bg-emerald-500` at 100% / `bg-signal-500` in progress; `BoardWorkflowSelector` badge uses `ring-1 ring-inset` + inner ENFORCED micro-chip (`bg-signal-100 text-signal-800`); `BoardColumn` WIP over-limit chip gains warning-triangle SVG icon + `ring-1 ring-inset ring-red-200`; `FromTemplateMenu` animation corrected to `animate-nl-fade-in` (system keyframe); `NotificationsPage` issue key spans use `.nl-issue-key` signature class + `bg-signal-50 ring-signal-100`; unread dot gains `motion-safe:animate-pulse`. All `data-testid`/`role`/`aria-label` hooks preserved. `tsc --noEmit` clean; `pnpm --filter @next-lane/web build` clean (CSS 86.63 kB). [UI-REVIEW.md 2026-06-29]

- [x] (P2, M) **UI design elevation pass 2 — older surfaces** (2026-06-29) — 9 files elevated to full Dispatch `ink-*/signal-*` token compliance: (1) `IssueCard.tsx` — due-date chip gains `ring-1 ring-inset` badge vocabulary; overdue: `bg-amber-50 text-amber-700 ring-amber-200`; normal: `ring-ink-200`; story-points chip gains `ring-1 ring-inset ring-ink-200`; (2) `AppHeader.tsx` — all NavLinks, desktop + mobile search buttons, user-menu avatar button gain `focus-visible:ring-2 focus-visible:ring-signal-500 focus-visible:ring-offset-1`; active NavLink adds `ring-1 ring-inset ring-signal-100`; dropdown items gain `focus-visible:bg-ink-50`; (3) `NotificationBell.tsx` — bell trigger + "Mark all read" button gain system focus rings; notification list-item buttons gain `focus-visible:bg-ink-50`; (4) `RoadmapPage.tsx` — all `slate-*` → `ink-*`; Shell breadcrumb gains truncation (`shrink-0`/`min-w-0`/`overflow-hidden`, matches AutomationsPage reference); (5) `LoginPage.tsx` — password label aligned to Field primitive (`text-xs font-medium text-ink-600`); forgot-password link `text-signal-600 font-medium`; footer `ink-*/signal-*`; error banner `role="alert" border border-red-200`; (6) `RegisterPage.tsx` — footer `ink-*/signal-*`; error banner `role="alert"`; (7) `ForgotPasswordPage.tsx` — success-state `slate-*` → `ink-*`; email literal → `<code>` mono chip; footer `ink-*/signal-*`; error banner `role="alert"`; (8) `ApiTokensSection.tsx` — full `slate-*` → `ink-*`; scope pills `bg-signal-50 ring-1 ring-inset ring-signal-100`; status badges `ring-1 ring-inset`; empty state icon+heading+description dashed-card; active dot `bg-emerald-500`/`bg-ink-300`; (9) `WebhooksSection.tsx` — full `slate-*` → `ink-*`; delivery badges `ring-1 ring-inset`; toggle `bg-emerald-500`/`bg-ink-300`; empty state icon+heading+description; `IconButton` `focus-visible:ring-signal-300`. All `data-testid`/role/aria-label hooks preserved; `pnpm --filter @next-lane/web build` clean (CSS 87.23 kB). [UI-REVIEW.md Phase B+C 2026-06-29]

_Docs site spacing/rhythm polish — ✅ shipped 2026-06-28:_

- [x] (P2, S) **Docs site padding/margin polish pass** — Playwright-driven audit at 1320×900 + 390×844 across home, quick-start, configuration, features, faq. 8-point spacing scale tokens (`--sp-1`…`--sp-9`) added to `custom.css` as single source of truth. Fixes: hero→features gap 0px→64px (`VPFeatures padding-block`); feature card box padding 24px flat → `32px 24px 24px`; VitePress negative-margin gutter pattern documented — `gap` overrides on `.items`/`.actions` removed (caused 4th card to wrap); `<pre>` horizontal padding 0→24px; code block `margin-block` 16→32px; lang label repositioned `top:8px right:16px`; table `margin-block` 20→32px, `th/td` padding raised; `h2 margin-top` 40→80px, `padding-top` 24→32px; `VPDoc padding-bottom` 0→80px; footer `padding-block` 32→48px; `.VPHomeContent` capped 768px + 32px gutters; home `<hr>` margins 16→80px. Mobile: all 5 pages zero overflow confirmed. Build clean. [docs-site spacing audit 2026-06-28]

_Docs site QA bug-fix pass — ✅ all shipped 2026-06-28:_

- [x] (P1, S) Fix task lists rendering as literal `[ ]` — installed `markdown-it-task-lists@2.1.1` devDep; wired in `config.ts` via `createRequire` ESM→CJS interop; Quick Start checklist now renders real checkboxes. [docs-site QA 2026-06-28]
- [x] (P1, S) Fix hero image showing login screen instead of board — `index.md` hero `src` changed from `/screenshots/home-desktop.png` to `/screenshots/board-desktop.png`; alt text corrected; `og:image` in `config.ts` also updated. [docs-site QA 2026-06-28]
- [x] (P1, S) Fix table mobile overflow on configuration + self-hosting pages — `.vp-doc table` CSS rule changed from `display:table` (no overflow containment) to `display:block; overflow-x:auto`; wide tables now scroll internally, page body no longer scrolls horizontally at 390px. [docs-site QA 2026-06-28]
- [x] (P2, S) Light-mode hero heading reset — added `:root:not(.dark) .VPHero .name` CSS rule to prevent VitePress default brand-blue bleed-through in light mode; gradient stays dark-only. [docs-site QA 2026-06-28]
- [x] (P2, S) Canonicalize image paths — `./public/screenshots/` and `../public/screenshots/` references in `index.md` and `guide/features.md` changed to root-relative `/screenshots/` (idiomatic VitePress). [docs-site QA 2026-06-28]
- [x] (P2, S) Split long kubectl+python3 command in troubleshooting.md — multi-line form reduces horizontal scroll extent on mobile code blocks. [docs-site QA 2026-06-28]

_Engineering-audit Pass-7 hardening — ✅ all shipped 2026-06-28:_

- [x] (P1, S) Scope `personalAnalytics` to workspaces the user is a member of — `prisma.issue.findMany({ where: { assigneeId: userId } })` had no workspace scope; a user assigned to an issue in a workspace they are not a member of would see it in their personal analytics (open/done/overdue counts, throughput, byType, byPriority, cycle-time); fixed by adding `project: { workspace: { memberships: { some: { userId } } } }` predicate to the query in `analytics.service.ts`; 2 regression tests added [engineering-auditor Pass 7] ✅ shipped 2026-06-28
- [x] (P2, S) `promoteCard` idempotency guard — double-call created a second Issue and orphaned the first; added `if (card.promotedIssueId !== null) throw new BadRequestException('Card already promoted')` inside the transaction in `personal-boards.service.ts`; 1 regression test added [engineering-auditor Pass 7] ✅ shipped 2026-06-28
- [x] (P2, M) `AutomationRun` serial inserts → single `createMany` batch — engine called `prisma.automationRun.create()` once per rule in a loop (N round-trips); refactored to collect run-row objects during the loop and issue one `prisma.automationRun.createMany({ data: [...] })` after all rules evaluated; actions still execute per-rule in order; FAILED mid-rule rows collected and included in batch; 1 new batch-insert regression test + all 37 existing engine tests updated to assert `createMany` instead of `create` [engineering-auditor Pass 7] ✅ shipped 2026-06-28
- [x] (P2, M) `projectAnalytics` query cost reduced + `ActivityLog` composite index — (a) `projectAnalytics` now runs two targeted queries instead of one full-project scan: workload uses full scan (needs all open issues), flow chart uses `createdAt: { gte: wStart }` window-scoped query (eliminates JS-side date filtering of the entire issue table); (b) composite index `ActivityLog(field, to, createdAt)` (migration `20260628080000_add_activitylog_status_index`) added to support the `completionMap` raw query which filters `field='status' AND to = ANY(doneIds) AND createdAt BETWEEN`; index design justification: `(field, to)` equality access on the two most selective columns for status-completion lookups, `createdAt` trailing column supports range scan inside matched rows [engineering-auditor Pass 7] ✅ shipped 2026-06-28

_Security fixes — ✅ all shipped 2026-06-27 (Pass 5 security/perf/infra batch):_

- [x] (P1, S) Guard password reset token log with `NODE_ENV !== 'production'` — raw reset URL (including 32-byte token) is emitted via `logger.log()` with no NODE_ENV guard; pino-http redact rules do not cover explicit logger calls; any log aggregator captures the token in plaintext; add `if (process.env.NODE_ENV !== 'production')` guard; skip log entirely when SMTP_HOST is set [engineering-auditor Pass 5] ✅ shipped 2026-06-27 (Pass 5 security/perf/infra batch)
- [x] (P1, S) Remove `image/svg+xml` from ALLOWED_MIME_TYPES (or serve as `application/octet-stream`) — SVG files with embedded `<script>` tags accepted and served with `image/svg+xml` Content-Type; direct-navigate to the download URL executes script in browser context; `nosniff` partially mitigates but does not close the direct-navigate vector; simplest fix is to remove SVG from the allowlist [engineering-auditor Pass 5] ✅ shipped 2026-06-27 (Pass 5 security/perf/infra batch)
- [x] (P1, M) Rewrite CFD and burndown as DB-level `generate_series` aggregation — `reports.service.ts` fetches all project issues and all activity logs into memory with no `take` limit; for any active project (5k+ issues × 5 status changes each = 25k+ rows) the in-memory O(issues × days) loop will OOM or timeout the API process; replace with a Postgres `generate_series` window-function query bounded by `windowDays × 3` rows regardless of project size [engineering-auditor Pass 5] ✅ shipped 2026-06-27 (Pass 5 security/perf/infra batch)

_Remaining P2 security hardening from Pass 5 (small, high-signal):_

- [x] (P2, S) Add null-file guard in `AttachmentsService.upload` (400 not 500) — POST with no `file` field dereferences `undefined` → TypeError → generic 500 via global filter; add `if (!file) throw new BadRequestException('No file uploaded')` [engineering-auditor Pass 5] ✅ shipped 2026-06-27 (Pass 5 security/perf/infra batch)
- [x] (P2, S) Enqueue only `subscriptionId` in BullMQ job body (not the HMAC secret) — webhook HMAC secrets stored as plaintext JSON in Redis; bundled Helm Redis has no auth by default; enqueue only `subscriptionId` and have the worker re-fetch the secret from DB at delivery time [engineering-auditor Pass 5] ✅ shipped 2026-06-27 (Pass 5 security/perf/infra batch)
- [x] (P2, S) Validate PAT `expiresAt` is a future date (`@MinDate`) — past dates accepted at creation; token immediately expires and is permanently unusable with no error; add `@MinDate` (or equivalent custom decorator) to `CreateApiTokenDto.expiresAt` [engineering-auditor Pass 5] ✅ shipped 2026-06-27 (Pass 5 security/perf/infra batch)
- [x] (P2, S) Add CSP `add_header` to nginx configmap (Kustomize + Helm) — SPA served from nginx without Content-Security-Policy; Helmet covers API responses but not the web container; add `Content-Security-Policy` directive to both `configmap-web-nginx.yaml` and Helm `configmap.yaml` [engineering-auditor Pass 5] ✅ shipped 2026-06-27 (Pass 5 security/perf/infra batch)
- [x] (P2, S) Add fail-fast Helm guard for Postgres default password `nextlane` — `values.yaml` has `password: nextlane` with a "CHANGE ME" comment but no enforcement (unlike JWT secret which has a template validation block); add a required validation that aborts the Helm release when the password is the default [engineering-auditor Pass 5] ✅ shipped 2026-06-27 (Pass 5 security/perf/infra batch)

_Phase 4 observability hooks — ✅ shipped 2026-06-27:_

- [x] (P3, M) Observability hooks — request correlation id (`X-Request-Id` header reuses incoming or generates UUID v4 via `genReqId`; bound to pino `requestId` field on every log line; echoed via `CorrelationIdInterceptor` + `CorrelationIdMiddleware`); enriched `/health` readiness (`{ status, uptime, version, db }`, 3-second `SELECT 1` ping, HTTP 503 on DB down) + `/health/live` liveness (no DB dep, always 200); both endpoints excluded from access-log + `/api` prefix; OTLP trace seam documented (stub — no deps installed; skeleton + env vars in `.env.example` + `DEPLOY-KUBERNETES.md`); 10 new unit tests (health.controller.spec); 349 total tests green. ✅ shipped 2026-06-27

_Product P1s — queue after security batch:_

- [x] (P1, S) Wire SMTP email delivery for password reset (nodemailer into existing stub seam) — `MailModule`/`MailService` added (`apps/api/src/mail/`); nodemailer transport when `SMTP_HOST` set; dev-log fallback when absent; production-safe (token never logged in prod without SMTP); all SMTP_* env vars documented in `.env.example`; `MailService.send()` injected into `PasswordResetService.deliverResetLink()`; 8 new `mail.service.spec.ts` tests + updated `password-reset.service.spec.ts`; 255 unit tests green; build clean; dev-log fallback verified on live instance ✅ shipped 2026-06-27 [product-auditor Pass 5]
- [x] (P1, S) Emit `WATCHED_UPDATED` notification on issue field changes — `IssuesService.update` now fans out to watchers (minus the actor) on meaningful field changes (status/assignee/priority/title/dueDate) via batched `createMany` + realtime, with a human-readable message; 11 unit tests — ✅ shipped 2026-06-27 [product-auditor Pass 5]
- [x] (P1, M) Due date on issues — add optional `dueDate DateTime?` to the `Issue` model (Prisma migration + DTO + drawer date-picker); show a due-date chip on the card when set; flag overdue issues in "My Work" with a warning color; add a due-date sort option to the backlog; the most commonly requested primitive in any issue tracker and the one missing from the schema [product-auditor Pass 4, Pass 5] ✅ shipped 2026-06-27 (migration `20260627220000_add_issue_due_date`; drawer date picker with clear + overdue amber; card chip; My Work sort + badge; 5 unit + 8 e2e)

---

## Next (P2 — Better-than-Jira gap follow-ups, Pass 12 groom)

Phase-2 slices of the Ready-queue items above, plus carried-forward P2 gaps from
Pass 9/10/11 that are real but not yet the highest-leverage next step.

- [x] (P2, S) **Complete the `@RequireScope` PAT-scope rollout on projects/workspaces controllers.** ✅ shipped 2026-07-06 (Hardening Night item 1) — expanded well beyond the original projects/workspaces/reports scope once a full inventory showed the gap was repo-wide: **every** controller under `apps/api/src/*` was audited; the ~30 that carried zero `@RequireScope` decorators (admin-settings, analytics, api-tokens, attachments, audit, automations, board, checklist, comments, components, custom-fields, dashboards, issue-templates, labels, notifications, personal-boards[promote route], poker, projects, reports, roadmap, saved-filters, search, share-tokens, sprints, standups, statuses, users, versions, work-logs, workflows, workspaces) are now fully gated, mirroring `issues.controller.ts`; 6 controllers documented in-code as legitimately exempt (`auth`, `auth/oidc`, `health`, `public` — `@Public()`/no `request.user` for `ScopeGuard` to check — plus `me` and the non-`promoteCard` routes of `personal-boards` — purely-personal caller-identity data with no shared-resource blast radius). Three new additive scope pairs in `PAT_SCOPES` (`packages/shared/src/types.ts`): `workspaces:read`/`workspaces:write` (workspace CRUD/membership + the `users` co-member directory), `admin:read`/`admin:write` (instance SSO/OIDC settings), `tokens:read`/`tokens:write` (closes a real self-escalation hole: a scoped-down PAT could previously mint itself a brand-new *unrestricted* token via `POST /me/tokens`). `projects:read`/`projects:write` and `issues:read`/`issues:write` doc comments extended to name every newly-covered project-scoped/issue-scoped resource (boards, statuses, labels, sprints, custom fields, components, versions, workflows, dashboards, automations, poker, standups, saved filters, share tokens, roadmap, reports, analytics / checklist, work logs, attachments, notifications, search, issue-scoped label+version assignment). New `pat-scope-rollout.integration.spec.ts` (real HTTP app, real `ScopeGuard`, no mocks) data-drives all 143 newly-gated routes through a DENY (wrong-scope PAT → 403 + exact `ScopeGuard` message) and ALLOW (correctly-scoped PAT never blocked by the guard) pair — 286 assertions green; `tenant-isolation.integration.spec.ts` unaffected (108/108 still BLOCKED); full API unit suite 1808 green; shared vitest 146 green; MCP 110/104-tools green; `tsc --noEmit` clean api/web/mcp. `apps/web/src/components/settings/ApiTokensSection.tsx` scope-label map and the `@next-lane/mcp` README's new "PAT scopes" reference table both updated for the 6 new scopes, flagging that some previously-any-scope-works MCP tools (`list_workspaces`, `list_projects`, `list_users`, all report tools) now enforce their matching scope. **Acceptance met:** every controller mutation/read route carries an appropriate `@RequireScope` (or a documented exemption); the new integration suite proves a mismatched-scope token is rejected (403) on project/workspace/report/and-every-other-newly-gated-domain's routes, with a matching-scope ALLOW check per route as well. [code-reviewer, role-override review 2026-07-02; founder directive Hardening Night item 1]

- [x] (P2, S) **Workspace-switcher search/filter + recently-visited** ✅ shipped 2026-07-02 — `WorkspaceChip` dropdown (`apps/web/src/components/AppHeader.tsx`) gains a case-insensitive, autofocused, per-keystroke search box once the workspace count exceeds 8 (`data-testid="workspace-switcher-search"`), a scroll-capped list (`max-h-64 overflow-y-auto`) below it, and a "Recent" section (last 3 workspaces switched to via `setActiveWorkspaceId`, excluding the currently-active one, tracked as a small localStorage MRU list on `WorkspaceContext` — no duplicate state) shown above the full list while the query is empty; `#pulse-ws-select` left untouched (native `<select>` already scales). All existing `workspace-chip`/`workspace-switcher-item` test hooks and the footer links preserved; 2 new e2e in `apps/web/e2e/workspace-switcher.spec.ts` (per-keystroke `pressSequentially` narrowing + empty-state, and recent-list ordering/click-to-switch) — 22/22 green desktop+mobile; `tsc --noEmit` + build clean. — Original scope: the chip dropdown is a flat unstyled list; reproduced live at 50+ entries in the demo account with zero search, grouping, or archive affordance — any self-hoster running an agency/consultancy pattern hits this quickly. **Acceptance:** a text-filter input once workspace count exceeds ~8 (autofocus, case-insensitive), plus a "recently visited" section (last 3-5, localStorage-tracked) above the full list; e2e covers an >8-workspace fixture, filter-typing, and recent-list ordering, desktop + mobile. [product-auditor Pass 9/10/11, ranked #3 Pass 11; VISION.md § Better-than-Jira gaps #2]
- [x] (P2, S) **Tenant-isolation matrix: personal-cards, quick-links, workspace PATCH/logo, GitHub, dashboards** ✅ shipped 2026-07-02 — flagged unfixed for **three consecutive audit passes** (Pass 10 Risk #4, Pass 11 P2-4, Pass 12 P2-3 — "flag as blocking, not backlog, for the next build-loop batch"). `tenant-isolation.integration.spec.ts` fixture provisioning extended with `personalCardId`/`quickLinkId`/`dashboardId`/`gadgetId` (+ a GitHub integration configured per-tenant, keyed by the existing `projectId`); 20 new matrix rows added: `PATCH`/`DELETE /me/personal-cards/:id`, `PATCH`/`DELETE /me/quick-links/:id`, `PATCH /workspaces/:id`, `POST /workspaces/:id/logo`, `GET`/`PUT`/`DELETE /projects/:id/github`, `GET /issues/:id/github-links`, and the full dashboards surface (`GET`/`POST /projects/:id/dashboards`, `GET`/`PATCH`/`DELETE /dashboards/:id`, `GET /dashboards/:id/data`, `POST /dashboards/:id/gadgets`, `PATCH`/`DELETE /gadgets/:id`) — matrix grew from ~72 to 94 rows (sanity threshold raised to >90); all 93 non-sanity-check rows confirmed BLOCKED (403/404, zero leaked foreign ids) against a real cross-tenant attempt; full API unit suite (1521 tests) + integration suite both green. Ownership logic itself was already correct and unit-tested — this closed the regression-guard gap, not a live vulnerability. [engineering-auditor Pass-10 Risk #4 / Pass-11 P2-4 / Pass-12 P2-3]
- [x] (P2, S) **`resolveEnforcedWorkflowId` N+1 in bulk status updates** ✅ shipped 2026-07-02 — `bulkUpdate`'s existing perf preload (`bulkWorkflowEnforced`, one `isEnforcementEnabled` call per batch) only ever fed the *legacy* project-level fallback inside `enforceStatusChange`, not the WF-1 named-workflow resolution branch (`resolveEnforcedWorkflowId`) that branch actually hits when no explicit `boardId` is passed — exactly how `bulkUpdate` calls `update()`. For a named-workflow-enforced project, a 100-issue bulk status edit did up to ~200 extra sequential `board.findMany`/`sprint.findUnique` round trips the preload comment claimed were avoided. Fixed: new `buildBulkWorkflowResolution(projectId, issueRows)` loads the project's enforced-workflow boards ONCE and every DISTINCT sprint's ACTIVE state in ONE `sprint.findMany` (not once per issue), applies the same visibility/tie-break rule (factored into a new pure `pickEnforcedWorkflowId()` helper shared with the single-issue path) per issue in memory, and threads the result through a new `MutationOpts.resolvedWorkflowId` field (`undefined` = not pre-resolved, `null` = pre-resolved to "none applies", `string` = pre-resolved workflow id) so `enforceStatusChange`'s WF-1 branch becomes a plain map lookup instead of a fresh query per issue. 2 new unit tests assert `board.findMany` is called exactly once for a 5-issue batch (not 5) and `sprint.findMany` is called exactly once for a 4-issue, 2-distinct-sprint batch (not 4), plus that the resolution is actually load-bearing (every visible issue is enforced via `enforceTransitionForWorkflow`, not the legacy `enforceTransition` fallback); full API unit suite (1521 tests) green; `tsc --noEmit` clean. [engineering-auditor Pass-12 P2-1]
- [ ] (P2, M) **SSO/OIDC — Phase 2: SAML + multi-provider + JIT provisioning** — follow-up to the Ready-queue Phase 1 (generic OIDC) item and the Ready-queue admin-configuration-screen item: SAML 2.0 support, multiple simultaneously-configured providers, and just-in-time workspace/role provisioning on first SSO login. [product-auditor Pass 9/10/11/12 admin-controls carryover; VISION.md § Better-than-Jira gaps #2]
- [ ] (P2, S) **Sidebar auto-collapse (or a one-click nudge) at ≤1024px** — either default `collapsed: true` when `window.innerWidth` is below a small-laptop threshold on first load, or surface a lightweight "collapse for more room?" affordance when the board's own horizontal scroll container is triggered by sidebar width. Not broken, but a real, measurable reduction in usable board width on a common 13" laptop resolution introduced by the sidebar's own headline nav feature (`AppSidebar.tsx` — `collapsed` is purely user-toggled/persisted, no viewport-aware default). [product-auditor Pass-12 rank #6; AUDIT-PRODUCT.md Pass 12]
- [ ] (P2, S) **Product decision + implementation: distinct "switch workspace" vs. "view workspace page" affordance** — since the route-derived tenant-context fix shipped, simply viewing (or even just landing on, no save required) any workspace-scoped page now permanently persists that workspace as the user's default landing workspace on next app open (`useSyncActiveWorkspace` fires on mount regardless of any action taken). This may be intentional ("wherever you last navigated is where you land next") or may want a distinct "switch to this workspace" affordance separate from mere viewing — needs a founder/product call before building either direction. [product-auditor Pass-12 rank #8; AUDIT-PRODUCT.md Pass 12]
- [ ] (P2, S) **HTTP-level integration test for the GitHub webhook receiver** — every existing GitHub test calls `GithubService`/`GithubController` methods directly with a hand-constructed `Buffer` as `rawBody`; none boots the actual Nest app (`NestFactory.create` with `rawBody: true`) and POSTs a real HTTP request with a computed `X-Hub-Signature-256` header through Express's body-parser pipeline — the exact mechanism a future dependency bump could silently break with no test catching it. Add one integration test (style of `tenant-isolation.integration.spec.ts`, which already boots a real `INestApplication`): seed a `GithubIntegration` row, compute a real HMAC signature over a real JSON payload via `supertest`, assert 200 + the expected `IssueGithubLink` row, plus a negative tampered-signature case asserting 401. [engineering-auditor Pass-12 P2-5]
- [ ] (P2, M) **Realtime coverage: Board/Workflow/CustomFieldDefinition/Component/Version emit zero events on mutation** — filed by the `project.updated` realtime-coverage inventory (see Ready-queue item above, shipped 2026-07-02): renaming a board, editing a named workflow's transitions, adding/editing a custom field definition, component, or version does not notify a second open tab — the same "stale second viewer" bug class Project (and, until this pass's fix batch, Dashboards) had. Both Pass-11 and Pass-12 engineering audits proposed doing this as an actually-executed inventory rather than one-off fixes per entity (Pass-12's own Ideation #3: "the missing piece has been actually doing the sweep instead of fixing findings one at a time as they're independently discovered by two different audit passes"). Scope: add `SocketEvents.BoardUpdated`/`WorkflowUpdated`/`CustomFieldDefinitionUpdated`/`ComponentUpdated`/`VersionUpdated`/`GithubIntegrationUpdated` (`packages/shared`), wire `RealtimeService.emitToProject` into each service's create/update/delete paths following the exact `project.updated`/`dashboard.updated` pattern, extend `useBoardRealtime`'s generic handler, one unit test per service confirming the emit. [engineering-auditor Pass-11 P3-1 follow-up / Pass-12 Ideation #3, filed by project.updated ticket 2026-07-02]
- [ ] (P2, S) **Project creation: template + visual identity** — a project-type preset (Kanban starter / Scrum starter with seeded statuses) and a color/icon picker at creation time, shown on the project card. Every project card in a workspace looks identical today until a user memorizes the key. Cheap "feels considered" win aligned with the design-elevation directive. [product-auditor Pass 10 rank #7]
- [ ] (P2, M) **Roadmap: drag-to-reschedule epics + cross-project view** — the read-only Gantt bars exist and are well-built; make them draggable (reuse the fractional-rank/date-patch pattern used for sprint bars) and add a workspace-level "all projects" roadmap toggle. [product-auditor Pass 10 rank #8]
- [ ] (P2, M) **Scheduled/time-based automation trigger** (`TIME_ELAPSED_SINCE_TRANSITION`, `DUE_DATE_APPROACHING`) evaluated by a cron sweep — today's 4 automation triggers are all reactive to a user action; the most valuable real-team automations (SLA nudges, stale-issue escalation, due-date reminders) are time-based and structurally impossible right now. Carried forward unretested from Pass 9/10/11/12 (no code change since Pass 9). [product-auditor Pass 9/10/11/12 rank #6/#10]
- [ ] (P2, S) **Flaky "More" project-nav dropdown click in mobile-chrome e2e** — `automation.spec.ts` ("reaches the Automation tab from the project nav") and `roadmap.spec.ts` ("renders the sprint bar…") intermittently fail with "element was detached from the DOM, retrying" when clicking a `ProjectNav` "More" menu item, most consistently on the `mobile-chrome` Playwright project even for `describe` blocks that force a desktop viewport (device-emulation touch/UA differences, not viewport width). Reproduced on `main` (pre-existing, unrelated to the 2026-07-02 workspace-switcher/breadcrumb changes — confirmed via `git stash` bisection during that work): 3/3 failures against unmodified `ProjectNav`/`AppHeader`. Root cause not yet isolated (candidate: the "More" dropdown closing on the same tick a query refetch re-renders `ProjectNav`, racing the click). [discovered during workspace-switcher/breadcrumb build, 2026-07-02]
- [ ] (P2, S) **Smart-commit syntax** (`NL-123 #done`, `NL-123 #time 2h`) parsed from commit messages during the existing GitHub `push` webhook handler — the one remaining slice of the original Phase-9 GitHub follow-up vision not already promoted to Ready (auto-transition-on-merge + PR-badge, and GitLab/Gitea, are both now Ready items). [ROADMAP Phase 9; VISION.md § Better-than-Jira gaps #3]
- [x] (P2, M) **Kanban sections by field (Swimlanes v2)** ✅ shipped 2026-07-02 (founder-flagged — "swim lanes is pretty distinct, I want to be able to add sections within the kanban based on fields") — extends the shipped swimlane group-by (`BoardSwimlanesView.tsx` `computeLanes()`, previously Assignee/Priority/Issue type/Epic only) to **Component**, **Sprint**, **Labels**, and project-defined **custom SELECT fields**, plus a **per-board default grouping**. `computeLanes()` stays a pure function generalized over a `GroupByDimension` union (`assignee|priority|type|epic|component|sprint|label|cf:<fieldId>`, `apps/web/src/components/board/BoardSwimlanesView.tsx`); Component/Sprint get an "unset" lane (`No component`/`No sprint`); **decision: Labels duplicates an issue into every one of its label lanes** (an issue with N labels appears in N lanes) rather than a "first label + N hint" — matches the existing multi-label-filter mental model elsewhere in the app, and per-lane independent-`DndContext` isolation (cross-lane DnD stays structurally out of scope, unchanged from v1) makes duplicate lane membership safe; plus a `No labels` lane. Custom SELECT fields render one lane per configured option (field order) then a literal `None` lane; a value later removed from the field's option list still surfaces its own lane instead of silently dropping affected issues. Lane headers gained an optional color dot (`SwimLane.color`) for label lanes. **Schema (additive, as scoped):** `Board.defaultGroupBy String?` (migration `20260702020000_board_default_group_by`) — a core dimension key or `cf:<fieldId>`, server-validated (`BoardService#updateBoard`) against the project's actual SELECT fields (rejects unknown/foreign-project/non-SELECT); `Board`'s `issueInclude` in `board.service.ts` also gained `component` (missing before — the board view never carried component data, so cards couldn't show it either, a latent gap this surfaced). URL stays `?group=<dimension>` (`cf:<fieldId>`-safe); no `?group=` falls back to `defaultGroupBy`; explicit `?group=<dim>` always overrides it; a new `?group=none` sentinel lets a user explicitly flatten the board even when a default is configured (bare param removal would just re-apply the default). `GroupBySelector` and `BoardSettingsModal`'s new "Default grouping" `<select>` (`board-default-groupby`) share one source of truth (`apps/web/src/lib/groupByDimensions.ts`) so the toolbar and settings never drift. 15 new API unit tests (1519 API tests green); `update_board` MCP tool extended with `defaultGroupBy`. 3 new/extended `swimlanes.spec.ts` cases (component incl. "No component"; custom SELECT incl. "None" + reload persistence; per-board default applies on fresh load / URL override wins / reload persists / explicit None beats a configured default) plus full `board.spec`/`board-switcher.spec`/`nav-sidebar.spec`/`components.spec`/`custom-fields.spec`/`dashboards.spec` regression, all green desktop+mobile; `api`/`web`/`mcp` `tsc --noEmit` clean; light+dark screenshots verified. **Note:** interpretation flagged for founder confirmation (the founder said "if we need to talk about that item we can") but scoped to proceed rather than block, per direction. [founder directive 2026-07-02]

_Hardening Night close-out ingest (2026-07-06) — P2s:_

- [ ] (P2, S/M) **Dynamic PAT-scope route-coverage test (Nest `DiscoveryService` walk)** — code-reviewer-endorsed 2026-07-06: the new `pat-scope-rollout.integration.spec.ts` (143 routes) is a strong hand-maintained matrix but not a live introspection of the route table — it can't fail when route #201 ships with no `@RequireScope` and no matching matrix row added. Add a companion spec that enumerates every registered controller method at runtime via Nest's `DiscoveryService`/`Reflector` and asserts each carries `@RequireScope` metadata or is on a small, centrally-documented identity-exemption allowlist (`/me`, `/auth`, `/health`, `/public`) — turns "did we forget one" into structurally impossible to skip, so the PAT-scope matrix can't silently drift again. [code-reviewer 2026-07-06; engineering-auditor Pass 13 Ideation #1]
- [ ] (P2, S) **`bulk_update_issues`: `expectedProjectKey` guard + `{id,key}` success echo** — MCP-QA pass 2 finding 3: the highest-blast-radius write tool on the MCP surface (100 ids) has no misfire guard and a blind success envelope — ids from two different projects plus a project-agnostic field (priority/assignee/type) silently write cross-project with nothing in the response revealing it (cross-project statusId/sprintId/parentId/labels ARE caught per-item; priority/assignee/type are not). Add an optional `expectedProjectKey` (reject any id resolving outside that project, mirroring `create_issue`'s guard) and echo `updated: [{id,key}]` (plus `dryRun.wouldUpdate` keys, not bare ids) in the response. [MCP-QA pass 2, finding 3]
- [ ] (P2, S) **`list_project_activity`: resolve status/assignee ids to names in `summary`** — MCP-QA pass 2 finding 2: status/assignee change summaries are raw cuids (`"status: cmr4z8… → cmr4z8…"`) while `actor` and priority already render readably, defeating the tool description's own "cheap to skim without interpreting field/from/to yourself" promise. Resolve display names at feed-read time (both tables are already joined for `actor`); keep raw ids in `from`/`to`. [MCP-QA pass 2, finding 2]
- [ ] (P2, M) **Extend `ui/Badge` with a semantic `variant` prop; consolidate duplicated status-chip implementations** — `IssueCard`'s inline PR badge, `GithubLinksSection`/`GitlabLinksSection`'s `StateBadge`, and the blocked/overdue chips are 5 independently hand-rolled recipes of the same `bg-X-50 text-X-700 ring-X-200` Tailwind pattern; the 2026-07-06 dark-mode fix closed the "Merged" badge bug at the token layer, but the duplication itself remains — the next one-off status chip will drift the same way. [UI-REVIEW.md 2026-07-06, "Top 5" #3]
- [ ] (P2, S) **`Badge`'s custom-`color` path isn't dark-mode aware** — `hexWithAlpha`/`darken` are flat, mode-unaware transforms (a straight ×0.6 RGB multiply) applied to any user-picked label hex; reuse `applyBrandColor.ts`'s mix-toward-dark-canvas/paper technique (already built to solve exactly this problem for the workspace brand color) instead. [UI-REVIEW.md 2026-07-06, P2 #5]
- [ ] (P2, S) **Live-status-unavailable hint fails contrast in both themes** — the "⋯" hint in `GithubLinksSection`/`GitlabLinksSection` (shown when a live PR/CI or MR/pipeline check errors) uses `text-ink-300` (~1.6:1 in light mode, well under the 4.5:1 text/3:1 UI floor) for a real, actionable failure state; bump to `ink-400` or swap for a small icon at the `ChecksIndicator` dot's visual weight. [UI-REVIEW.md 2026-07-06, P2 #6]
- [ ] (P2, S) **Inconsistent Development-section naming when both GitHub and GitLab are configured** — `GithubLinksSection`'s heading reads "Development" (`aria-label="Development"`); `GitlabLinksSection`'s heading reads "GitLab" but its `aria-label` is "GitLab Development" — a project with both integrations configured shows two stacked headers that don't read as parallel provider sections. Pick one convention (both "Development", or both provider-named) and align the heading text and `aria-label` to match. [UI-REVIEW.md 2026-07-06, P2 #4]

---

## Next (P1 — high value, queue as Ready empties)

- [x] (P1, S–M) **Config-parity CI smoke test: `docker-compose.yml` vs `.env.example`** ✅ shipped 2026-07-06 — see "Already Done" below. [engineering-auditor Pass 13 Ideation #2]

- [x] (P1, M) Planning poker backend (2026-06-28) — `apps/api/src/poker/` module: `PokerService` + `PokerController` + `PokerModule` registered in `AppModule`; 9 REST endpoints (create/list/get/update session; add/remove item; cast vote; reveal item; commit estimate); vote masking (other users' values hidden pre-reveal; own vote always visible; all exposed post-reveal); state-transition validation (VOTING→REVEALED→CLOSED, no re-open from CLOSED); `activeItemId` validated to belong to session; commit writes `issue.storyPoints` atomically via `$transaction`; 6 Socket.io events to project room (`poker.vote.cast` — userId only, value suppressed; `poker.item.revealed`; `poker.session.updated`; `poker.item.added`; `poker.item.removed`; `poker.estimate.committed`); 32 new unit tests (473 total); `tsc --noEmit` clean.
- [x] (P1, L) Planning poker frontend (2026-06-28) — `apps/web/src/api/poker.ts` (10 hooks + `usePokerRealtime`); `PokerStartPage` at `/projects/:projectId/poker` (sessions list + create modal with sprint picker + issue multi-select); `PokerSessionPage` at `/projects/:projectId/poker/:sessionId` (active item card, POKER_DECK hand, participant strip, distribution + average on reveal, commit input + auto-advance, prev/next nav, facilitator controls, VIEWER read-only); "Poker" tab added to `ProjectNav`; "Estimate / Poker" link on BacklogPage; `usePokerRealtime` subscribes all `poker.*` events and invalidates session query; `apps/web/e2e/poker.spec.ts` (desktop + mobile); build green.
- [x] (P2, L) Custom fields backend (2026-06-28) — `apps/api/src/custom-fields/` module: `GET /projects/:projectId/custom-fields` (ordered by order asc, createdAt asc), `POST /projects/:projectId/custom-fields` (name slug → key, uniqueness suffix _2/_3…, options required for SELECT/MULTI_SELECT, order=max+1), `PATCH /custom-fields/:id` (name/options/appliesToTypes/required/order mutable; type+key immutable), `DELETE /custom-fields/:id` (orphaned Issue.customFields values silently ignored); `CustomFieldsService.validateAndNormalize` wired into issue create (store) and update (MERGE — only mentioned keys change, null clears, rest untouched); `customFields` JSONB returned in single-issue + create/update responses; 43 new unit tests (414 total); `tsc --noEmit` clean. Registered in `AppModule`; `IssuesModule` imports `CustomFieldsModule`.
- [x] (P2, L) Custom fields frontend slice (2026-06-28) — `apps/web/src/api/custom-fields.ts` with `useCustomFields`/`useCreateCustomField`/`useUpdateCustomField`/`useDeleteCustomField`/`useUpdateIssueCustomFields` hooks; `qk.customFields(projectId)` query key; `CustomFieldsSection` on SettingsPage (add/edit/delete with modal, options editor, appliesToTypes multi-select, required toggle, MEMBER+ gated, VIEWER read-only); `<CustomFieldInput>` for TEXT/URL/NUMBER/DATE/CHECKBOX/SELECT/MULTI_SELECT; `CustomFieldsDrawerSection` in IssueDetailDrawer sidebar (filtered by appliesToTypes); custom fields in CreateIssueModal; 10+ `data-testid` hooks; `apps/web/e2e/custom-fields.spec.ts` (desktop + mobile); build + typecheck green.
- [x] (P2, L) Multiple boards per project — frontend slice 1 (board management + switching): `useBoards`/`useBoardView`/`useCreateBoard`/`useUpdateBoard`/`useDeleteBoard` hooks in `apps/web/src/api/boards.ts`; `BoardSwitcher` component with dropdown, type badges (Kanban/Scrum), create-board modal, per-board settings (rename/type/delete, default-board guard); `BoardPage` refactored to be boardId-driven with `localStorage` persistence of selected board per project; `useMoveIssue`/`useCreateIssue`/`useDeleteIssue` extended with `boardId` param to keep drag-and-drop optimistic updates keyed to `qk.boardView(boardId)`; `useBoardRealtime` extended with `boardId` to invalidate the right cache on realtime events; `useCreateStatus`/`useUpdateStatus`/`useDeleteStatus`/`useUpdateLabel`/`useDeleteLabel`/`useToggleIssueLabel` all extended with `boardId` for consistent cache invalidation; `CreateIssueModal` extended with `boardId` prop; new `qk.boardView(boardId)` and `qk.boards(projectId)` query keys; 8 Playwright e2e tests (desktop + mobile): default board visible, create Scrum board, switch boards, rename board, delete non-default board, default-board deletion blocked. Build green, typecheck clean. ✅ shipped 2026-06-27

- [x] (P1, L) Tenant-isolation test harness (PART 1 complete) — `apps/api/src/tenant-isolation.integration.spec.ts`: two independent workspaces (A/B) + full fixture setup (issue, comment, label, sprint, status, webhook, API token); 42-endpoint data-driven matrix (HTTP isolation) + WebSocket gateway rejection check; all 42 cross-tenant attempts BLOCKED (403/404), zero data leaks; 7 integration tests pass; run via `pnpm test:isolation` or `npx jest --config jest.integration.config.js`; unit suite unchanged at 349. PART 2 (declarative `@RequireRole`/`@ResourceScope` decorators) deferred — role context is workspace-scoped and derived from the resource in the service layer; adding a guard at the controller level without removing service checks adds confusion without safety, while removing service checks is risky without comprehensive coverage; existing `assertProjectRole`/`assertWorkspaceRole` service-level assertions remain as the authoritative enforcement layer (defense in depth). ✅ shipped 2026-06-27 [engineering-auditor]
- [x] (P1, M) Full-text search — Postgres `tsvector` generated column (title+description) + GIN index (migration `20260627230000_issue_full_text_search`); `websearch_to_tsquery` + `ts_rank` in search.service + `findAll` (q≥2; ILIKE fallback for short/key queries); parameterized; 15 unit + 10 e2e — ✅ shipped 2026-06-27. (Structured filter-grammar + persisted SavedView remain tracked at P3 "Saved/shareable views + query DSL".) [engineering-auditor, product-auditor]

- [x] (P1, M) Issue links / dependencies backend (2026-06-28) — `apps/api/src/issue-links/` module: `POST /issues/:id/links` (MEMBER+; canonical-type normalization — BLOCKED_BY→BLOCKS source/target swapped, DUPLICATED_BY→DUPLICATES swapped, CLONES/BLOCKS/DUPLICATES/RELATES_TO stored as-is with source=:id; self-link 400; duplicate + inverse-duplicate 409; cross-project 404; target resolved by issue key e.g. "NL-5" or bare CUID); `GET /issues/:id/links` (VIEWER+; returns IssueLinkDto[] ordered by type then createdAt, each perspective-resolved: source links use stored type, target links use ISSUE_LINK_INVERSE[storedType], label from ISSUE_LINK_TYPE_LABELS); `DELETE /issue-links/:linkId` (MEMBER+; membership-checked via link's source project); `toIssueLinkDto(link, viewerIssueId)` exported mapper; links served separately via `GET /issues/:id/links` (IssueDto not modified — avoids touching shared package); 32 new unit tests (505 total); `tsc --noEmit` clean.

- [x] (P2, S) Quick-filter preset chips on the board toolbar (2026-06-28) — "My issues" / "High priority" / "Unresolved" / "Recently updated" one-click chips added to `BoardPage.tsx` toolbar; each chip toggles on/off (`aria-pressed`), active = cobalt filled; implemented as a client-side post-filter pass over the already pill+NLQL filtered issue set; mutually composable with manual pill filters and NLQL bar; `StatusCategory` import added; `QuickFilterBar` + `QuickFilterKey` type added at module scope; `data-testids`: `quick-filter-my-issues`, `quick-filter-high-priority`, `quick-filter-unresolved`, `quick-filter-recent`; `apps/web/e2e/quick-filters.spec.ts` (desktop + mobile; isolated project per test); build green.
- [x] (P2, S) Watch toggle in the issue drawer (2026-06-28) — `useToggleWatch(issueId)` mutation hook in `apps/web/src/api/issues.ts`: `POST /issues/:id/watch` (watch) / `DELETE /issues/:id/watch` (unwatch); optimistic toggle of `isWatching`/`watcherCount` on the cached `qk.issue(issueId)` entry with rollback on error; `IssueWithWatch` local type extension (`isWatching?: boolean; watcherCount?: number`) read defensively from the issue response; Watch/Watching button (eye icon + watcher count badge) added to `IssueDetailDrawer` header — any role can watch; `data-testid="issue-watch-toggle"`; `apps/web/e2e/watch.spec.ts` (desktop + mobile); build green.
- [x] (P2, M) Async standups backend (2026-06-28) — `StandupsModule` (`apps/api/src/standups/`): `GET /projects/:projectId/standups?date=YYYY-MM-DD` (VIEWER+, team digest ordered by user name + blockerLinks with resolved IssueRefDto), `GET /projects/:projectId/standups/me?date=YYYY-MM-DD` (VIEWER+, caller's entry or null), `POST /projects/:projectId/standups` (MEMBER+, upsert for userId+projectId+date via Prisma upsert; atomic blocker-link replacement in `$transaction`: deleteMany+createMany; validates blockerIssueIds belong to project), `GET /projects/:projectId/standups/prefill` (VIEWER+, yesterday from 24-hour ActivityLog grouped by issue, today from assigned IN_PROGRESS issues, no persistence); `toStandupEntryDto` exported mapper (UTC midnight→YYYY-MM-DD local, blockerIssueIds projection, nested user + IssueRefDto with issue key = project.key+number); date stored as midnight UTC (`T00:00:00.000Z`), `findFirst` used for nullable-teamId queries (Prisma compound-unique limitation with NULLS NOT DISTINCT); 25 unit tests (548 total); `tsc --noEmit` clean; `StandupsModule` registered in `AppModule`. [roadmap Phase 10]
- [x] (P2, L) Async standups frontend (2026-06-28) — `apps/web/src/api/standups.ts`: `useStandups(projectId, date)` / `useMyStandup(projectId, date)` / `useSubmitStandup(projectId)` / `useStandupPrefill(projectId)` hooks with `standupKeys` factory; `StandupsPage` at `/projects/:projectId/standups` with `DateSelector` (YYYY-MM-DD, today default, prev/next/today buttons), `MyStandupCard` editor (Yesterday/Today/Blockers textareas, Prefill button, `BlockerIssuePicker` combobox, Save/Update button — VIEWER-gated), `TeamDigest` showing all entries with `StandupEntryCard` (amber blocked badge, amber blocker text + issue-key chip links to board with issue drawer open); optimistic upsert on save (userId merge in digest list); "Standup" tab added to `ProjectNav`; route added to `App.tsx`; `data-testids`: `standup-yesterday`, `standup-today`, `standup-blockers`, `standup-prefill`, `standup-save`, `standup-entry`, `standup-date`; `apps/web/e2e/standups.spec.ts` (desktop + mobile); build green.
- [x] (P2, M) Personal boards backend (2026-06-28) — `PersonalBoardsModule` (`apps/api/src/personal-boards/`): all endpoints scoped to `userId` from JWT (private, no workspace membership check); `GET /me/personal-board` lazily creates three default columns ("To Do"/"Doing"/"Done", orders 0/1/2) in a `$transaction` on first access then returns `PersonalColumnDto[]` with `cards[]` ordered by rank; `POST /me/personal-columns` (order=max+1), `PATCH /me/personal-columns/:id` (name/order), `DELETE /me/personal-columns/:id` (cards cascade-deleted by FK); `POST /me/personal-cards` (fractional rank via `rankAfter` after last card in column), `PATCH /me/personal-cards/:id` (edit title/notes; move with `columnId`+`beforeId`/`afterId` → `rankBetween`; column-only move places card at end via `rankAfter`), `DELETE /me/personal-cards/:id`; `POST /me/personal-cards/:id/promote` (`assertProjectMember` check then `IssuesService.create` with type TASK + card title/notes as description, sets `promotedIssueId` on card, returns `{ card: PersonalCardDto, issue: IssueDto }`); `toPersonalColumnDto` / `toPersonalCardDto` mappers; 5 DTOs with class-validator; 21 unit tests (569 total); `tsc --noEmit` clean; `PersonalBoardsModule` registered in `AppModule`; `IssuesModule` imported for `IssuesService.create`. [roadmap Phase 10]
- [x] (P2, L) Personal boards frontend (2026-06-28) — `/my-board` route registered in `App.tsx` + "My Board" `NavLink` added to `AppHeader` alongside "My Work"; `apps/web/src/api/personal-board.ts` (8 hooks: `usePersonalBoard`, `useCreatePersonalColumn`, `useUpdatePersonalColumn`, `useDeletePersonalColumn`, `useCreatePersonalCard`, `useUpdatePersonalCard` with optimistic move + rollback, `useDeletePersonalCard` with optimistic remove + rollback, `usePromotePersonalCard`; `personalBoardQk` key); `PersonalBoardPage` at `apps/web/src/pages/PersonalBoardPage.tsx` — dnd-kit kanban (reuses project-board `DndContext`/`SortableContext`/`useDroppable`/`useSortable` patterns), horizontally-scrollable column layout, inline `AddCardComposer`, `EditCardModal` (title + notes), `PersonalColumn` with click-to-rename + ConfirmDialog-delete, keyboard-friendly `MoveCardMenu` affordance (move button on card → column picker menu; reliable e2e alternative to raw mouse drag), `PromoteCardModal` (workspace + project selects → promote mutation → success toast with issue key), promoted badge on cards, `DragOverlay` ghost, empty-state prompts; all required `data-testid` hooks: `personal-board`, `personal-column`, `personal-card`, `personal-add-card`, `personal-card-title`, `personal-card-save`, `personal-add-column`, `personal-promote`; `apps/web/e2e/personal-board.spec.ts` (desktop: defaults, nav link, add card, edit title, move via menu + reload assert, delete card, add/delete column; mobile: horizontal scroll layout + add card); build green.

- [x] (P1, L) Automation engine frontend (2026-06-28) — `apps/web/src/api/automations.ts` (7 hooks: `useAutomations`, `useAutomation`, `useCreateAutomation`, `useUpdateAutomation`, `useDeleteAutomation`, `useToggleAutomation` (optimistic with rollback), `useAutomationRuns`; `automationKeys` factory); `AutomationsPage` at `/projects/:projectId/automations` (rule list with enable/disable toggle, trigger label, condition summary, actions summary, edit + delete-with-ConfirmDialog; tabs: Rules / Run log); `AutomationRuleEditor` modal (name, description, enabled toggle, trigger select, NLQL condition via `NlqlConditionInput` with live validation, ordered actions builder with add/reorder/remove, type-switched param controls: ASSIGN→member picker, SET_PRIORITY→priority select, TRANSITION→status select, ADD_LABEL→label picker, ADD_COMMENT→textarea, SET_CUSTOM_FIELD→field+value); `AutomationRunsPanel` Glass Box audit trail (rule name, issue key link, trigger, matched, status badge color-coded SUCCESS/SKIPPED/FAILED, actionsApplied list, error, timestamp); `NlqlConditionInput` reusable NLQL condition field with `validateQuery` live feedback; `ActionParamsEditor` typed dispatch for all 6 action param shapes; "Automation" tab in `ProjectNav` (`data-testid="nav-automation"`); route `/projects/:projectId/automations` added to `App.tsx` behind `RequireAuth`; data-testids: `automations-page`, `automation-new`, `automation-row`, `automation-name-input`, `automation-trigger-select`, `automation-condition-input`, `automation-action-add`, `automation-action-row`, `automation-action-type`, `automation-save`, `automation-toggle`, `automation-delete`, `automation-runs`, `automation-run-row`; build green, tsc clean (pre-existing PersonalBoardPage unused-import not introduced by this change).

- [x] (P2, L) Personal & team analytics frontend (2026-06-28) — `apps/web/src/api/analytics.ts` (`usePersonalAnalytics(days)`, `useProjectAnalytics(projectId, days)`, `analyticsKeys` factory); shared analytics components: `WindowSelector` (14/30/90-day segmented control, `aria-pressed`), `StatCard`/`StatCardSkeleton` (headline metric tiles), `ThroughputChart` (hand-rolled SVG area+line, personal page), `FlowChart` (hand-rolled SVG dual-line created vs completed, project page), `CycleTimeChart` (horizontal bar distribution by bucket), `CategoryBars` (type/priority breakdowns), `WorkloadBars` (open issues per assignee); `PersonalAnalyticsPage` at `/me/analytics` (stat cards: open/completed/overdue/avg cycle time, throughput chart, by-type + by-priority horizontal bars, personal board mini-stats); `ProjectAnalyticsPage` at `/projects/:projectId/analytics` (stat cards: created/completed/avg cycle time, flow chart, cycle-time distribution, workload by assignee, window selector); "Analytics" tab added to `ProjectNav`; "Insights" link added to `AppHeader` (`data-testid="nav-my-analytics"`); routes added to `App.tsx`; all charts have `role="img"`, `aria-label`, and a visually-hidden `sr-only` data summary; `data-testid` hooks: `personal-analytics`, `project-analytics`, `personal-analytics-window`, `project-analytics-window`, `personal-stat-cards`, `project-stat-cards`, `stat-open`, `stat-completed`, `stat-overdue`, `stat-cycle-time`, `project-stat-created`, `project-stat-completed`, `project-stat-cycle-time`, `throughput-chart`, `flow-chart`, `cycle-time-chart`, `workload-bars`, `by-type-bars`, `by-priority-bars`, `personal-board-stats`; build green.

- [x] (P1, M) **Workspace discoverability + settings batch** (2026-06-30, frontend-only) — Closed 4 founder-session + discoverability audit gaps: (1) **Quick Links** — `apps/web/src/api/quick-links.ts` (`useQuickLinks`/`useCreateQuickLink`/`useUpdateQuickLink`/`useDeleteQuickLink`; `qk.quickLinks` key); `QuickLinksMenu` in `AppHeader` (chain-link icon `data-testid="quick-links-button"`, dropdown with link list as new-tab anchors, add-form with `data-testid` hooks: `add-quick-link-label`, `add-quick-link-url`, `add-quick-link-submit`, `add-quick-link-error`; per-row `quick-link-edit`/`quick-link-delete`; empty state `quick-links-empty`). (2) **Workspace chip + switcher** — `WorkspaceChip` in `AppHeader` (`data-testid="workspace-chip"`; if >1 workspace opens switcher dropdown `data-testid="workspace-switcher-item"` with `setActiveWorkspaceId` + footer links to General settings + Members; single-workspace links to `/workspaces/:id/settings`). (3) **Workspace General settings page** — `apps/web/src/pages/WorkspaceSettingsPage.tsx` at `/workspaces/:id/settings` (rename form: `data-testid="workspace-name-input"/"workspace-name-save"`, `useUpdateWorkspaceBranding`; danger zone: `data-testid="delete-workspace-button"/"delete-workspace-dialog"/"delete-workspace-confirm-input"/"delete-workspace-confirm-button"`, `useDeleteWorkspace` hook DELETE `/workspaces/:id`; on success navigates home + switches active workspace; admin-gated); route added to `App.tsx`. (4) **Shared `WorkspaceSettingsNav`** — `apps/web/src/components/WorkspaceSettingsNav.tsx` (`data-testid="workspace-settings-nav"`; tabs: General/Members/Audit log/Branding; replaces duplicated nav in all three existing workspace pages). (5) **Invite member + role change** — `useAddMember(workspaceId)` hook in `workspaces.ts` (POST `/workspaces/:id/members`, upserts); `InviteForm` on Members page (`data-testid="invite-member-form"/"invite-email-input"/"invite-role-select"/"invite-member-submit"`); per-row role select for non-self members (`data-testid="member-role-select"`); all existing `member-row`/`member-role-badge`/`remove-member-button`/`workspace-members-page` testids preserved. tsc `--ignoreDeprecations 6.0 --noEmit` zero errors; `pnpm --filter @next-lane/web build` clean. [founder-session / discoverability-audit]

- [x] (P1, M) **Workspace selector correctness fix** (2026-07-01, frontend-only) — QA of the workspace switcher (founder report "the workspaces selector seems buggy") reproduced four real bugs with Playwright and fixed them: (1) the header `WorkspaceChip` and the Pulse dashboard's own `<select>` were **two independent, unsynced states** — changing one never updated the other; (2) switching the chip **only re-themed the header** and left you on the previous workspace's content; (3) the active workspace **did not persist** across reload (always reset to the first workspace); (4) the chip **misreported** the current workspace when viewing a project board. Fixes: `WorkspaceContext` is now the single source of truth, persisted to `localStorage` (`nl.activeWorkspaceId`) and restored synchronously on first render, healing to the first workspace only when the stored id is gone (covers deleting the active workspace); `PulseDashboardPage` reads/writes the context instead of a local `selectedWs` (removed the local state + sync effect); the chip switcher now `navigate('/')`s so content re-scopes to the newly-active workspace; new reusable `useSyncActiveWorkspace(workspaceId)` hook exported from `WorkspaceContext`, wired into `BoardPage`, `BacklogPage`, `TriagePage`, `SettingsPage`, `StandupsPage`, `PokerStartPage`, `PokerSessionPage`, `AutomationsPage` so the chip tracks the workspace of whatever you're viewing. All `data-testid`s preserved. Verified: 7/7 Playwright QA scenarios green (incl. single-workspace user renders chip as a link, and deleting the active workspace heals to a remaining one); web `tsc --noEmit` + `pnpm build` clean; 40 desktop + 17 mobile e2e (pulse-dashboard, board, board-switcher, auth, my-work, personal-board, realtime-auth) green — no regressions (the transient failures during the run were the API rate limiter, resolved by running with `RATE_LIMIT_DISABLED=true` per the e2e preconditions). [founder QA report]

- [x] (P2, M) Bulk edit frontend (2026-06-28) — `apps/web/src/components/issue/BulkActionBar.tsx`: `BulkActionBar` (sticky portal, `role="region"` + `aria-label="Bulk actions"`, `data-testid="bulk-action-bar"`; status/assignee/priority/sprint/label controls; Apply `data-testid="bulk-apply"` disabled until a field is set; Clear `data-testid="bulk-clear"`; ink-900 Dispatch bar surface + signal-600 accent; mobile-wrapping at 390 px; `prefers-reduced-motion` via Tailwind animate class); `BulkSelectCheckbox` (`data-testid="bulk-select-row"`, `data-issue-id`, stops propagation to prevent row-open); `BulkSelectAll` (`data-testid="bulk-select-all"`, indeterminate state); `useBulkUpdateIssues()` mutation in `apps/web/src/api/issues.ts` (`POST /issues/bulk`, invalidates `projectIssues`, `board`, individual `issue` cache entries on success); BacklogPage: per-row checkbox + per-section `BulkSelectAll` in section header (sprint sections + backlog section); `BulkIssueChangesDto` import from `@next-lane/shared`; handleBulkApply sends only touched fields; success toast `"Updated N issues."`; warning toast for `failed.length > 0`; TriagePage: same checkbox/selectAll pattern; sprint control hidden (`showSprint={false}`); `useSprints` + `SprintState` added for TriagePage; all existing BacklogPage/TriagePage `data-testid` attrs, ARIA roles, keyboard handlers (j/k/s/p/a/l/f/?/Esc) fully preserved; tsc + build clean. [product-auditor parity gap]

---

- [x] (P2, S) Docs site Overcastly v2 re-theme (2026-06-28) — `docs-site/.vitepress/theme/custom.css` rewritten: Overcastly v2 token system (canvas `#15161a`/`#1c1d22`/`#25262c`, ink `#f4f4f1`/`#b8b9b6`/`#6f7075`, accent `#4F8BFF`/`#7AA8FF`, success `#7BD389`, hairlines `rgba(255,255,255,0.08/0.16)`); dotted-grid body background (signature element, radial-gradient dots at 32px grid); pill buttons (`999px`); mono-uppercase eyebrows on sidebar group titles, table `<th>`, code-block lang labels, custom-block titles; `h2` accent bar; `appearance:'dark'` in `config.ts`; SVG logos + favicon to `#4F8BFF`; theme-color meta to `#4F8BFF`; WCAG-AA verified; build clean 4.4s. [oss-curator / frontend-design]

## Already Done (recent shipments — ticked for reference)

- [x] (P1, S–M) **Config-parity CI smoke test: `docker-compose.yml` vs `.env.example`** ✅ shipped 2026-07-06 — closes the *class* behind the same-day compose env-passthrough fix (`3c22f21`, audit pass 13 Risk 1: SMTP/CORS/etc. never reached the container): a future documented env var that isn't forwarded (or a forwarded var that isn't documented) now fails CI instead of shipping silently broken. New `scripts/smoke-config-parity.sh` (mirrors the `scripts/smoke-web-csp.sh` precedent; bash + docker + jq only, no build/containers — `docker compose config` just parses + interpolates): parses `.env.example` for both active (`VAR=value`) and commented-optional (`# VAR=value`) declarations, anchored at line-start so prose mentions of a var name (`OIDC_ISSUER_URL — the provider's...`) are never mistaken for a declaration; renders `docker compose config --format json` with a **distinct sentinel value per documented var** (not just "is the key present" — proves real `${VAR}` passthrough, not a hardcoded/default value coincidentally matching); asserts every documented var (minus a reviewed, one-reason-each `IGNORE_NOT_FORWARDED` list — `DATABASE_URL`/`REDIS_URL`/`API_PORT` composed or hardcoded inline; `POSTGRES_*`/`REDIS_PORT`/`WEB_PORT` belong to other services' port mappings; `VITE_API_URL` is a web build ARG; `UPLOADS_DIR` intentionally container-internal; `RESET_BASE_URL` a dead fallback superseded by `WEB_BASE_URL`; `OTEL_*` a documented future/stub with no reading code yet) round-trips its sentinel into the rendered `api` service environment. **Reverse direction too:** every `${VAR}` the api service's own compose stanza reads must be documented in `.env.example` (empty `IGNORE_UNDOCUMENTED` list today) — this direction caught FIVE real, live documentation gaps at HEAD (`AUTO_SEED`, `WEB_BASE_URL`, `GITLAB_TOKEN_ENCRYPTION_KEY`, `GITLAB_WEBHOOK_BASE_URL`, `OIDC_CONFIG_ENCRYPTION_KEY` — all genuinely forwarded/consumed by code but absent from `.env.example`), fixed in `.env.example` in the same commit (closing the standalone `GITLAB_TOKEN_ENCRYPTION_KEY` P3 backlog item — Pass 13 Risk 5 — as a side effect) so the check is green, not silenced. Host-port vars (`POSTGRES_PORT`/`REDIS_PORT`/`API_PORT`/`WEB_PORT`) get a valid numeric sentinel instead of an alnum string (compose validates `ports:` values; they're ignore-listed for the forward check anyway). Wired into CI as a new early step in `.github/workflows/ci.yml`'s existing `docker-build` job ("Production image build (compose)" — the docker-compose build guard), before the slower image build so a config-parity break fails fast. Both-ways-proven: green at HEAD (29 non-ignored vars round-tripped, 33 forwarded vars all documented); locally reverting `SMTP_HOST`'s passthrough line in `docker-compose.yml` reproduced the exact original bug class and the script failed with `MISSING: .env.example documents "SMTP_HOST" but it does not appear at all in the rendered api service environment` — reverted after confirming. `shellcheck`-clean. [engineering-auditor Pass 13 Ideation #2]

- [x] (P1, S) **Hardening Night frontend batch (2026-07-06)** — closed the
  four `docs/UI-REVIEW.md` 2026-07-06-dated findings: **(1) mobile toast/modal
  overlap** — `ui/Toast.tsx`'s viewport now bottom-anchors at every
  breakpoint (was `top-0` below `sm:`, landing squarely on every `Modal`'s
  `items-start`/`mt-8` header+close button — 17+ modal-hosting components
  affected on any mutation error) plus `pb-[calc(1rem+env(safe-area-inset-
  bottom))]` for notched-phone safe area; paired with capping `ui/Modal.tsx`'s
  panel at `max-h-[calc(100dvh-4rem)]` with a sticky header/footer + scrollable
  body so a tall form's footer action row can't newly collide with the
  bottom-pinned toast either (the review's flagged residual risk). **(2)
  "Merged" PR/MR badge dark-mode break** — `purple` added to
  `tailwind.config.js`'s CSS-var-backed `varScale()` pipeline (mirroring
  emerald/red/amber/etc.) with `:root`/`.dark` values in `index.css` derived
  via the same `applyBrandColor.ts` mix-toward-canvas/mix-toward-paper method
  as every other scale — the existing `bg-purple-50 text-purple-700
  ring-purple-200` classes in `IssueCard.tsx`, `GithubLinksSection.tsx`,
  `GitlabLinksSection.tsx`, `WorkspaceAuditLogPage.tsx`, and
  `WorkspaceMembersPage.tsx` all inherit dark-mode-legible values with ZERO
  per-component changes (fix-once-at-the-token-layer, per the review's
  prescription); contrast verified ≥4.5:1 in both modes (light 6.5:1/5.9:1,
  dark 5.1:1/4.7:1 for the 50/700 and 100/700 pairs respectively).
  **(3) Dashboard selection was shadow state** — `DashboardsPage.tsx`'s
  `selectedId` moved from local `useState` to the `?dashboard=<id>` URL
  search param, mirroring `BoardPage`'s existing URL-as-source-of-truth
  filter pattern; reload/deep-link/share now land on the same dashboard,
  with the 6447e76 synchronous-append create fix (new dashboard stays
  selected) preserved. **(4) P2 quick wins** — `GadgetCard`'s drag handle
  grew a `min-h-10 min-w-10` (≈40px) touch target on mobile (icon stays
  visually small, resets at `sm:`+ for pointer precision); `GithubLinksSection`
  /`GitlabLinksSection`'s live PR/CI and MR/pipeline status polls now render a
  small `Spinner` while `liveQuery.isLoading` instead of nothing, matching the
  drawer's established loading vocabulary (`ui/States`' `Spinner` extended to
  forward arbitrary HTML attributes for the new `data-testid` hooks). Gates:
  `tsc --noEmit` + web build clean; 142 targeted e2e green desktop+mobile
  (`dashboards.spec.ts`, `dashboards-phase2.spec.ts`, `pr-auto-transition.spec.ts`,
  `board.spec.ts`, `toast.spec.ts`, `personal-board.spec.ts`,
  `pulse-dashboard.spec.ts`, `webhooks-ui.spec.ts`, `components.spec.ts`,
  `csv-import.spec.ts`, `issue-templates.spec.ts`, `github-integration.spec.ts`,
  `gitlab-integration.spec.ts`, `issue-detail.spec.ts`, `issue-links.spec.ts`);
  all `data-testid`/`role`/`aria-label` hooks preserved (no e2e selector
  changes needed). [frontend-qa 2026-07-06 UI review, Hardening Night task #93]

- [x] (P2, M) **DNS-rebinding TOCTOU closed in the shared SSRF guard
  (2026-07-06, Hardening Night, audit pass 13 Risk 3)** — the old
  `resolveAndCheckBlocked` + raw `fetch()` pattern resolved DNS once to vet a
  target then let `fetch()` re-resolve DNS itself; a short-TTL attacker
  nameserver could answer the vetting query with a public IP and the real
  connection query with `169.254.169.254`/`127.0.0.1`/an internal host.
  New shared `apps/api/src/common/ssrf-safe-fetch.ts`: `ssrfSafeFetch()`
  resolves DNS exactly once and PINS the actual TCP/TLS connection to that
  one vetted address via a custom undici `Agent` `connect.lookup` (added
  `undici` as a direct `apps/api` dependency) that ignores whatever hostname
  it's asked to resolve — there is no second real DNS query left for a
  rebinding nameserver to answer differently; Host/SNI still use the real
  hostname so TLS cert validation is unaffected. All three outbound-call
  families now share this one path: webhook delivery (`webhooks.service.ts`,
  re-vetted fresh per retry attempt), `GithubClient`, `GitlabClient`
  (self-hosted GitLab's admin-supplied `baseUrl` was the primary risk).
  `redirect: 'manual'` semantics preserved; `WEBHOOK_ALLOW_PRIVATE=true`
  still bypasses the guard entirely for self-hosters. 15 new unit tests
  including a real-socket rebind simulation (mocked `dns.promises.lookup`
  answers public-then-private; the pinned request still hits the
  first-vetted address and `dns.promises.lookup` is proven called exactly
  once) plus direct `buildPinnedLookup` unit tests. All 71 pre-existing
  webhook/GitHub/GitLab SSRF tests pass unmodified. API unit 1823/1823,
  integration 293/293, `tsc --noEmit` clean. [orchestrator, audit pass 13
  Risk 3]

- [x] (P2, S) **`multer` DoS CVEs closed via root `pnpm.overrides`
  (2026-07-06, Hardening Night, audit pass 13 Risk 4)** — `apps/api`'s own
  direct `multer: "^2.2.0"` dependency was already patched, but
  `@nestjs/platform-express`'s `FileInterceptor` (used by every real upload
  endpoint — workspace logo, CSV import, issue attachments) bundled its own
  transitive `multer@2.0.2`, carrying 3 high + 1 moderate DoS advisories.
  Root `package.json` `pnpm.overrides` now forces `multer: "^2.2.0"` (and,
  since it was cheap and still major-version-4, `lodash: "^4.18.0"` for the
  one other HIGH — code injection via `_.template` — transitively pulled in
  by `@nestjs/swagger`). `pnpm audit --prod`: 5 high → **0 high** (10 → 7
  moderate; the remaining moderates — `js-yaml`, `qs`, `@nestjs/core`,
  `file-type` — are documented as deferred, not silently dropped: `qs`/
  `js-yaml` are minor-version-safe candidates for a follow-up pass,
  `@nestjs/core`'s fix needs a Nest v10→v11 major bump, and `file-type`'s
  fix needs v20+ (ESM-only) which the audit already flagged as its own
  ticket — none blindly bumped). Verified `pnpm why multer`/`pnpm why
  lodash` resolve every path to the patched version; live-verified with a
  REAL multipart `curl` round-trip against a running API instance (isolated
  port, so as not to disturb the concurrent QA agent's server on :4000) —
  workspace logo upload/download and issue attachment upload/download both
  byte-for-byte matched the uploaded file through the patched
  `FileInterceptor`/multer/`file-type`-sniff path. Attachment/workspace
  upload unit suites (241 tests) unaffected. [orchestrator, audit pass 13
  Risk 4]

- [x] (P3, S) **Issue-create `$transaction` timeout hardened against pool
  contention (2026-07-06, Hardening Night, qa-tester 2026-07-06 P3)** —
  `POST /issues` 500'd twice under 2-worker e2e parallelism in a
  resource-constrained sandbox ("Transaction already closed... timeout 5000
  ms" — Prisma's defaults). `IssuesService.createInner`'s default-status
  resolution (`status.findFirst` ×≤2, only when the caller omits `statusId`)
  moved OUT of the transaction onto the plain `prisma` client — a pure read
  with no dependency on the atomic write, trimming round-trips off the
  transaction's held-connection time — and the transaction now passes
  explicit `{ timeout: 12_000, maxWait: 12_000 }` (vs. Prisma's 5000ms/2000ms
  defaults), justified in a code comment tied to the QA finding. No
  correctness change: issueSeq increment, rank lookup, the insert, and its
  activity-log row still commit atomically together. 351/351 issue unit
  tests pass (2 test-double fixtures updated to mock `prisma.status.findFirst`
  at the top level, matching the moved call site). [orchestrator, qa-tester
  P3 2026-07-06]

- [x] (P1, S) **docker-compose env passthrough fix (2026-07-06, Hardening
  Night)** — audit pass 13 finding 1: compose forwarded only 7 of ~25
  documented operator vars; SMTP_* never reached the container so
  password-reset email was dead in every stock self-hosted deploy;
  CORS_ORIGINS/encryption keys/OIDC_*/THROTTLE_*/WEBHOOK_ALLOW_PRIVATE/
  MAX_FILE_BYTES/LOG_LEVEL equally inert; AUTO_SEED hardcoded. All forwarded
  now with code-matching defaults for `??`-consumed vars (empty string would
  override `??`; truthiness/`Number()||` vars pass through empty safely —
  each consumption pattern verified in code before choosing the form).
  Proven via `docker compose config` resolution. [orchestrator, audit pass 13
  finding 1]

- [x] (P1, S) **Activity logging for sprint/parent/component/label mutations
  (2026-07-03)** — MCP-QA pass 2's top finding fixed same-day: these
  mutations produced ZERO ActivityLog rows, so `list_project_activity` and
  agent-context `staleness` were blind to them (a bulk sprint re-scope of 6 +
  atomic re-parent of 14 left the feed empty and staleness unchanged while a
  single comment bumped it). Fix at the write path so both consumers inherit:
  `prepareUpdate` now logs `sprint`/`parent`/`component` field changes
  (single-issue AND bulk paths share it); label attach/detach logs a `label`
  activity in all three paths (LabelsService add/remove + bulk `addLabelIds`
  classic AND atomic — atomic keeps rows inside the shared transaction) with
  no-op-safe semantics (repeat attach / detach-of-unattached logs nothing).
  Live-verified: PATCH parentId wrote a `field:'parent'` ActivityLog row.
  9 new unit tests; affected suites 365/365; API total 1790. Web ActivityPanel
  renders the new fields via its generic fallback (id-resolution for
  sprint/label names stays a filed P2). [orchestrator, from mcp-consumer-qa
  pass 2 be6f7d3]

- [x] (P1, S) **Idempotency claim-first hardening (2026-07-03)** — code-review
  follow-up on AX Round 2 (all 4 findings fixed same-day): `withIdempotency`
  rewritten claim-first (pending row inserted before the mutation; unique
  constraint elects one executor; concurrent duplicate polls for the winner's
  response — closes the client-timeout-retry duplicate the old
  check-then-write TOCTOU allowed); failed attempts release the claim instead
  of poisoning retries; post-commit `notifyAssignment` guarded in
  create/finishUpdate (a notify failure no longer 500s a committed write);
  reused key + different payload → 409 via stored request hash;
  `bulkUpdateAtomic`'s post-commit fan-out loop per-item guarded. Migration
  `20260703083000_idempotency_claim_first` (responseBody nullable +
  requestHash). Live-verified: replay same-id, 409 on payload mismatch,
  dryRun no-write, cross-project 400s. API 1731/1731. [orchestrator]

- [x] (P1, M) **Agent Experience Round 2 — "production-grade for AI-agent-driven PM" batch — SHIPPED 2026-07-03** (founder-relayed field report #2). Per-criterion:
  1. **Cross-project write validation** — fixed the confirmed-live P1: `create()` now runs the same `assertSameProject` guard as `update()`/`move()` for statusId/sprintId/parentId (was only checking componentId on create). Already-safe relations verified and left unchanged: `update()` (statusId/sprintId/parentId/componentId), `move()` (statusId + beforeId/afterId neighbor), `LabelsService.addToIssue`/`removeFromIssue` (single-issue label attach), `VersionsService.setIssueVersions` (bulk-replace) — all already validated their referenced ids belong to the same project. One additional real gap found and fixed: `bulkUpdate`'s `addLabelIds` path (`attachLabel`) had NO project-scope check at all — new `assertLabelsInProject` helper closes it in both the classic per-issue path and the new atomic path. 19 new unit tests (create cross-project ×3 fields + accept-same-project, bulk label cross-project ×2).
  2. **Idempotency keys** — additive `IdempotencyRecord` model (migration `20260703070000_add_idempotency_records`, numbered after the newest existing folder), `key`+`userId`+`endpoint` unique, ~24h window, opportunistic `deleteMany` cleanup on write. `common/idempotency.util.ts` (`withIdempotency`) wraps `IssuesService.create` and `CommentsService.create` end-to-end (optional `idempotencyKey` DTO field); a retry with the same key replays the ORIGINAL response, no duplicate — live-verified (same issue id + same comment id on retry, only one row created each). MCP `create_issue`/`add_comment` accept `idempotencyKey`; descriptions tell agents to pass one on retries. 16 new unit tests (util + wiring).
  3. **Bulk parenting / transactional** — `parentId` added to `BulkIssueChangesDto` (cross-project guard reused from `prepareUpdate`'s `assertSameProject`). `update()` refactored into `prepareUpdate`/`writeIssueUpdate`/`finishUpdate` phases so `bulkUpdateAtomic` can validate every issue in a batch first (zero writes on any failure) then write all of them inside ONE shared `$transaction` (all-or-nothing) before firing side effects once per issue. Rollback mid-transaction reports every id in the batch as failed. MCP `bulk_update_issues` gained `parentId`/`atomic`/`dryRun` params (still a compact `{updated, failed, atomic?, dryRun?, wouldUpdate?}` envelope). Live-verified: one atomic call parented 30 tickets under an epic (`{"updated":30,"failed":[]}`).
  4. **Dry-run/preview** — `dryRun: true` (with or without `atomic`) validates every id independently via the same `prepareUpdate` path and returns `{updated: 0, wouldUpdate, failed}` — zero writes, live-verified (priority unchanged after a `dryRun` bulk call).
  5. **Comment edit/delete over MCP** — REST (`PATCH`/`DELETE /comments/:id`) already existed but was author-only; upgraded to author-or-effective-project-ADMIN (mirrors `WorkLogsService`'s gating exactly, via `getEffectiveProjectRole`). New MCP `update_comment`/`delete_comment` tools.
  6. **Change feed for agents** — new `GET /projects/:id/activity` (additive, VIEWER+, `projects:read` PAT scope): k-way-merges `ActivityLog` + `Comment` + `WorkLog` rows project-wide into one chronological, cursor-paginated feed (`since`/`cursor`, ascending order), with a per-item compact `summary`. MCP `list_project_activity` tool (92→97 tools). True push subscriptions remain a later item.
  7. **Wrong-project guard strictness** — `create_issue`'s MCP description now uses MUST-pass language for `expectedProjectKey`; server `instructions` (`apps/mcp/src/index.ts`) and `skills/project-context/SKILL.md` both gained the mandate + idempotencyKey + list_project_activity guidance; new optional `NEXT_LANE_MCP_STRICT_PROJECT_KEY` env var makes omitting it a hard error.
  Folded in from MCP-QA pass 1: `AgentContextService.computeStaleness` now also counts `Comment`/`WorkLog` rows newer than the doc (was ActivityLog+AuditEvent only — a project with an active comment thread and zero field changes looked falsely fresh); `GET /users` gained a server-side case-insensitive `q` name/email substring filter (`list_users` MCP param); new MCP `create_project`/`create_workspace` tools wrapping the existing `POST /projects`/`POST /workspaces` REST endpoints, echoing `id`/`key`/`slug` first in the response.
  Gates: api build + tsc clean; 1727/1727 API jest green (was 1683, +44); tenant-isolation 103/103 BLOCKED (1 new row: GET activity feed for project A); shared vitest 146/146 (types-only addition, untouched); mcp build + 97/97 vitest green (was 84, +13); apps/mcp/README.md updated (92→97 tools, new tables, env var, strict-mode note). [founder field report #2]

- [x] (P1, S) **Epic swimlanes fix (2026-07-03)** — the board endpoints never
  loaded `issue.parent` (`issueInclude` in `board.service.ts` omitted it), so
  the web's `computeLanes` epic branch put every card in "No epic" — epic
  grouping was structurally broken since Swimlanes v2 shipped. Found by the
  screenshot-reshoot agent staging real epics. Fix: `parent` (IssueRef select
  incl. `project.key`) added to `issueInclude`; regression unit test asserts
  the include + mapped `parent` on the DTO (board suites 53/53). **Follow-up
  shipped 2026-07-06 (Hardening Night, `b40fbbf`)** — new epic-lane case added
  to `swimlanes.spec.ts` (group-by-epic renders one lane per parent epic + a
  "No epic" lane, seeded fixture issues land correctly, desktop + mobile);
  the coverage gap flagged below is closed. [orchestrator; qa-tester 2026-07-06]

- [x] (P2, M) VitePress documentation site (2026-06-28) — `docs-site/` registered as `@next-lane/docs` pnpm workspace package (`"type": "module"`); `vitepress ^1.6.3` devDependency; `base: '/Next-Lane/'`; Dispatch cobalt `#2563EB` design tokens (`--vp-c-brand-1/2/3` + soft + dark-mode overrides) in `docs-site/.vitepress/theme/custom.css`; extended default theme with premium gradient hero, card hover effects, H2 cobalt left-bar, inline-code cobalt tint, clean table headers; nav (Guide / Features / Self-Hosting / Architecture / GitHub), sidebar grouped in 5 sections (Getting Started / Using Next Lane / Operating / Contributing / Reference); local search; `editLink` pointing at repo; `lastUpdated: true`; footer (MIT); `ignoreDeadLinks: [/^http:\/\/localhost/]`; screenshots in `docs-site/public/screenshots/`; SVG logo + favicon (cobalt kanban icon); 9 content pages ported from `wiki/` with fixed internal links; GitHub Pages deploy workflow (`.github/workflows/docs.yml`) with pnpm@v4 + node22 cache + upload-pages-artifact@v3 + deploy-pages@v4; `docs-site/.vitepress/dist` artifact; `wiki/` deleted; `docs:build` confirmed green in 4s. [oss-curator]

- [x] (P2, M) Workspace branding backend (2026-06-28) — `PATCH /workspaces/:id` (name + `brandColor` #RRGGBB or null, Admin-only, 400 on bad hex); `POST /workspaces/:id/logo` (multipart `file` field, png/jpeg/webp only, svg explicitly rejected, 2 MB cap, replaces previous logo file, Admin-only); `DELETE /workspaces/:id/logo` (Admin-only, best-effort unlink); `GET /workspaces/:id/logo` (PUBLIC — @Public() decorator bypasses JwtAuthGuard so plain `<img src>` can load it; streams file with `Cache-Control: public, max-age=3600`; 404 when no logo); `UpdateWorkspaceDto` with `@Matches(/^#[0-9a-fA-F]{6}$/)` + null-allowed `brandColor` + trimmed `name`; `toWorkspaceDto` centralized mapper (brandColor passthrough, logoUrl derived from logoStorageKey); `WorkspaceRow` type expanded with branding fields; `LOGO_ALLOWED_MIME_TYPES` exported constant; 30 unit tests (745 total); build clean. [roadmap Phase 5]
- [x] (P2, M) Workspace branding frontend (2026-06-28) — Token refactor: `signal` + `brand` Tailwind color scales replaced with CSS-var references (`--nl-signal-50…900`); `:root` defaults in `index.css` set exact original electric-cobalt hex values (zero visual regression); `--nl-accent*` vars derive from signal vars (DRY). `applyBrandColor.ts` in `src/lib/`: generates full 50–900 scale from a single hex anchor (600=input; 50–500 mixed toward white; 700–900 mixed toward black with lightness steps mirroring the default cobalt scale); writes `--nl-signal-*` CSS vars on `documentElement`; clears inline overrides when null (restores `:root` defaults). `WorkspaceContext` (`src/contexts/WorkspaceContext.tsx`): React context provider mounted in `App.tsx` (inside `ToastProvider`, outside `RequireAuth`); tracks active workspace (first by default), calls `applyBrandColor` on every workspace/brandColor change, exposes `setActiveWorkspaceId` for workspace-scoped pages. AppHeader logo: `WorkspaceLogo` component renders `<img data-testid="workspace-logo" alt={workspaceName}>` at `API_URL/api/logoUrl` when set, falls back to default `<Logo>`. Branding settings page at `/workspaces/:workspaceId/branding` (ADMIN-only; non-admins see lock + "Admin access required" message): logo section (`data-testid="logo-upload-input"`, file picker accept png/jpeg/webp ≤4 MB with client-side type+size validation, live preview, Upload/Replace, `data-testid="logo-remove"` button, server errors → toast); accent-color section (`data-testid="branding-settings"`, 10 preset swatches with aria-pressed, `data-testid="brand-color-input"` hex input with live preview pane showing primary button + active nav chip + status chip, `data-testid="brand-color-save"`, Reset to default clears brandColor to null); workspace sub-nav on all three pages (Members / Audit log / Branding) unified to `NavLink` with `signal-50/signal-700` active style and `ink-*` tokens; `useUpdateWorkspaceBranding`/`useUploadWorkspaceLogo`/`useDeleteWorkspaceLogo` mutation hooks in `workspaces.ts`; build + tsc clean. [roadmap Phase 5]

- [x] (P2, S) Export CSV affordance (2026-06-28) — `apps/web/src/api/export.ts`: `useExportCsv` hook; `fetchCsvBlob` async function fetches `GET /projects/:id/issues.csv?q=<NLQL>` with `Authorization: Bearer <token>` header (localStorage JWT — not a cookie, so a plain anchor would 401), materialises response as Blob, triggers download via `URL.createObjectURL` + synthetic `<a download>` + `URL.revokeObjectURL` after 2 s; filename from `Content-Disposition` with `<projectKey>-issues.csv` fallback; `isExporting` React state disables button + shows spinner while in flight; error path calls `onError` callback → `toast.error("Couldn't export issues.")`. BacklogPage: button in header toolbar (left of "Estimate / Poker", left of "+ Create sprint"), `variant="secondary"` with download SVG icon, `data-testid="export-csv"`, `aria-label="Export issues as CSV"`, no filter param (exports all). BoardPage: button in `ml-auto` toolbar group (left of "+ Create issue"), same styling/testid; passes current `nlqlQuery` as `?q=` so active NLQL filter is respected on export. Both pages derive `projectKey` from cached board data for the fallback filename. Dispatch `secondary` token colours (ink border, white bg). Build clean.
- [x] (P2, M) Bulk edit backend (2026-06-28) — `POST /issues/bulk`; `BulkUpdateIssuesDto` (ids: non-empty string[], max 100 via `@ArrayMaxSize`; `BulkIssueChangesDto` with statusId, assigneeId|null, priority, sprintId|null, type, addLabelIds) + `BulkUpdateResultDto ({ updated: number; failed: { id, reason }[] }`); `IssuesService.bulkUpdate` delegates per-issue to existing `update()` so authz (MEMBER+), same-project validation, assignee-in-workspace check, ActivityLog, realtime, webhooks, and automation events all fire; `addLabelIds` calls private `attachLabel()` (prisma.issueLabel.upsert) per id per label after the field update; each issue wrapped in try/catch — one bad id (NotFound/Forbidden/Bad) is appended to `failed[]`, batch continues (partial success); empty `changes` and >100 ids throw `BadRequestException`; `@Post('bulk')` static route registered before param routes in `IssuesController`; 11 new unit tests (3 suites: all-succeed, partial-success, guards); 646 total green; build clean. [product-auditor parity gap]

- [x] (P0, M) Authenticate realtime gateway + membership-check subscribe [engineering-auditor]
- [x] (P0, M) Validate tenant ownership of statusId/sprintId/parentId/beforeId/afterId [engineering-auditor]
- [x] (P0, M) Enforce roles (VIEWER read-only; ADMIN-only member mgmt & deletes) [engineering-auditor, product-auditor]
- [x] (P0, S) Fail fast on missing JWT_SECRET + remove bypass paths [engineering-auditor]
- [x] (P1, S) CORS origin allowlist from env [engineering-auditor]
- [x] (P1, S) Fix label-chip contrast in issue drawer [ui-review]
- [x] (P1, S) Drawer as first-class overlay — scroll-lock, focus trap + restore [ui-review]
- [x] (P1, M) Replace native window.prompt/window.confirm with themed modals [ui-review]
- [x] (P1, M) Add a lightweight toast system; surface drawer-patch + mutation errors [ui-review, product-auditor]
- [x] (P0, S) Validate assigneeId is a workspace member on issue create/update [engineering-auditor]
- [x] (P0, S) Scope GET /users to the caller's co-members [engineering-auditor]
- [x] (P1, M) API unit tests for membership.util + assertSameProject + GitHub Actions CI [engineering-auditor]
- [x] (P1, S) Comment edit-in-place + delete in CommentsPanel [product-auditor]
- [x] (P1, S) Activity log: resolve status/user IDs to human names [product-auditor]
- [x] (P1, L) Backlog + sprint planning view [product-auditor, roadmap]
- [x] (P1, M) Labels assign/unassign + filter UI [product-auditor, roadmap]
- [x] (P1, M) Story points field + parent/child picker in issue drawer [product-auditor, roadmap]
- [x] (P1, S) Sprint TOCTOU race: move assertNoOtherActiveSprint inside $transaction + add partial unique index [engineering-auditor]
- [x] (P1, S) Sprint lifecycle: emit sprint.updated realtime event on start/complete [engineering-auditor]
- [x] (P1, M) Global exception filter: map Prisma errors to structured HTTP responses [engineering-auditor]
- [x] (P1, S) Input bounds: @MaxLength on description, @Min/@Max on storyPoints, hex validation on label color [engineering-auditor]
- [x] (P1, S) VIEWER-aware UI: hide/disable edit affordances based on role [product-auditor, engineering-auditor]
- [x] (P1, M) Manage board columns (statuses) from the board UI [user-reported]
- [x] (P1, M) Project Settings page [user-reported]
- [x] (P1, S) Webhook SSRF guard (DNS pre-flight + redirect:manual + socket drain + p-limit fan-out cap) [engineering-auditor, security]
- [x] (P1, S) Composite index for cursor pagination — `@@index([projectId, createdAt, id])` [engineering-auditor]
- [x] (P1, S) helmet() + @nestjs/throttler (100/min global, 10/min auth) [engineering-auditor]
- [x] (P3, S) First-run onboarding + optional sample project + 4-step tour [product-auditor]
- [x] (P1, S) Board type + priority filters [product-auditor Pass 4]
- [x] (P1, M) @mention autocomplete in comment composer [product-auditor Pass 4]
- [x] (P1, M) Password reset (POST /auth/forgot-password + time-limited token + SMTP/dev-log delivery) [product-auditor Pass 4]
- [x] (P3, M) File attachments (uploads) [roadmap Phase 2/3]

---

## Next (P2)

- [x] (P2, S) GET /users/:id authorization — scope to co-members [engineering-auditor]
- [x] (P2, M) Replace assertNoParentCycle sequential waterfall with single recursive CTE [engineering-auditor Pass 3, Pass 4]
- [x] (P2, M) Cursor pagination for issue list [engineering-auditor]
- [x] (P2, M) Transactional move + rank-collision rebalance [engineering-auditor]
- [x] (P2, S) Fix Dockerfile --no-frozen-lockfile → --frozen-lockfile [engineering-auditor]
- [x] (P2, M) Command palette (Cmd-K) navigation & actions [product-auditor]
- [x] (P2, S) Inline issue creation in backlog (ghost row, type-and-Enter) [product-auditor]
- [x] (P2, M) Plugin/webhook event system (HMAC-signed outbound POST on issue.* + sprint.* events) [engineering-auditor, product-auditor]
- [x] (P2, M) Observability baseline (pino structured logs, requestId, /health enrichment, OTel Prisma traces) — nestjs-pino shipped; requestId + OTel remain [engineering-auditor Pass 4]
- [x] (P2, M) Redis-backed webhook delivery queue with retries / BullMQ [engineering-auditor Pass 4]
- [x] (P2, S) Wire Socket.io Redis adapter [engineering-auditor Pass 4]
- [x] (P2, S) Add take cap to board and roadmap endpoints (take: 500 + hasMore flag) [engineering-auditor Pass 4]
- [x] (P2, S) useProjectIssues pagination waterfall — partial fix (limit=200) [engineering-auditor Pass 4]
- [x] (P2, S) Fix remaining e2e specs using DEMO user for mutating flows [qa-review Pass 4]
- [x] (P2, M) Password reset back-end [product-auditor, engineering-auditor]
- [x] (P2, L) Roadmap / timeline view (epics + sprints as bars) [product-auditor]
- [x] (P2, M) Wire the Playwright e2e suite into CI [hardening, roadmap]
- [x] (P2, M) Magic-byte MIME validation using `file-type` package on upload — client Content-Type is fully controllable; malicious files stored with mismatched MIME; read first 16 bytes after multer writes to tmpdir and compare against known magic signatures using `file-type` npm package [engineering-auditor Pass 5] ✅ shipped 2026-06-28 (Pass-8 hardening — workspaces logo endpoint; logo magic-byte MIME validation added to `workspaces.service.ts` via `assertLogoMagicBytes()`; 4 new unit tests; was already applied to attachments in Pass 5)
- [x] (P2, S) PAT authentication in WebSocket gateway handshake — `nlp_` tokens rejected at socket handshake; automation scripts using PATs cannot subscribe to project realtime events; check for `nlp_` prefix in `handleConnection` and validate via `ApiTokensService.validateRawToken` [engineering-auditor Pass 5] ✅ shipped 2026-06-27
- [ ] (P2, M) PAT token scope model + `@RequireScope` decorator — PATs currently carry full user permissions; extend PAT model with optional `scopes` string array enforced at route level; enables minimal-privilege CI tokens [engineering-auditor Pass 5]
- [x] (P2, M) Public read-only project share link — `ShareToken` model (SHA-256 hashed `nls_` token) + `GET /public/board/:token` (unauthenticated, rate-limited) + ADMIN mint/list/revoke at `/projects/:id/share-tokens` + `/share/:token` standalone read-only board view (no DnD, no create, no drawer; read-only banner); 17 unit tests + 14 e2e (desktop + mobile) — ✅ shipped 2026-06-27 (migration `20260627235000_add_share_token`) [product-auditor]
- [ ] (P2, S) Fix stale socket token after re-auth — getSocket() captures the token once at init; when refresh tokens land the socket will carry a stale credential until page reload [engineering-auditor]
- [ ] (P2, S) Inline card status transition (right-click / keyboard shortcut) — tiny context menu on the card showing 2–4 statuses; eliminates drawer round-trip for status changes [product-auditor]
- [x] (P2, M) Live board presence indicators (per-project viewer avatars via WebSocket) — `RealtimeGateway` maintains in-memory presence map; `presence.update` event emitted on subscribe/unsubscribe/disconnect; `PresenceAvatars` component (stacked avatars, +N overflow, aria-label, tooltip); `usePresence` hook (self-exclusion, cleanup unsubscribe); 7 unit tests + 6 e2e (desktop + mobile) — ✅ shipped 2026-06-27 [engineering-auditor, product-auditor Pass 5]
- [ ] (P2, S) Board-overview prefetch endpoint + stale-while-revalidate caching — collapses 4 sequential requests into 1 on first load [engineering-auditor]
- [ ] (P2, M) Slim planning-view endpoint (or virtual scroll) to avoid all-pages waterfall — full elimination: add `GET /projects/:id/issues/planning` with slim projection (id, title, type, priority, statusId, sprintId, rank — no description, no labels, no comment count) [engineering-auditor Pass 4, Pass 5]
- [x] (P2, M) Batch notifyComment inserts (createMany) + rebalanceAndPlace (executeRaw batch UPDATE) — serial N inserts per watcher; serial N updates in rebalance tx [engineering-auditor Pass 4, Pass 5] ✅ shipped 2026-06-27: `notifyComment` MENTIONED+COMMENTED fan-out now uses `createMany` (2 round-trips for any watcher count); `rebalanceAndPlace` uses single `$executeRaw` CASE UPDATE inside the existing `$transaction`; 5 new unit tests + 324 total green
- [x] (P2, M) Markdown rendering in issue descriptions and comments (marked/remark preview pane) — plain textarea signals immaturity to developers; no schema change; purely UI enhancement [product-auditor Pass 5] — ✅ shipped 2026-06-27: `marked` + `DOMPurify`; `MarkdownRenderer` component; description view/edit toggle; comment body rendered; XSS sanitized; @mention tokens survive; 10 e2e (desktop+mobile)
- [x] (P2, L) UI design elevation — "Slate + Teal-Shift" design system: deep teal accent, Plus Jakarta Sans + IBM Plex Mono (self-hosted), stone/amber/emerald status-progression arc, refined shadow/radius/animation scales, all primitives + board + drawer + auth elevated; nl-issue-key/nl-data-chip signature classes; prefers-reduced-motion; WCAG-AA; all test hooks preserved — ✅ shipped 2026-06-27 [ui-review]
- [x] (P2, S) Analytics/reports token-cohesion fix + chart primitives + P2 cheap-fixes (2026-06-28): `slate-*`→`ink-*` in `PersonalAnalyticsPage`, `ProjectAnalyticsPage`, `ReportsPage`; breadcrumb style in analytics/reports Shell matched to `AutomationsPage` reference; SVG grid/axis classes migrated from raw hex to `stroke-ink-*`/`fill-ink-*` in `FlowChart`, `ThroughputChart`, `VelocityChart`, `BurndownChart`, `CumulativeFlowChart`; `CategoryBars` colorFn refactored from hex to Tailwind class strings aligned with `issueMeta` vocabulary; `AutomationsPage` rule list `shadow-sm`→`shadow-card`; `ActionParamsEditor` DATE uses `ui/Input`, CHECKBOX adds `focus-visible:ring`; `AddCardComposer` input uses `focus-visible:ring-*`; `PromoteCardModal` raw `<select>`→`ui/Select`; `ProjectNav` `<nav>` gets `aria-label="Project navigation"`. Build clean. [ui-review 2026-06-28]
- [ ] (P2, S) Theme tokens for issueMeta hardcoded hex (IssueTypeIcon bg + PRIORITY_META color); create-issue modal single-column on mobile (grid-cols-1 sm:grid-cols-2) [ui-review]
- [ ] (P2, S) Extract shared InlineError/FormError (4 duplicated banners) + drawer title aria-label + min 40px touch tap targets [ui-review]
- [x] (P2, S) Attachment admin-delete UX — `AttachmentsPanel` shows the delete button to all editors but a project ADMIN who tries to delete another user's file gets a 403 toast; pass current user's role to `AttachmentRow` and set `canDelete` to `true` when `editable && (isUploader || isAdmin)` [product-auditor Pass 5] — ✅ shipped 2026-06-27: `viewerRole` prop propagated page→drawer→panel; ADMIN sees delete on any attachment; VIEWER/non-uploader MEMBER does not; 4 e2e (desktop+mobile)
- [ ] (P2, S) Webhook signing key rotation endpoint — no rotation path without deleting and recreating subscription; add `POST /projects/:id/webhooks/:wid/rotate-secret` [engineering-auditor Pass 5]
- [ ] (P2, M) DB-level time-series reports: throughput, cycle time, age distribution — once generate_series aggregation is in place, additional report types are marginal cost; high value for team health visibility [engineering-auditor Pass 5]
- [x] (P2, S) Add `removeMember` UI in workspace member management — `useRemoveMember` hook (invalidates members query); `WorkspaceMembersPage` at `/workspaces/:id/members` with sorted member list, ADMIN-only Remove button (hidden for self + non-ADMINs), ConfirmDialog guard, server-error toast; Members nav button on dashboard (ADMIN-only); workspace sub-nav (Members / Audit log); 11 desktop + 11 mobile e2e — ✅ shipped 2026-06-27 [product-auditor]
- [x] (P2, S) Add e2e spec for audit log — `audit-log.spec.ts`: ADMIN performs audited action (API token create + member add/remove), confirms events appear in table; non-admin VIEWER/MEMBER: nav button hidden + direct-route shows access-denied; member management UI tests (Remove button visibility, remove flow, role guard); 11 desktop + 11 mobile tests — ✅ shipped 2026-06-27 [test-infra]
- [x] (P2, M) Issue links / dependencies frontend (2026-06-28) — `apps/web/src/api/issue-links.ts` with `useIssueLinks`/`useAddIssueLink`/`useRemoveIssueLink` hooks + `qk.issueLinks(issueId)` key; `LinkedIssuesSection` component in IssueDetailDrawer sidebar between ParentSubtasks and CustomFieldsDrawerSection; links grouped by label (blocks/is blocked by/relates to/duplicates/is duplicated by/clones); each row shows related issue key (mono/cobalt) + title + status chip, clickable to navigate; remove (x) button MEMBER+; "Add link" form with 6-type select and issue-key input; optimistic remove with rollback; 400/404/409 errors surfaced as error toasts; data-testids `issue-link-add/type/target/row/remove`; board card badge skipped (board payload excludes links; N+1 per-card fetches avoided); `apps/web/e2e/issue-links.spec.ts` (desktop + mobile); build green. ✅ shipped 2026-06-28 [roadmap Phase 5]
- [x] (P2, M) E2E test suite repair (2026-06-28) — root-caused and fixed 15 pre-existing Playwright failures introduced by the design-elevation redesign + feature additions: (1) `LabelPicker` missing `boardId` → `useToggleIssueLabel` prop chain, so label toggles never invalidated the `boardView` cache; (2) description Edit button missing `aria-label="Edit description"`, causing strict-mode collision with LabelPicker's "Edit"; (3) `IssueCard` missing `data-testid="issue-card"` (due-date specs relied on `[class*="rounded-lg"]` which the redesign removed); (4) `CumulativeFlowChart` legend missing `data-testid="cfd-legend-item"` (specs relied on `.text-gray-500` which the redesign renamed); (5) six ConfirmDialog-using specs using `role="dialog"` instead of `role="alertdialog"`; (6) attachments download spec used `waitForEvent('download')` (doesn't fire for blob: URLs in headless Chrome) — replaced with `page.route()` interception. CI: added `PW_API_LOG` to `e2e.yml` for password-reset log discovery. 3 password-reset + 2 webhooks remain env-only (no Redis in harness); 219/226 pass in the no-Redis environment. ✅ shipped 2026-06-28 [test-infra, qa]

---

## Later (P3)

_Hardening Night close-out ingest (2026-07-06):_

- [ ] (P3, S) **`list_project_activity`: malformed `cursor` should 400, not silently restart from history start** — MCP-QA pass 2 finding 6: a garbage `cursor` value is silently ignored and returns the full history from the beginning (same as omitting it entirely) — a truncated cursor in an agent's polling loop restarts the walk with no signal, risking double-processing of already-seen items. `since` already 400s on a malformed value; `cursor` should too. [MCP-QA pass 2, finding 6]
- [ ] (P3, S) **GitLab auto-transition e2e parity** — `pr-auto-transition.spec.ts` drives a real locally-signed webhook end-to-end for GitHub only; add the GitLab mirror (a locally token-verified `merged` MR webhook through the auto-transition toggle, asserted against the REST issue endpoint), matching the existing GitHub coverage depth. [qa-tester 2026-07-06 P3]
- [ ] (P3, M) **`@nestjs/core` moderate CVE — Nest v10→v11 major upgrade** — the one remaining `pnpm audit --prod` moderate advisory that isn't a drop-in `pnpm.overrides` fix; needs its own migration/breaking-change review across the Nest ecosystem, deliberately not bundled with the cheap dependency-refresh item below. [engineering-auditor Pass 13 Risk 4]
- [ ] (P3, S) **Dependency refresh: `file-type` v16→v21 + `js-yaml`/`qs` moderates** — `file-type`'s CVE fix needs v20+ (ESM-only; requires a dynamic `import()` refactor of the two `require('file-type')` call sites in `attachments.service.ts`/`workspaces.service.ts`); `js-yaml`/`qs` are minor-version-safe candidate bumps via `@nestjs/swagger`/`@nestjs/platform-express`. Bundle as one dependency-refresh pass; re-run `pnpm audit --prod` after to confirm clean. [engineering-auditor Pass 13 Risk 4]
- [x] (P3, S) **Document `GITLAB_TOKEN_ENCRYPTION_KEY` in `.env.example`** ✅ shipped 2026-07-06 (closed as a side effect of the config-parity CI smoke test build — see "Already Done" below; the reverse-direction check that build introduced caught this gap for real, along with `GITLAB_WEBHOOK_BASE_URL`/`AUTO_SEED`/`WEB_BASE_URL`/`OIDC_CONFIG_ENCRYPTION_KEY`, which were also undocumented). [engineering-auditor Pass 13 Risk 5]
- [ ] (P3, S) **Log the swallowed post-commit side-effect failure in `bulkUpdateAtomic`** — the post-commit fan-out loop (`issues.service.ts`) wraps `finishUpdate(...)` in a bare `try/catch` with no `logger.warn`/`.error`, unlike every other post-commit-failure catch in the codebase; a webhook/notification/realtime emit failure after an atomic batch commit currently leaves zero trace. [engineering-auditor Pass 13 Risk 6]
- [ ] (P3, S) **Starter-gadget first-run has no in-product explanation** — a project's first dashboard is silently pre-populated with 3 starter gadgets; nothing tells a first-time user they're editable/deletable defaults, not fixed system gadgets. A one-time dismissible hint under the dashboard tabs would close it. [UI-REVIEW.md 2026-07-06, P3 #1]
- [ ] (P3, S) **Staleness pill's "why" is mouse-hover-only** — `AgentContextSection`'s amber staleness-pill explanation lives only in a `title` attribute, unlike `IssueCard`'s blocked badge which pairs `title` with an `aria-label` carrying the same text for touch/AT users. Low priority — the pill's own visible text ("N changes since last update") is already fairly self-explanatory. [UI-REVIEW.md 2026-07-06, P3 #2]

- [ ] (P3, S) **`ReportsPage`'s `selectedSprintId` never re-validates after the initial pick** — if the selected sprint is later deleted, the seed-once effect keeps pointing at the dead id and `useBurndown` silently returns empty/404 data instead of falling back. Change the guard to also reset when `selectedSprintId` is set but no longer present in `sprints` (same "clamp instead of reset-on-refetch" shape `BoardPage`'s board-selection healing effect already uses). [engineering-auditor Pass-11 P3-3]
- [ ] (P3, S) **`updateColumn` allows an arbitrary `order` write that can desync from the `reorderColumns` invariant** — dead/unused-but-exposed API surface (the web UI only ever calls the atomic `reorderColumns` endpoint); prefer removing `order` from `UpdatePersonalColumnDto` entirely over adding a defensive tiebreaker. [engineering-auditor Pass-10 Risk #5]
- [ ] (P3, M) **"Board health" proactive nudge banner** — a lightweight, dismissible banner/badge surfacing board hygiene at a glance: N issues blocked with no comment in 7+ days, N issues with no assignee in an active sprint, N stale issues untouched 14+ days. Turns existing analytics/automation infrastructure into a proactive nudge instead of a passive report. [product-auditor Pass 10 ideation #1]
- [ ] (P3, S) **Inline card context menu** (quick sprint/epic/assignee from the board card) — right-click/long-press menu removes several clicks from the most repetitive daily board action. [product-auditor Pass 10 ideation #2]
- [ ] (P3, M) **Public embeddable project status page** (share-token reuse) — a read-only, branded, no-login "project status" page (sprint progress, recently completed, upcoming milestones) a team can send to a client/stakeholder. [product-auditor Pass 10 ideation #3]
- [ ] (P3, S) **Recently-visited project switcher (+ command palette)** — the workspace half of this shipped 2026-07-02 (recently-visited workspaces in the header chip, verified live with a 14-workspace fixture in Pass 12 — see the ticked "Workspace-switcher search/filter + recently-visited" entry). Remaining scope: the same recency treatment for projects within a workspace, and surfacing recents in the Cmd-K command palette, not just the workspace chip. [product-auditor Pass-11 ideation #3, workspace half closed Pass 12]
- [ ] (P3, S) **Design-elevation carry-forward** — `docs/UI-REVIEW.md` § Remaining gaps tracks the still-unaudited/unmigrated surfaces (`WebhookFormModal`, `ShareSection`, `WorkspaceMembersPage`, `WorkspaceAuditLogPage`, `SharedBoardPage`, `IssueDetailDrawer` sub-panels, `CommandPalette`, `ProjectCard`/`CreateProjectModal`) — work this list down per the standing design-elevation directive in CLAUDE.md. [frontend-qa / UI-REVIEW.md]
- [ ] (P3, S) **GitHub webhook — log `X-GitHub-Delivery` on signature-verification failures** — diagnosability improvement, lets a self-hoster correlate a failed delivery in GitHub's own repo-settings UI with the exact Next Lane log line. [engineering-auditor Pass-12, diagnosability idea]
- [ ] (P3, S) **Swimlane WIP/health summary strip** — now that Swimlanes v2 groups issues into real named sections (component/label/custom field/sprint), add an optional per-swimlane summary strip (count, oldest-issue age, or a configurable custom-field rollup like "story points remaining") in the swimlane header, the same way each status column already shows a count badge. [product-auditor Pass-12 Ideation #3]
- [ ] (P3, S) **Unify the mobile theme-control location** — `MobileSidebarDrawer.tsx` reuses `SidebarNavContent` but doesn't render `<ThemeToggle>` or the collapse control that desktop's `AppSidebar.tsx` footer has; the toggle is still reachable via the avatar/user menu on mobile (not a dead end), but it's two different mental-model locations for the same control depending on viewport width. Either add `<ThemeToggle>` to `MobileSidebarDrawer.tsx`, or intentionally document/standardize on the avatar-menu placement for mobile. [product-auditor Pass-12 rank #7]
- [ ] (P3, S) **SSRF hardening for outbound `GithubClient`/`GitlabClient` calls, once real outbound requests land** — both clients are currently unit-tested/mocked seams with zero real network egress (webhook payloads carry everything v1 needs); the moment the PR-status/live-CI-status work (Ready queue, above) makes its first *real* outbound call (a self-hosted GitLab base URL is admin-supplied, the same shape of risk the webhook-URL SSRF guard already closes for outbound webhooks), it must reuse the existing `resolveAndCheckBlocked()`/`isBlockedIp()` DNS-preflight + `redirect: 'manual'` guard (`webhooks.service.ts`) rather than a bare `fetch`. Filed now as a standing note (also cross-referenced on the PR-status Ready item) so that build doesn't reintroduce the exact class of bug the webhook SSRF guard already fixed once. [engineering-auditor pattern carryover; filed at GitLab v1 ship, 2026-07-03]
- [ ] (P3, M) **MCP session-start token cost: ~97 tools ≈ 73+ KB of schemas at `tools/list`** — MCP-QA pass 1, finding 8: not blocking (hosts cache the list after the first call), but it grows with every feature per the standing MCP-exposure definition-of-done, and was already ~18K tokens before the first real call at 91 tools (now 97, growing). **Fix shape (from the QA report, not yet designed):** grouped/dynamic toolsets, or trimmed descriptions for the long tail (the 10 biggest descriptions were all >500 B) — needs a real design pass, not a one-line change; revisit if a host's context budget starts getting measurably squeezed by it. [MCP-QA pass 1, finding 8]
- [ ] (P3, S) **`color-mix()` opacity utilities have no fallback for pre-2023 browsers** — `withOpacity()` (`tailwind.config.js`) emits `color-mix(in srgb, ...)` for every `/NN`-opacity Tailwind utility built on a CSS-var color; browsers older than Safari 16.4/Chrome 111/Firefox 113 drop the entire declaration (no color at all) rather than degrading. Low risk for a 2026 self-hosted target audience — not urgent; if pursued, emit a plain fallback declaration immediately before the `color-mix()` one for load-bearing surfaces (scrim, modal backdrop), or just document the browser floor in `docs/ARCHITECTURE.md`. [engineering-auditor Pass-12 P3-1]

- [x] (P3, M) REST API tokens (PATs) — shipped 2026-06-27 [roadmap]
- [x] (P3, M) Workspace audit log — shipped 2026-06-27 [roadmap]
- [x] (P3, S) Label rename / edit — shipped 2026-06-27 [product-auditor]
- [x] (P3, S) VITE_API_URL is baked at build-time — shipped 2026-06-27 [qa-review Pass 4, roadmap]
- [x] (P3, M) "Team pulse" home dashboard — shipped 2026-06-27 [product-auditor Pass 4]
- [x] (P3, L) Keyboard triage mode — shipped 2026-06-27 [product-auditor Pass 4]
- [x] (P3, M) Structured request logging (pino/nestjs-pino) + enriched /health + optional OTel export — nestjs-pino shipped 2026-06-27; enriched /health + OTel remain as follow-ups [engineering-auditor Pass 4]
- [x] (P3, S) Scrub trademarked category phrase from seed data [ui-review]
- [ ] (P3, M) Per-project "Definition of Done" checklist — ADMIN configures a per-project checklist (JSON array on Project model) surfaced as a blocking prompt when an issue is moved to a DONE-category status [product-auditor Pass 4]
- [x] (P3, L) Automation rules engine backend (trigger → action: status/assignment/label) — `AutomationsModule` at `apps/api/src/automations/`: `AutomationEngineService` (@OnEvent for ISSUE_CREATED/UPDATED/TRANSITIONED/COMMENTED; loop guard automated=true; NLQL parse+evaluate; AutomationRun Glass Box logging for every rule run: SUCCESS/SKIPPED/FAILED); `AutomationsService` (CRUD + NLQL validation + action param validation); 7 project-scoped REST endpoints; EventEmitter2 seams on IssuesService + CommentsService mutation paths; 37 new unit tests (668 total); tsc clean ✅ shipped 2026-06-28 [product-auditor, roadmap]
- [x] (P3, L) Saved filters backend + NLQL query validation — `SavedFiltersModule` (`GET/POST /projects/:projectId/saved-filters`, `PATCH/DELETE /saved-filters/:id`); NLQL query validated with `validateQuery` (custom-field-aware); owner-only update/delete (403 for non-owner); list returns caller's own + shared filters ordered by name asc; `toSavedFilterDto` mapper (isShared→shared, dates→ISO); board `updateBoard` now validates `filterQuery` and each `colorRules[].query` via NLQL before persisting; 27 new unit tests (441 total); `tsc --noEmit` clean. ✅ shipped 2026-06-28 [roadmap Phase 5]
- [x] (P3, L) NLQL query bar + saved filters frontend — inline query bar on BoardPage (data-testid="nlql-query-input") with live `validateQuery` + inline error (data-testid="nlql-error"); `filterIssues` applied on top of pill filters (AND composition) using currentUser/users/customFieldDefs context; saved filters dropdown (data-testid="saved-filter-select") shows personal + shared (shared badge), selecting loads query; Save button (data-testid="saved-filter-save") opens Modal (name + share toggle); owner rename (edit modal) + delete (ConfirmDialog); `apps/web/src/api/saved-filters.ts` hooks (useSavedFilters/useCreateSavedFilter/useUpdateSavedFilter/useDeleteSavedFilter + savedFilterKeys); `apps/web/e2e/nlql-filter.spec.ts` (desktop + mobile: valid/invalid query, save+reload+select, shared badge, delete); build green. ✅ shipped 2026-06-28 [roadmap Phase 5]
- [x] (P2, S) **Reuse NlqlInput autocomplete in automation condition + card-color query fields** (2026-06-29) — `NlqlConditionInput` in `apps/web/src/components/automation/NlqlConditionInput.tsx` now wraps `NlqlInput` (combobox + suggestions) instead of plain `<Input>`; added `projectId` prop (fetches labels/users/sprints/components/customFields context automatically); `AutomationRuleEditor` threads `statuses.map(s=>s.name)` as `statuses` prop. `RuleRowEditor` inside `CardColorsManager` likewise replaced with `NlqlInput`; `CardColorsManager` adds `useStatuses(projectId)` to supply status-name vocab and passes `projectId`+`statusNames` down to each row. All existing `data-testid` hooks preserved (`automation-condition-input`, `color-rule-query`); no BoardPage/API changes; `tsc --noEmit` + `pnpm --filter @next-lane/web build` clean. ✅ shipped 2026-06-29
- [x] (P2, M) **NLQL query autocomplete** (2026-06-29) — context-aware suggestion engine in `packages/shared/src/nlql/suggest.ts` (`suggestNlql(source, cursor, ctx)` pure function; exported via nlql/index.ts); state machine infers parse phase (field / operator / value / logical / orderByField / orderByDirection) by tokenizing the prefix up to the cursor, splitting into completed tokens + partial token; field suggestions (16 canonical fields), per-kind operator sets (=/<>/IN/IS NULL etc.), value suggestions from live context (statuses/types/priorities/labels/assignees/components/sprints/custom-field keys), `me()`/`now()`/`today()`/`startOfDay()` function completions for applicable fields, AND/OR/ORDER BY after complete comparisons, ORDER BY field names + ASC/DESC direction; quoting of values with spaces; 47 unit tests (122 total shared tests pass); `NlqlInput` React component in `apps/web/src/components/board/NlqlInput.tsx` — combobox ARIA pattern (`role=combobox`, `aria-expanded`, `aria-controls`, `aria-activedescendant`; listbox `role=listbox`; options `role=option`; `aria-label="NLQL suggestions"`); keyboard UX (ArrowDown/Up highlight, Enter/Tab accept with focus preserved, Escape close, `onMouseDown e.preventDefault()` to prevent focus loss on click); loads context from `useLabels`/`useUsers`/`useSprints`/`useComponents`/`useCustomFields`; Dispatch kind-color badge tags (field=signal/50-700, operator=ink, keyword=emerald, function=amber, value=ink); mobile-safe dropdown (`w-full min-w-[18rem] max-h-64 overflow-y-auto`); wired into `BoardPage.tsx` replacing plain `<Input>` in `NlqlQueryBar`, threads `statuses` + `customFieldDefs` props; `data-testid`: `nlql-query-input` / `nlql-suggestions` / `nlql-suggestion-N`; `apps/web/e2e/nlql-autocomplete.spec.ts` (desktop: field suggestion on "pri", Enter accepts, full field→operator→value→filtered board flow, Escape closes, URL param persists, combobox+listbox+option ARIA roles, ORDER BY suggestion; mobile: no horizontal overflow, Tab accepts); build + `tsc --noEmit` clean. ✅ shipped 2026-06-29 [roadmap Phase 5 / DX]
- [x] (P3, M) Custom fields (typed, JSONB-backed) — shipped 2026-06-28 [roadmap]
- [x] (P3, M) SMTP email notification delivery for all notification types (opt-in per user; nodemailer/SMTP env config) — shipped 2026-06-28: `emailNotifications` User field + migration `20260628110000_add_user_email_notifications`; `PATCH /auth/me` profile-update endpoint; `sendEmailToRecipients` fan-out in `NotificationsService` covers ASSIGNED / MENTIONED / COMMENTED / WATCHED_UPDATED; issue deep-link URL in email body; fire-and-forget (never blocks in-app flow); error-resilient outer try/catch; `UpdateProfileDto` validated DTO + `AuthService.updateProfile`; 809 tests green. Frontend opt-in toggle (`NotificationPreferencesSection`, role=switch) on `/me/settings` wired to `PATCH /auth/me` via `useUpdateProfile` (persists across reload); ProfileSettingsPage elevated to Dispatch `ink-*` tokens; 2 e2e + live dev-mail smoke [product-auditor Pass 4, Pass 5] [frontend shipped 2026-06-28]
- [ ] (P3, L) JWT migration to httpOnly cookie + POST /auth/refresh (short-lived access tokens) — token in localStorage is XSS-extractable; cookie migration + short-lived access tokens is the durable fix; helmet CSP mitigates the XSS vector adequately for now; revisit when a rich-text editor lands [engineering-auditor Pass 4]
- [ ] (P3, M) Sprint retrospective panel (What went well / improve / actions — JSONB on Sprint, retro badge on velocity chart) — keeps sprint story in one place; natural extension of sprint lifecycle [product-auditor Pass 5]
- [x] (P3, M) Issue templates per project — backend + frontend shipped 2026-06-29 (see Ready queue above) [product-auditor Pass 5]
- [x] (P3, L) Tracker importers (Jira/GitHub/Linear) — file-based only (`?source=jira|github|linear`; no live API calls); ships as a pre-normalisation layer on top of the existing CSV import pipeline: header-alias maps + enum-value maps per tracker; GitHub JSON array path; `issues-import.sources.ts` pure module; `ImportSource` type in `@next-lane/shared`; 126 unit tests; 1303 total green; `tsc --noEmit` clean. SSO/OIDC remains open. [roadmap, shipped 2026-06-29]
- [ ] (P3, S) Optional metrics endpoint + `ServiceMonitor` + OTLP traces (builds on pino structured logging) for operator-grade observability in-cluster [engineering-auditor]

## Cloud-native / Kubernetes (post-v1 epic — ROADMAP Phase 4)

Make Next Lane deployable on Kubernetes with HA + autoscaling, keeping the
single-host Compose path for small installs. Sequenced so single-replica Helm
can ship first; multi-replica HA depends on the Redis items below.

- [x] (P3, S) Web runtime config for `VITE_API_URL` (+ public config) — shipped 2026-06-27 [roadmap, qa]
- [x] (P3, M) Socket.io Redis adapter — shipped 2026-06-27 [engineering-auditor]
- [x] (P3, M) Redis-backed webhook delivery queue (BullMQ) — shipped 2026-06-27 [engineering-auditor Pass 4]
- [x] (P3, M) Schema migrations as a Helm pre-upgrade Job — shipped 2026-06-27 [roadmap]
- [x] (P3, L) Helm chart `deploy/helm/next-lane` — shipped 2026-06-27 [roadmap]
- [x] (P3, S) K8s Secret strategy — shipped 2026-06-27 [roadmap]
- [x] (P3, M) Kustomize base + overlays as a Helm alternative — shipped 2026-06-27 [roadmap]
- [x] (P3, S) `docs/DEPLOY-KUBERNETES.md` — shipped 2026-06-27 [roadmap]
- [x] (P3, M) Publish `api` + `web` container images to GHCR via CI — shipped 2026-06-27 [roadmap]
- [x] (P2, M) Wire the Playwright e2e suite into CI — shipped 2026-06-27 [hardening, roadmap]
- [ ] (P3, S) Optional metrics endpoint + `ServiceMonitor` + OTLP traces for operator-grade observability in-cluster [engineering-auditor]

_Engineering-audit Pass-9 hardening — ✅ all shipped 2026-06-30:_

- [x] (P1, S) **WebSocket CORS allowlist** — `realtime.gateway.ts` had `@WebSocketGateway({ cors: true })` (wide-open); module-level `_wsCorsOption` variable now mirrors `main.ts` CORS_ORIGINS parsing: when env var is set, uses `{ origin: string[], credentials: true }`; falls back to `true` (local-dev safe, not production-safe); no `allowedHeaders` regression [engineering-auditor Pass 9] ✅ shipped 2026-06-30
- [x] (P1, S) **WorkflowTransition `toStatusId` index** — `@@index([projectId, toStatusId])` added to `WorkflowTransition` in `schema.prisma` (between the existing `[projectId, fromStatusId]` and `[workflowId]` indexes); hand-authored migration `20260630000000_workflow_transition_to_status_index`; `prisma validate` confirms schema valid; migration SQL: `CREATE INDEX "WorkflowTransition_projectId_toStatusId_idx" ON "WorkflowTransition"("projectId", "toStatusId")` [engineering-auditor Pass 9] ✅ shipped 2026-06-30
- [x] (P2, S) **`exportCsv` 10 000-row cap with `X-Next-Lane-Truncated` header** — `IssuesService.CSV_ROW_CAP = 10_000` static constant; `findMany` uses `take: CAP + 1` (no extra COUNT query); truncation detected by length comparison, rows spliced to CAP; service returns `{ csv, projectKey, truncated }`; controller sets `X-Next-Lane-Truncated: true` response header when truncated [engineering-auditor Pass 9] ✅ shipped 2026-06-30
- [x] (P2, S) **Password `@MinLength` consistency** — `RegisterDto.password` raised from `@MinLength(6)` to `@MinLength(8)`, aligning with `ResetPasswordDto.newPassword`; seed and test-service code bypass DTO validation so no test changes needed [engineering-auditor Pass 9] ✅ shipped 2026-06-30
- [x] (P2, S) **WorkLogsService `assertProjectMember` refactor** — `update()` and `remove()` replaced a bespoke private `resolveWorkspaceId()` + raw `membership.findUnique` pattern with `assertProjectMember` from `common/membership.util`; the helper returns the project (with `workspaceId`) so the subsequent role lookup is unchanged; 403/404 semantics identical; `resolveWorkspaceId()` private method removed [engineering-auditor Pass 9] ✅ shipped 2026-06-30
- [x] (P2, S) **AUTO_SEED default flip** — `docker-entrypoint.sh` default changed from `${AUTO_SEED:-true}` → `${AUTO_SEED:-false}`; `docker-compose.yml` api service now explicitly sets `AUTO_SEED: "true"` (local/demo stack unaffected); production deployments that omit the env var no longer auto-seed demo data [engineering-auditor Pass 9] ✅ shipped 2026-06-30
- [x] (P2, M) **Tenant-isolation matrix extended to >65 rows** — `tenant-isolation.integration.spec.ts` provisioning extended with `workLogId`, `pokerSessionId`, `personalColumnId`, `issueTemplateId`; 19 new `buildMatrix()` rows covering: work-logs GET/POST/PATCH/DELETE, standups GET/POST, issue-templates GET/POST/PATCH/DELETE, personal-columns PATCH/DELETE, poker sessions GET/POST + session GET/PATCH; sanity threshold raised to >65; 1306 unit tests green; build clean [engineering-auditor Pass 9] ✅ shipped 2026-06-30

_Engineering-audit Pass-8 hardening — ✅ all shipped 2026-06-28:_

- [x] (P1, S) **Logo upload magic-byte validation** — `workspaces.service.ts` `uploadLogo` trusted the client-declared MIME type; content-sniffed via `file-type` library (same as `attachments.service.ts`); reject files whose actual magic bytes don't match declared type or aren't in the png/jpeg/webp allowlist; 4 new unit tests (real PNG/JPEG/WebP + spoofed file); `workspaces.service.spec.ts` [engineering-auditor Pass 8] ✅ shipped 2026-06-28
- [x] (P1, M) **Tenant-isolation matrix extended** — `tenant-isolation.integration.spec.ts` extended to cover workflow GET/PATCH/transitions (POST/PATCH/DELETE), automations CRUD + runs, analytics GET, CSV export GET, and workspace logo DELETE; matrix now >45 rows; sanity check assertion updated; Tenant fixture provisioning extended with `automationRuleId` + `workflowTransitionId` [engineering-auditor Pass 8] ✅ shipped 2026-06-28
- [x] (P2, S) **Automation rules ADMIN-gated** — `automations.service.ts` `create`/`update`/`delete` changed from `Role.MEMBER` to `Role.ADMIN` (aligned with webhooks); reads (list rules/runs) remain at MEMBER; 3 new MEMBER-rejection unit tests + 2 "MEMBER is sufficient for reads" tests [engineering-auditor Pass 8] ✅ shipped 2026-06-28
- [x] (P2, S) **Automation TRANSITION/ADD_LABEL cross-project validation** — `automations.service.ts` `validateActionParamsDeep()` async helper added; DB lookup verifies `statusId` (TRANSITION) and `labelId` (ADD_LABEL) belong to the rule's project; reject at save time with 400; 6 new cross-project unit tests in `automations.service.spec.ts` [engineering-auditor Pass 8] ✅ shipped 2026-06-28
- [x] (P2, S) **Analytics `days` parameter validation** — `analytics.controller.ts` replaced raw `Number(daysStr)` with `AnalyticsQueryDto` (`@IsOptional @Type(()=>Number) @IsInt @Min(1) @Max(366) days?: number`) enforced by the global `ValidationPipe`; new file `analytics/dto/analytics-query.dto.ts`; 9 validation unit tests in `analytics-query.dto.spec.ts` [engineering-auditor Pass 8] ✅ shipped 2026-06-28
- [x] (P2, M) **bulkUpdate workflow-context preload** — `issues.service.ts` `bulkUpdate` pre-loads `workflowEnforced` once per batch (via `workflowSvc.isEnforcementEnabled(projectId)`) and passes it to each per-issue `update()` call; `WorkflowService.enforceTransition` uses the hint to skip the per-issue DB lookup (was N×2 extra queries per bulk call → now 1 lookup); `MutationOpts.workflowEnforced?: boolean` added; 4 new unit tests (2 enforcement-state × 2 behavior scenarios) [engineering-auditor Pass 8] ✅ shipped 2026-06-28
- [x] (P2, M) **projectAnalytics workload SQL aggregation** — `analytics.service.ts` `projectAnalytics` replaced in-memory issue materialisation + JS groupBy with a single `$queryRaw` `SELECT GROUP BY "assigneeId"` query (LEFT JOIN User; NOT IN doneIdList; open-issues-only; `open_count::bigint`); eliminates O(N) JS grouping for large projects; "uses SQL GROUP BY" assertion added to `analytics.service.spec.ts` [engineering-auditor Pass 8] ✅ shipped 2026-06-28

---

## Changelog
- 2026-07-06 (Hardening Night close-out groom) — Reconciled tonight's shipped commits against the board (`b40fbbf` epic-lane e2e, `d36e0f4` audit pass 13, `4aec12a` PAT-scope rollout completion, `3c22f21` docker-compose env passthrough, `c3a6cef` UI review pass, `908504a` frontend P1 batch, `6fd9201` security wave 2). **Verified:** the build agents' same-commit doc updates already ticked all of tonight's shipments in "Already Done" except one gap — the Ready-queue "Epic-swimlanes e2e coverage gap" item was still unticked despite shipping via `b40fbbf`; ticked it, dropped it from the queue, and cross-referenced the closure into the original epic-swimlanes fix's "Already Done" entry. **Re-sequenced Ready** per the Better-than-Jira switcher-impact test: NLQL unresolved-name diagnostics first (cheap, fully unblocked, unchanged from Pass 13), then Gitea integration v1 and dashboard public embed (both unblocked P2/M extensions), then cross-project issue MOVE last (still founder-decision-gated — kept visible, not silently dropped). **Ingested 16 new items, deduped against the existing board (confirmed none pre-existing via targeted grep before adding):** a dynamic PAT-scope route-coverage test via Nest `DiscoveryService` (P2, S/M — code-reviewer-endorsed 2026-07-06, closes the "hand-maintained matrix can silently drift" gap in the new `pat-scope-rollout.integration.spec.ts`) and a config-parity CI smoke test for `docker-compose.yml` vs `.env.example` (P1, S–M — the structural closer for tonight's compose fix, mirrors `scripts/smoke-web-csp.sh`, filed at the top of "Next (P1)" since it's cheap and fully unblocked); three `docs/MCP-QA.md` pass 2 findings — `bulk_update_issues` `expectedProjectKey` guard + `{id,key}` echo (P2, finding 3, the biggest-blast-radius MCP writer with no misfire guard), `list_project_activity` raw-cuid status/assignee summaries (P2, finding 2), and a malformed-`cursor` silently restarting from history start instead of 400ing (P3, finding 6); a GitLab auto-transition e2e parity gap mirroring the shipped GitHub coverage (P3, qa-tester 2026-07-06); the remaining `pnpm audit --prod` moderates from audit pass 13 Risk 4, split into an `@nestjs/core` v10→v11 major upgrade as its own ticket (P3, M — a breaking-change review, not a drop-in override) and a `file-type` v16→v21 + `js-yaml`/`qs` dependency-refresh bundle (P3, S); two remaining audit pass 13 "Later"-list items not yet queued — documenting `GITLAB_TOKEN_ENCRYPTION_KEY` in `.env.example` (P3, S, Risk 5) and logging the swallowed post-commit catch in `bulkUpdateAtomic` (P3, S, Risk 6); and six `docs/UI-REVIEW.md` 2026-07-06-dated P2/P3 findings the frontend P1 batch didn't close — `ui/Badge` semantic-variant consolidation (P2), `Badge`'s custom-color dark-mode blind spot (P2), the `text-ink-300` live-status-error contrast failure (P2), inconsistent GitHub/GitLab "Development" section naming (P2), a starter-gadget first-run explainer (P3), and the staleness-pill's mouse-hover-only tooltip (P3). Updated the Groom-state note (pruned Pass-13's prose, fully preserved above in this Changelog) and this entry. **Per explicit task scope, only `docs/BACKLOG.md` was touched this pass** — `docs/ROADMAP.md` already carries same-commit Phase 3 entries for every one of tonight's shipments (verified via read, not edited); its "Current focus" narrative reconciliation and `docs/VISION.md` are a concurrent doc-syncer's territory tonight, not duplicated here.
- 2026-07-03 (Pass 13 groom) — Reconciled `git log` since Pass 12 against the board: verified as already-and-correctly ticked (no duplicates created) — GitLab v1 (`cf50d59`), NLQL person/sprint name resolution (`ec9f02e`), the agent-context web panel (`e605c44`), the epic-swimlanes parent-loading fix (`503b48d`), and Agent Experience Round 2 (`f79268f`, spot-checked against `git show --stat` — all 7 acceptance criteria match the shipped diff). Confirmed the docs-site refresh (`5e109fb`) is already ticked and that the curator's screenshot-gallery/README passes (`52c5ee3`, `94f95a1`) correctly have **no** discrete backlog entry — `docs-site/**` upkeep became standing `oss-curator` territory in `7317e82`, so routine chapter/screenshot maintenance is role duty, not a queued deliverable. **Refilled the Ready queue** (was correctly reported empty of un-shipped P1s after AX Round 2): kept the two already-filed, ROADMAP-sequenced P1s at the top unchanged (PR-status + auto-transition-on-merge; Configurable dashboards Phase 2), added a cross-reference from the PR-status item to a new SSRF-hardening note (below) since it's the first slice to make a real outbound `GithubClient`/`GitlabClient` call, then filed three new P2 items mined from `docs/MCP-QA.md` pass 1's still-open findings: NLQL unresolved-name diagnostics (finding 1's deliberately-deferred "should this 400" half), an epic-swimlanes e2e coverage gap (fix shipped, regression-guard test didn't), and cross-project issue MOVE (finding 4, filed as decision-needed — needs a founder/product call before it's buildable). Kept Gitea v1 and dashboard-sharing/public-embed queued below, unchanged, as the lower-leverage extensions. Filed two new P3s in Later (P3): the SSRF-hardening standing note referenced above, and the MCP session-start token-cost finding (~97 tools ≈ 73+ KB of schemas, MCP-QA pass 1 finding 8 — explicitly non-blocking per the QA report). `docs/ROADMAP.md` § Current focus verified already current (reconciled in the same commit as AX Round 2 by its own author) — no edit needed this pass.
- 2026-07-03 — Agent Experience Round 2 batch (P1, M): 7 acceptance criteria from a founder-relayed field report, see the ticked "Already Done" entry above for full per-criterion detail (cross-project write validation fix on `create()` — the confirmed-live P1 — plus a second real gap found in bulk label attach; idempotency keys via a new additive `IdempotencyRecord` table; atomic all-or-nothing bulk updates with bulk `parentId`; `dryRun` preview; comment edit/delete author-or-admin gating + MCP tools; a new unified `GET /projects/:id/activity` feed + MCP `list_project_activity`; `expectedProjectKey` MUST-pass language + optional strict-mode env var; folded-in staleness/list_users-q/create_project+create_workspace items from MCP-QA pass 1). 44 new API unit tests (1683→1727), 13 new MCP tests (84→97), 1 new tenant-isolation row (103/103 BLOCKED), live-verified end-to-end against the running API.
- 2026-07-03 — GitLab integration v1 — two-way link (P1, M): reconciled a prior in-flight build attempt's uncommitted work (schema/backend/web/e2e all found complete and correct — kept verbatim, reapplied on top of the intervening agent-context commit) and finished the remaining slice (migration renumbering + validation, MCP tool + README, live round-trip verification against the running API, doc sync). Additive `GitlabIntegration`/`IssueGitlabLink` parallel table pair (mirrors `GithubIntegration`/`IssueGithubLink` shape, GitLab semantics — self-hosted `gitlabBaseUrl`, `namespace/project` paths, AES-256-GCM-encrypted PAT), migration `20260703060000_add_gitlab_integration` (renumbered past the same-day `20260703050000_add_project_agent_context`; verified byte-for-byte against `schema.prisma` via `prisma migrate diff` — zero drift on the new tables). `GitlabModule` — ADMIN-gated `PUT/GET/DELETE /projects/:projectId/gitlab`, public `POST /gitlab/webhook/:projectId` verifying the literal `X-Gitlab-Token` header via `crypto.timingSafeEqual` (GitLab doesn't HMAC-sign, unlike GitHub), `Push Hook`/`Merge Request Hook` event parsing via a newly-shared `common/issue-key.util.ts` (extracted from GitHub's own util, byte-for-byte behavior-preserving); `GET /issues/:issueId/gitlab-links`; two new PAT scopes (`gitlab:read`/`gitlab:write`), every route scope-gated. Frontend: `GitlabSection.tsx` settings card + `GitlabLinksSection.tsx` issue-drawer Development sub-section, rendered beside the GitHub equivalents. 1665 API unit tests green (5 new GitLab spec files); tenant-isolation matrix 102/102 endpoints BLOCKED (4 new GitLab rows); `gitlab-integration.spec.ts` 10/10 e2e green desktop+mobile; `github-integration.spec.ts`/`issue-detail.spec.ts`/`issue-drawer-overlay.spec.ts`/`settings-robustness.spec.ts` regression 40/40 green. Live round-trip verified against the running API (fresh user/workspace/project/issue, not a mock): valid MR webhook → linked (`linksUpserted:1`); tampered/missing token → 401; replay → idempotent (same row id, no duplicate); PAT scope gating → 403 without `gitlab:read`, 200 with it. MCP: `list_issue_gitlab_links` (92 tools total, was 91) following the compact/paged envelope, live-verified end-to-end via a scripted tool invocation against the running API including the 403 scope-denial path; also fixed a pre-existing duplicated `update_project_context` row in `apps/mcp/README.md`'s tool table (MCP-QA pass 1, finding 9). Gitea (the other half of the original "GitLab/Gitea" gap) filed as a new Next(P2) item — different signature scheme (HMAC like GitHub, not GitLab's shared-secret token) makes it closer to a `GithubModule` mirror than a `GitlabModule` one. [ROADMAP Phase 9; VISION.md § Better-than-Jira gaps #3]
- 2026-07-02 — Per-project role override — schema + backend (Phase 1 of 2), P1/M: additive `ProjectMembership(projectId, userId, role)` override table (sparse, no backfill) resolved through a new `getEffectiveProjectRole` helper in `apps/api/src/common/membership.util.ts`; `assertProjectRole` — already the single chokepoint every project-scoped service calls — now resolves the caller's effective role through it, so every one of those call sites gets override support with a one-function change (workspace ADMINs always retain full access; a `ProjectMembership` row alone never grants access without workspace membership). New `ProjectMembershipsModule` (`GET`/`PUT`/`DELETE /projects/:id/members[/:userId/role]`, effective-project-ADMIN gated, audit-logged). 22 new unit tests — 1575 API tests green; 3 new tenant-isolation-matrix rows (96 total, all BLOCKED). MCP: `list_project_role_overrides`/`set_project_role_override`/`remove_project_role_override` (88 tools, up from 85). Live end-to-end verified (elevate/restrict/admin-immune/revert). Frontend UI (Phase 2 of 2) remains open in Ready. See the ticked Ready-queue entry above for full detail. [product-auditor Pass 9/10/11/12 carryover; VISION.md § Better-than-Jira gaps #2]
- 2026-07-02 (Pass 12 groom) — Reconciled both Pass-12 audits + `docs/VISION.md`'s re-scored Better-than-Jira scorecard (**4 better / 3 parity / 3 behind**, up from 3/2/5) against `git log` and the live working tree, per the vision-steward's explicit Ready-queue ordering note in `docs/ROADMAP.md` § Current focus. **Fixed the stale scorecard tally** at the top of this file (was "3 better, 2 parity, 5 behind") and rewrote the "Better-than-Jira gaps" note to point at the three remaining Behind rows only (Mobile, Admin controls, Integrations), dropping the four now-closed Pass-11 gaps. **Verified and ticked as shipped** the engineering P1 fix batch already self-documented by that build agent's own same-commit doc update (CSP `script-src` blocking the dark-mode bootstrap → moved to `theme-init.js`; Dashboards' zero realtime coverage → `dashboard.updated` + family invalidation; the third-consecutive-pass tenant-isolation matrix gap → 72→94 rows; `resolveEnforcedWorkflowId` N+1 in bulk updates) — no duplicate items created. **Left the mobile Swimlanes v2 paint-regression fix marked "IN FLIGHT — do not double-pull"** at the top of Ready: code for the portalled `<DropdownPanel>` fix, the chip-row overflow fix, and the label-wrap fix is present in the working tree but not yet committed/QA'd by that agent, so the item stays open with acceptance criteria for whoever finishes it rather than being duplicated or prematurely ticked. **Rebuilt the Ready queue** in the exact order the vision-steward specified: (1) mobile paint regression [in flight], (2) CSP/dashboards-realtime fix batch [in flight, largely complete], (3) admin depth — split into an in-app SSO/OIDC config screen (P1, M) and per-project role override sliced into schema+backend (P1, M) / frontend UI (P2, M), (4) integrations depth — GitLab/Gitea two-way link v1 (P1, M) and PR-status + auto-transition-on-merge + board-card badge (P1, M), (5) dashboards Phase 2 — cross-sprint trend + cross-workspace scoping + gadget caps (P1, M) plus a separate dashboard-sharing/public-embed item (P2, M). Updated the "Parity gaps" subsection to Pass-12 numbers: Dashboards/gadgets and Workspace-switcher scale both closed 5/5; Cross-surface coherence improved 2/5→4/5; Permissions granularity (3/5) and Integrations (4/5) carried forward with refreshed evidence; Mobile board toolbar flagged 2/5 — regressed. **Removed 3 now-redundant Next(P2) items** superseded by the new Ready-queue entries (old flat "Per-project role override," "Cross-sprint reporting trend view," and "Configurable dashboards — Phase 2" write-ups; the old "GitHub integration — Phase 9 follow-up slice" catch-all was trimmed to just its still-unclaimed smart-commit-syntax slice, the rest promoted to Ready). **Added 11 new P2/P3 items** mined from both Pass-12 audits' backlog-groomer ingest lists, deduped against existing entries: sidebar auto-collapse at ≤1024px, the "does viewing a workspace page silently switch your default" product decision, an HTTP-level integration test for the GitHub webhook receiver, a realtime-coverage inventory for Board/Workflow/CustomFieldDefinition/Component/Version (now also covering GitHub), smart-commit syntax, `X-GitHub-Delivery` logging on signature failures, a swimlane WIP/health summary strip, unifying the mobile theme-control location, and a `color-mix()` browser-floor note. **Pruned** ~90 lines of superseded pre-Pass-11 grooming prose from the file header (Pass 5/6-era narrative, fully preserved in this Changelog already) to keep the board from growing unbounded, per the standing "prune or merge" instruction. Per explicit task scope, `docs/VISION.md` and `docs/ROADMAP.md` were **not** touched this pass — both were freshly updated by the vision-steward immediately prior and treated as source of truth.
- 2026-07-02 (Pass-12 engineering fix batch) — Closed four findings from `docs/AUDIT-ENGINEERING.md` Pass 12: **(P1-1) CSP blocked the dark-mode bootstrap in the Docker artifact** — moved the inline theme-init `<script>` to a self-hosted `public/theme-init.js` loaded via a blocking `<script src>`, satisfying `script-src 'self'` outright; hardened `scripts/smoke-web-csp.sh` with a mode-3 inline-script-vs-script-src guard and added `apps/web/e2e/csp-artifact.spec.ts` (serves the real `dist/` bundle with the production CSP header, asserts zero `script-src` violations + dark mode applies). **(P1-2) Dashboards had zero realtime coverage** — `SocketEvents.DashboardUpdated` added, emitted from every `DashboardsService` CRUD mutation; `DashboardsPage` now subscribes via `useBoardRealtime`; new `invalidateDashboardDataFamily()` invalidates all open dashboards' gadget data on any project issue event; new e2e case proves a STAT gadget updates live from an API-created issue with zero `page.reload()`. **(P2-3) Tenant-isolation matrix gap** — open for three consecutive audit passes — closed: personal-cards, quick-links, workspace PATCH+logo POST, GitHub integration (config + issue links), and dashboards/gadgets (94 rows total, up from ~72; all confirmed BLOCKED). **(P2-1) `resolveEnforcedWorkflowId` N+1 in `bulkUpdate`** — new `buildBulkWorkflowResolution()` precomputes the WF-1 named-workflow resolution once per batch (O(1) board query + O(1) sprint query, not O(issues)), threaded via a new `MutationOpts.resolvedWorkflowId`; 2 new unit tests assert bounded call counts. Gates: full API unit suite (1521 tests) green, integration suite (94-row matrix, 7 tests) green, `tsc --noEmit` clean both apps, web build clean, e2e green (`theme.spec.ts`, extended `dashboards.spec.ts`, `board.spec.ts`, `csp-artifact.spec.ts` — desktop + mobile). See the four ticked Ready-queue/Next(P2) entries above for full per-item detail. [engineering-auditor Pass-12 P1-1/P1-2/P2-1/P2-3]
- 2026-07-02 — Navigation & IA overhaul — Phase 1: persistent left sidebar framework (P1, L): App.tsx-level `AppShellFrame` renders a persistent desktop sidebar (`lg`+, collapsible icon rail, `localStorage`-persisted) + below-`lg` overlay drawer as siblings of `<Routes>` — one mount for the session. Workspace section reuses the header `WorkspaceChip`'s extracted `WorkspaceSwitcherMenuContent` (no duplicated state); projects list scoped to the active workspace with `aria-current` + a signature cobalt rail-tick on the active row; personal links (My Work/My Board/Insights/Notifications); workspace-settings utility link. `AppHeader`'s duplicate personal links hidden at `lg`+; `gotoSection` e2e helper made viewport-generic. New `nav-sidebar.spec.ts` (12 tests) + a sidebar case in `workspace-switcher.spec.ts`; full e2e suite green desktop+mobile; `tsc --noEmit` + build clean. See the ticked Ready-queue entry above for full detail. Phase 2 (surface Board/Backlog/Roadmap/Reports + workspace-admin/Branding + clickable filter indicator as first-class sidebar links) is next. [founder-flagged 2026-07-02; VISION.md § Better-than-Jira scorecard "Cross-cutting finding"]
- 2026-07-02 — Settings robustness pass — fix batch (P1, M): fixed all 4 confirmed defects from the `qa-tester` sweep in `docs/UI-REVIEW.md` ("Settings robustness sweep — 2026-07-02"). **SETTINGS-1 (P1, admin lockout):** `workspaces.service.ts#addMember` no longer upserts an existing member's role (was: a solo admin re-typing their own email into the Invite form silently demoted them, zero recovery path) — inviting an already-member email now returns a friendly 409; role changes moved to new `PATCH /workspaces/:id/members/:membershipId`; that endpoint and `removeMember` both enforce a last-admin invariant (400, friendly), covering self-demotion/self-removal by the sole admin while a non-last-admin demote/remove still works. **SETTINGS-2 (P2):** branding hex input normalizes CSS 3-digit shorthand to 6-digit client-side before submit (`WorkspaceBrandingPage.tsx#normalizeHex`) instead of round-tripping a raw server 400. **SETTINGS-3 (P2):** `statuses.service` gained a case-insensitive same-project duplicate-column-name guard (409, friendly), mirroring Labels/Components/Versions; service-level only, no DB `@@unique` (existing installs may already have dupes). **SETTINGS-4 (P2):** `labels.service` now catches Prisma P2002 on create/update and throws `A label named "X" already exists in this project` instead of the generic fallback. P3 polish: GitHub `repoFullName`, custom-field type-name, and branding-hex validation messages reworded to user voice. All 4 `test.fixme()` tests in `apps/web/e2e/settings-robustness.spec.ts` un-fixme'd, 20/20 green desktop+mobile; 23 new API unit tests; 1461 API unit tests green; `tsc --noEmit` clean both apps. [founder directive 2026-07-02]
- 2026-07-02 — GitHub integration kickoff — v1 two-way link (Phase 9 start, P1, M): per-project `GithubIntegration` (repoFullName + AES-256-GCM-encrypted PAT + generated HMAC webhook secret) + `IssueGithubLink` (PR/COMMIT/BRANCH, unique `[issueId, kind, externalId]`); `GithubModule` — ADMIN-gated `PUT/GET/DELETE /projects/:projectId/github`, public `POST /github/webhook/:projectId` verifying `X-Hub-Signature-256` against the raw request body (`main.ts` `rawBody: true`) before processing `push`/`pull_request` events, project-key-scoped issue extraction, idempotent link upserts, `issue.updated` realtime reuse; `GET /issues/:issueId/github-links`; `GithubClient` single injectable outbound seam (unused in v1, mocked in tests, zero github.com egress). Frontend: Settings "GitHub" section (ADMIN form + webhook URL/secret + instructions; MEMBER read-only summary) + issue-drawer "Development" section (PR/commit/branch rows, state badges). Two new PAT scopes. 63 new unit tests (1440 total API tests green); real-DB tenant-isolation suite green; 4 new Playwright e2e scenarios × desktop+mobile (locally HMAC-signed fake webhook, zero GitHub egress); `tsc --noEmit` clean both apps.
- 2026-07-02 (Pass 11 groom) — Reconciled both Pass-11 audits + `docs/VISION.md`'s new "Better-than-Jira scorecard" against `git log`. **Ticked/annotated as already-shipped** (not re-queued): workspace-chip sync on all 15 pages + dead `DashboardPage.tsx` + `WorkspaceBrandingPage` side-effect removal (already present, unchanged); newly added ticked entries for the **cross-page state-coherence QA gate + workspace-selector correctness fix** (P1, M — `c8bf9c8`/`12c7d5a`, was in ROADMAP but missing from BACKLOG), the **Blocked-badge-clears-on-resolve fix** (P2, S — eng-audit Pass-11 P2-3), and the **`qk.boardView` cache-family invalidation helper** (`invalidateBoardFamily`, P2, S — eng-audit Pass-11 P2-2/Ideation #2, commit `892ec30`). **Rebuilt the Ready queue** (7 new P1/P2 items) from the five switcher-impact-ordered "Better-than-Jira gaps" in the note above: route-derived `<WorkspaceScopedLayout>` structural fix (P1, M), `project.updated` realtime event + coverage inventory (P1, S), QuickLinksMenu/workspace e2e coverage — flagged 2 consecutive passes (P1, M), SSO/OIDC Phase 1 (P1, M), GitHub integration kickoff v1 (P1, M), configurable dashboards Phase 1 (P1, M), mobile breadcrumb truncation fix (P2, S). Added a **"Parity gaps"** subsection to Ready mirroring the VISION scorecard (kept visible until each row closes, per CLAUDE.md). **Priority tension resolved:** the structural tenant-context layout — product-auditor rated it P2/M, engineering-auditor's own ideation rated the identical fix P1/M — elevated to P1 given the founder's explicit "top item" framing in ROADMAP § Current focus and the bug class's second recurrence. Added a new **"Next (P2 — Better-than-Jira gap follow-ups)"** section (8 items: workspace-switcher search/recent, tenant-isolation matrix rows, per-project role override, SSO Phase 2, cross-sprint reporting trend, configurable-dashboard Phase 2, scheduled automation trigger, project-creation template) and 7 new P3 carry-forwards (ReportsPage stale-sprint-id, `updateColumn` order-write, board-health nudge, inline card context menu, public status page, recently-visited switcher, UI-REVIEW design-elevation carry-forward). Per explicit task scope, `docs/ROADMAP.md` was **not** touched this pass — it was already reconciled to the current phase/"Current focus" state in the immediately-preceding commit (`892ec30`).
- 2026-07-02 — Workspace chip sync completed for the remaining seven pages (P1, M): `useSyncActiveWorkspace` wired into `ReportsPage`/`RoadmapPage`/`ProjectAnalyticsPage` (via `boardQuery.data?.project.workspaceId`) and `WorkspaceMembersPage`/`WorkspaceAuditLogPage`/`WorkspaceSettingsPage`/`WorkspaceBrandingPage` (via the route's `useParams` workspaceId), closing the gap both Pass-11 audits flagged independently. Removed the `setActiveWorkspaceId` side effect from `WorkspaceBrandingPage`'s save/reset handlers (superseded by the on-load sync). Deleted dead `DashboardPage.tsx`. `workspace-switcher.spec.ts` extended with a deep-link-to-workspace-settings case; 17/17 green desktop + mobile; `tsc --noEmit` clean.
- 2026-06-30 — Engineering-audit Pass-9 hardening (7 items): (P1) WebSocket CORS — `@WebSocketGateway({ cors: true })` replaced with module-level `_wsCorsOption` that mirrors `main.ts` `CORS_ORIGINS` parsing (split by comma, trim, filter empty, credentials: true); open fallback preserved for local dev. (P1) `WorkflowTransition.toStatusId` index — `@@index([projectId, toStatusId])` added to schema.prisma + hand-authored migration `20260630000000_workflow_transition_to_status_index`; `prisma validate` clean. (P2) `exportCsv` 10k row cap — `IssuesService.CSV_ROW_CAP = 10_000`; `take: CAP + 1` truncation detection; `X-Next-Lane-Truncated: true` response header on the controller when truncated; service returns `{ csv, projectKey, truncated }`. (P2) Password MinLength consistency — `RegisterDto.password` raised from MinLength(6) → MinLength(8), matching ResetPasswordDto. (P2) WorkLogsService membership refactor — `resolveWorkspaceId()` private method removed; `update()`/`remove()` now use `assertProjectMember` from `common/membership.util` (same semantics, less code). (P2) AUTO_SEED default flip — `docker-entrypoint.sh` default `${AUTO_SEED:-false}` (was `:-true`); `docker-compose.yml` api env sets `AUTO_SEED: "true"` explicitly so local/demo stack is unaffected; production deployments no longer auto-seed. (P2) Tenant-isolation matrix >65 rows — 19 new matrix rows across work-logs/standups/issue-templates/personal-boards/planning-poker; 4 new provisioned resources (`workLogId`, `pokerSessionId`, `personalColumnId`, `issueTemplateId`); sanity threshold >65; 1306 unit tests green; build clean.
- 2026-06-29 — Workflows MCP surface / Phase 4 (P1, M): new standalone `@next-lane/mcp` package (`apps/mcp/`) — a Model Context Protocol server over stdio using the official `@modelcontextprotocol/sdk` (1.29.0) + `zod` (3.25.x). Lets external agents (Claude Desktop, Claude Code) read AND write a project's workflows/SDLC + core entities via the Next Lane REST API, authenticated with a PAT (`NEXT_LANE_TOKEN`, `Authorization: Bearer`; `NEXT_LANE_API_URL` default `http://localhost:4000`; fails fast on missing token). 18 tools — read: `list_workspaces`, `list_projects`, `list_boards`, `list_statuses`, `list_workflows`, `get_workflow`, `list_issues`, `get_issue`; write: `create_workflow`, `create_workflow_from_template`, `update_workflow`, `delete_workflow`, `add_workflow_transition`, `update_workflow_transition`, `delete_workflow_transition`, `assign_board_workflow`, `create_issue`, `move_issue`. Centralized `client.ts` fetch wrapper (base URL + bearer + JSON, surfaces API error message + HTTP status); thin zod-validated handlers; `bin` = `next-lane-mcp` (shebang). 18 vitest unit tests (mocked fetch — missing-token fast-fail, URL/header/method/body construction, NestJS error surfacing); `tsc` build clean; stdio `tools/list` smoke verified (18 tools). README with Claude Desktop (`mcpServers` JSON) + Claude Code (`claude mcp add`) config. Additive — no schema/backend changes. [founder request 2026-06-28]
- 2026-06-29 — Per-board named workflows backend (P1, L): Named `Workflow` entity CRUD (`GET/POST /projects/:projectId/workflows`, `GET/PATCH/DELETE /workflows/:id`) with `transitionCount`/`boardCount` rollups; workflow-scoped transition CRUD (`POST /workflows/:id/transitions`, `PATCH/DELETE /workflow-transitions/:id`) on non-colliding paths vs legacy endpoints; seed-from-template for `simple`/`kanban`/`scrum`/`bug-triage` with graceful degradation for missing statuses; board workflow assignment in `UpdateBoardDto`/`BoardService.updateBoard` (same-project validation, `connect`/`disconnect` Prisma relation syntax, `toBoardSummaryDto` returns `workflowId`); `boardId?` added to `MoveIssueDto`; 3-branch `enforceMove` in `IssuesService.move` (board with enforced named workflow → `enforceTransitionForWorkflow`; board without / non-enforced → project-level `enforceTransition`; no board → project-level; automation bypass preserved); `WorkflowService.enforceTransitionForWorkflow` scoped to workflow entity transitions; 3 new spec files (47 tests); 1085 total; `tsc --noEmit` clean. Satisfies founder "workflows editable per board" request.
- 2026-06-28 — Checklists backend (P2, M): `ChecklistModule` (`apps/api/src/checklist/`); `ChecklistItem` Prisma model + migration `20260628130000_add_checklist_items` (table verified live — count/create/read/delete round-trip); 5 REST endpoints (`POST /issues/:issueId/checklist`, `PATCH /checklist/:itemId`, `DELETE /checklist/:itemId`, `PUT /issues/:issueId/checklist/reorder`, `GET /issues/:issueId/checklist`); order = max+1 on create; tenant isolation via item→issue→project lookup; `toChecklistItemDto` mapper; `checklistItems` added to `listInclude` in `IssuesService` (orderBy order asc); `IssueDto.checklist` + `IssueDto.checklistProgress: { done, total }` added to `@next-lane/shared/types` + issue mapper; `ChecklistItemDto`/`CreateChecklistItemDto`/`UpdateChecklistItemDto` in shared package; `ChecklistModule` wired into `AppModule`; 40 new unit tests; 958 total green; tsc clean.
- 2026-06-28 — Engineering Pass-8 hardening: (P1) logo upload magic-byte validation in `workspaces.service.ts` via `assertLogoMagicBytes()` (file-type@16; reject on content/declared-type mismatch or non-allowlisted format; 4 unit tests); (P1) tenant-isolation matrix extended to >45 rows (workflow REST, automations CRUD+runs, analytics, CSV export, workspace logo DELETE); (P2) automations `create`/`update`/`delete` elevated from MEMBER→ADMIN role (aligned with webhooks; 5 new tests); (P2) `validateActionParamsDeep()` DB-lookup guards for TRANSITION `statusId` and ADD_LABEL `labelId` cross-project validity (6 new tests); (P2) `AnalyticsQueryDto` replaces raw `Number(daysStr)` in analytics controller (`@IsInt @Min(1) @Max(366)`; 9 DTO tests); (P2) `bulkUpdate` workflow pre-load: 1 `isEnforcementEnabled` call per batch, hint propagated via `MutationOpts.workflowEnforced` to `enforceTransition` (4 new tests); (P2) workload aggregation in `projectAnalytics` replaced with SQL `GROUP BY "assigneeId"` via `$queryRaw` (eliminates O(N) JS materialisation). 790 unit tests green; `pnpm --filter @next-lane/api build` clean.
- 2026-06-28 — CSV export of project issues (P1, M): `GET /projects/:projectId/issues.csv?q=<NLQL>` streaming `text/csv; charset=utf-8` with `Content-Disposition: attachment; filename="<projectKey>-issues.csv"`; 13-column header (Key/Title/Type/Status/Priority/Assignee/Reporter/Story Points/Sprint/Labels/Due Date/Created/Updated); RFC-4180 `csvCell()` helper (commas/quotes/newlines quoted, internal quotes doubled, leading `=`/`+`/`-`/`@` prefixed with `'` to prevent formula injection); auth: any project member (VIEWER+) via `assertProjectMember`; optional `q` NLQL filter validated with `validateQuery` (400 on invalid) and applied with `filterIssues` (same evaluator as the board); rows ordered by issue number ascending; labels joined by `; `; assignee/reporter: name with email fallback; `IssuesCsvController` registered in `IssuesModule`; `csv.util.ts` + `issues-csv.controller.ts` + `exportCsv` on `IssuesService`; 39 new unit tests (all green): `csvCell` escaping (commas/quotes/newlines/formula injection), header row, data rows with full relations, NLQL filter, invalid NLQL → 400, non-member → 403; build clean; 749 unit tests total. ✅ shipped 2026-06-28 [roadmap Phase 5, data-ownership]
- 2026-06-28 — ProjectNav mobile-UX redesign (P2, S): 10-tab horizontal-scroll replaced with primary-tabs-always-visible + More dropdown + right-pinned Settings. Primary: Board/Backlog/Triage/Reports. More menu (role=menu/menuitem, aria-haspopup/expanded/controls, Escape closes + returns focus to trigger, outside-click closes via `pointerdown`, first-item auto-focus on open): Analytics/Roadmap/Poker/Standup/Automation. Settings: gear icon + text hidden `sm:inline`, right-pinned with cobalt active state. More button: cobalt-signal active treatment when collapsed route is live; label becomes "More · <ActiveLabel>". Active item in menu: `bg-signal-50` fill + cobalt dot indicator. `data-testid="nav-automation"` preserved; all 10 `to` paths + exact labels preserved; `aria-label="Project navigation"` kept. `useLocation` (safe, no hook-in-map) for active-collapsed detection. `prefers-reduced-motion` via `motion-reduce:transition-none` + `motion-safe:animate-nl-fade-in`. Build + tsc clean. [ui-review 2026-06-28]
- 2026-06-28 — UI token-cohesion fix pass (P2, S): analytics/reports `slate-*`→`ink-*`; SVG chart chromes on `ink-*` tokens; `CategoryBars` colorFn class strings; `AutomationsPage` `shadow-card`; `ActionParamsEditor` DATE/CHECKBOX; `AddCardComposer` `focus-visible`; `PromoteCardModal` `ui/Select`; `ProjectNav` `aria-label`. Build clean; no testid/role/text changes. [ui-review 2026-06-28]
- 2026-06-28 — Personal & team analytics backend (P2, M): `AnalyticsModule` (`apps/api/src/analytics/`): `GET /me/analytics?days=N` → `PersonalAnalyticsDto` (open/completed/overdue counts, per-day throughput FlowPointDto[], avg cycle time, byType/byPriority CategoryCountDto[], personal board stats); `GET /projects/:projectId/analytics?days=N` → `ProjectAnalyticsDto` (flow FlowPointDto[], createdTotal/completedTotal, avg cycle time, 5-bucket CycleTimeBucketDto[] with en-dash labels, WorkloadRowDto[] busiest-first + Unassigned row); ActivityLog completion-date reconstruction identical to reports.service; days default 30 clamped [1, 366]; 25 unit tests green; build clean; registered in AppModule.
- 2026-06-28 — Async standups backend (P2, M): `StandupsModule` (`apps/api/src/standups/`) with 4 endpoints (digest, me, upsert, prefill); `toStandupEntryDto` mapper; atomic transaction for blocker-link replacement; prefill from ActivityLog + in-progress issues; 25 unit tests (548 total); `tsc --noEmit` clean; registered in `AppModule`.
- 2026-06-28 — SavedFilter backend + NLQL query validation (P3, L): `apps/api/src/saved-filters/` module — `GET/POST /projects/:projectId/saved-filters`, `PATCH/DELETE /saved-filters/:id`; `validateQuery` (custom-field-aware) rejects invalid queries with 400; list returns own + shared filters (OR clause), ordered name asc; owner-only patch/delete (403 for non-owner); `toSavedFilterDto` mapper (isShared→shared, createdAt/updatedAt→ISO); board `updateBoard` validates `filterQuery` and `colorRules[].query` before persisting; 27 new unit tests in `saved-filters.service.spec.ts` + `board.service.spec.ts`; `tsc --noEmit` clean; `SavedFiltersModule` registered in `AppModule`; 441 unit tests total.
- 2026-06-27 — Multiple boards per project (backend, P2, M): `Board` CRUD endpoints (`GET/POST /projects/:id/boards`, `GET/PATCH/DELETE /boards/:id`); KANBAN/SCRUM issue-scope split; `toBoardSummaryDto` with colorRules coercion; lazy-create default board fallback on `GET /projects/:id/board`; delete guards (isDefault, only-board); 27 unit tests green; Prisma client regenerated from updated schema.

- 2026-06-27 — UI design elevation: "Slate + Teal-Shift" design system foundation (P2, L): deliberate token system; deep teal accent (#0891b2, brand-600) replacing generic indigo; Plus Jakarta Sans Variable for UI + IBM Plex Mono for issue keys/story points (self-hosted via @fontsource); stone-400/amber-500/emerald-500 status-progression arc; revised shadow/radius/animation scales; all UI primitives (Button, Input, Select, Textarea, Field, Badge, Avatar, Modal, Toast, ConfirmDialog) + AppHeader, BoardColumn, IssueCard, CardStatusPicker, IssueDetailDrawer, AuthShell elevated; nl-issue-key and nl-data-chip CSS signature classes; drawer/modal entrance animations with prefers-reduced-motion guard; WCAG-AA contrast maintained; all data-testid/ARIA preserved; 24/24 representative e2e green. Resolves "generic SaaS indigo template" look from previous audits.
- 2026-06-27 — Live board presence indicators (P2, M): per-project in-memory presence map in gateway; `presence.update` socket event; `PresenceAvatars` component + `usePresence` hook; self-excluded from viewer list; 7 unit + 6 e2e (desktop + mobile) green; 276 unit tests total.
- 2026-06-27 — SMTP email delivery for password reset (P1, S): `MailModule`/`MailService` (nodemailer); real SMTP when `SMTP_HOST` set; dev-log fallback when absent; production-safe; SMTP_* env vars documented in `.env.example`; 255 unit tests green.
- 2026-06-27 (Pass 5 groom) — Security hardening cluster + product P1s + deferrals captured.
  - **In-flight (current build batch, mark done when confirmed):** plaintext token log guard (P1, S); SVG-XSS from ALLOWED_MIME_TYPES (P1, S); CFD/burndown unbounded queries rewrite to generate_series (P1, M); null-file upload guard (P2, S); webhook secret out of BullMQ job body (P2, S); PAT expiresAt past-date validation (P2, S); nginx CSP header (P2, S); Helm Postgres fail-fast guard (P2, S). All confirmed as in-flight by engineering-auditor Pass 5; keeping as open items until build agent ticks them.
  - **Added to Ready (product P1s):** SMTP email delivery wiring for password reset (P1, S); WATCHED_UPDATED notification emission (P1, S); due date on issues (P1, M). These are the three highest-trust product gaps per product-auditor Pass 5.
  - **Moved to Next (P2, genuinely open):** magic-byte MIME validation (P2, M); PAT at WS handshake (P2, S); PAT scopes + @RequireScope (P2, M); webhook signing key rotation (P2, S); DB-level time-series report types (P2, M); attachment admin-delete UX (P2, S — was P2 in Pass 5 ingest); markdown rendering (P2, M); batch notifyComment + rebalanceAndPlace (carry-forward); slim planning-view endpoint (carry-forward); public share link (carry-forward); live presence indicators (carry-forward).
  - **Captured known deferrals:** audit log e2e spec (P2, S); removeMember UI (P2, S). These are gaps confirmed as "exists at API, not yet surfaced in UI/test" — important to track, not yet blocking.
  - **Ticked done [x]:** attachments (P3, M — shipped 2026-06-27); label rename (P3, S — shipped 2026-06-27); Team Pulse (P3, M); keyboard triage (P3, L); PATs (P3, M); workspace audit log (P3, M); pino structured logging (P3, M — partial, enriched /health + OTel remain); GHCR image CI (P3, M); e2e in CI (P2, M); board/roadmap result caps (P2, S); assertNoParentCycle CTE (P2, M); Redis adapter + BullMQ queue (P2, M+S); Helm/Kustomize/DEPLOY-KUBERNETES.md (P3, L+M+S).
  - **Pruned:** removed stale "Attachments (uploads volume)" P3 placeholder — fully shipped. Removed "Password reset back-end P2 duplicate" — superseded by shipped P1 item.
  - **ROADMAP reconciliation:** Phase 2 fully done (CFD ticked); Phase 3 in progress; Phase 4 Kubernetes packaging 🚧 (Helm/Kustomize/GHCR CI all shipped; observability hooks remain). "Current focus" updated in ROADMAP to reflect the post-Pass-5 security hardening sprint.
- 2026-06-27 — CI/CD pipelines (P3, M + P2, M): GHCR image publish + Playwright e2e in CI.
- 2026-06-27 — Kubernetes packaging (P3, L+M+S×4): shipped the Phase 4 deploy story under `deploy/`.
- 2026-06-27 — Web runtime config (P3, S): `VITE_API_URL` no longer baked into the web bundle at build time.
- 2026-06-27 — Workspace audit log (P3, M).
- 2026-06-27 — Personal API tokens / PATs (P3, M).
- 2026-06-27 — Team Pulse dashboard (P3, M).
- 2026-06-27 — Keyboard triage mode (P3, L).
- 2026-06-27 — File attachments (P3, M).
- 2026-06-27 — Label rename/edit (P3, S).
- 2026-06-27 — CFD report (Phase 2 remaining).
- 2026-06-27 — BullMQ + Socket.io Redis adapter (P2, M+S).
- 2026-06-27 — nestjs-pino structured logging (P3, M — partial).
- 2026-06-27 — UX/a11y polish pass (P1, S — board toolbar overflow-x-auto, MentionComposer no-results, password min-length alignment, aria-haspopup, OnboardingPanel SVG icons, MyWorkPage EmptyState, autoFocus auth forms).
- 2026-06-27 — assertNoParentCycle recursive CTE (P2, M).
- 2026-06-27 — Board/roadmap result caps 500 (P2, S).
- 2026-06-27 — e2e test isolation hygiene (P2, S).
- 2026-06-27 — Board type + priority filters (P1, S).
- 2026-06-27 — @mention autocomplete / MentionComposer (P1, M).
- 2026-06-27 — Password reset end-to-end (P1, M).
- 2026-06-27 (Pass 4 groom) — Ingested Pass 4 audits; promoted board filters, @mention, password reset to P1 Ready; promoted Redis queue/adapter to P2.
- 2026-06-27 — Security + scale hardening: SSRF guard, pagination index, helmet(), throttler.
- 2026-06-27 — First-run onboarding (P3, S — v1.0 criterion ticked).
- 2026-06-26 (Pass 3 groom) — Ingested Pass 3 audits.
- 2026-06-26 (Pass 2 groom) — Ingested Pass 2 audits.
- 2026-06-26 (Pass 1 groom) — Initial seeding from Pass 1 audits + UI review.
- (initial) Seeded from UI review + roadmap.
