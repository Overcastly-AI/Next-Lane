# Next Lane — Dev Board (Backlog)

Single prioritized board maintained by the **backlog-groomer**, fed by the two independent auditors (`docs/AUDIT-PRODUCT.md`, `docs/AUDIT-ENGINEERING.md`), the front-end QA review (`docs/UI-REVIEW.md`), and the roadmap (`docs/ROADMAP.md`). The autonomous build loop pulls from **Ready (top of queue)**.

Format: `- [ ] (P1, M) title — description [src]` · P0 critical / P1 now / P2 next / P3 later · size S/M/L. Checked `[x]` = done.

> Grooming note (2026-06-27, Pass 4): Pass 4 closes the security hardening sprint (SSRF
> guard, pagination index, helmet, throttler) and the first-run onboarding item. Product
> auditor Pass 4 finds the product is now a credible full-cycle agile tracker and pivots
> the remaining gaps to: board type/priority filters (S, high daily-driver value),
> @mention autocomplete (M, the #mention feature is hidden without it), and password
> reset (M, table-stakes for self-hosted multi-user). Engineering auditor Pass 4
> confirms SSRF and pagination index are shipped and now focuses on: getBoard()/
> getRoadmap() still unbounded (P2, S); useProjectIssues serial pagination waterfall
> (P2, M); Redis-backed webhook delivery queue / BullMQ (P2, M); JWT→httpOnly cookie +
> CSP (P2, L); assertNoParentCycle recursive CTE (P2, M); structured logging/pino +
> OTel (P3, M). QA findings: three specs used DEMO user for board-columns /
> viewer-aware / webhooks-ui — these should use isolated users (P2, S, test hygiene);
> VITE_API_URL is baked at build-time (P3, S, documentation note).
>
> Priority tension — Pass 4: Engineering auditor flags Redis-backed webhook queue and
> JWT cookie migration as P1-class given open-source release posture; product auditor
> rates daily-driver UX (type/priority filter, @mention autocomplete, password reset)
> and first-run onboarding as the highest remaining conversion levers. Resolution: board
> filters, @mention autocomplete, and password reset stay P1 (they hit every user on
> day one); Redis queue is promoted to P2-High (already in compose, concrete path, but
> not user-visible); JWT cookie migration stays P2-L (no active XSS vector today; CSP
> header via helmet mitigates the risk adequately for now). Onboarding is ticked done
> (OnboardingPanel + 14 e2e tests shipped 2026-06-27).

---

## Ready (top of queue)

- [x] (P0, M) Authenticate realtime gateway + membership-check subscribe — any client could join any projectId room and receive all issue/comment events; cross-tenant leak. Auth the handshake (JWT) and assertProjectMember before join [engineering-auditor]
- [x] (P0, M) Validate tenant ownership of statusId/sprintId/parentId/beforeId/afterId on issue update & move — members could attach issues to another project's status/sprint/parent and corrupt foreign boards [engineering-auditor]
- [x] (P0, M) Enforce roles (VIEWER read-only; ADMIN-only member mgmt & deletes) — Role enum was stored but never checked; any member could self-upsert ADMIN via addMember [engineering-auditor, product-auditor]
- [x] (P0, S) Fail fast on missing JWT_SECRET + remove bypass paths (docker-compose default + RealtimeModule fallback) — app could boot with a globally-known signing key when env was unset; compose injected the known string silently [engineering-auditor]
- [x] (P1, S) CORS origin allowlist from env — dropped origin:true + credentials:true; defaults to the web app URL [engineering-auditor]
- [x] (P1, S) Fix label-chip contrast in issue drawer — reuse Badge instead of inline raw-color chips so light labels stay legible [ui-review]
- [x] (P1, S) Drawer as first-class overlay — scroll-lock, focus trap + restore, z-index aligned with Modal via shared hook [ui-review]
- [x] (P1, M) Replace native window.prompt/window.confirm (new workspace, delete issue) with themed modals [ui-review]
- [x] (P1, M) Add a lightweight toast system; surface drawer-patch + mutation errors consistently [ui-review, product-auditor]
- [x] (P0, S) Validate assigneeId is a workspace member on issue create/update — any authenticated user ID can be set as assignee cross-tenant; assertWorkspaceMember utility already exists, just needs to be applied in IssuesService.create/update [engineering-auditor]
- [x] (P0, S) Scope GET /users to the caller's co-members — current endpoint returns all users' names + emails to any authenticated user across all tenants; add workspaceId filter to UsersService.findAll [engineering-auditor]
- [x] (P1, M) API unit tests for membership.util + assertSameProject + GitHub Actions CI — zero unit tests; test script is still a stub; isolation fixes are Playwright-only (slow, DB-dependent); a Jest suite + Actions workflow makes the security model self-maintaining [engineering-auditor]
- [x] (P1, S) Comment edit-in-place + delete in CommentsPanel — backend PATCH/DELETE /comments/:id exist; UI has no edit or delete affordance; visible gap that reduces user trust [product-auditor]
- [x] (P1, S) Activity log: resolve status/user IDs to human names — log currently shows raw DB IDs in from/to fields; drawer already has statuses and users in scope; <10-line fix [product-auditor]
- [x] (P1, L) Backlog + sprint planning view — `/projects/:id/backlog` page grouping issues into PLANNED/ACTIVE sprint sections + a Backlog section, with a "Move to" menu, create-sprint modal, Start (single-active enforced server-side), and Complete (incomplete issues return to backlog). Board/Backlog sub-nav added [product-auditor, roadmap]
- [x] (P1, M) Labels assign/unassign + filter UI — create/edit/delete labels, assign on card and in drawer, filter board by label(s); useLabels hook is written and backend CRUD + assign/unassign endpoints are live [product-auditor, roadmap]
- [x] (P1, M) Story points field + parent/child picker in issue drawer — expose storyPoints number field and a parentId picker for epic/story/subtask hierarchy; schema and API fully support it [product-auditor, roadmap]
- [x] (P1, S) Sprint TOCTOU race: move assertNoOtherActiveSprint inside $transaction + add partial unique index — `assertNoOtherActiveSprint` runs a `findFirst` OUTSIDE the `$transaction` block; two concurrent "start sprint" requests both pass the guard and both write `state: ACTIVE`, violating the one-active-sprint invariant; no DB-level partial unique index to enforce it. Fix: (a) move guard inside tx using `tx` client, (b) add migration `CREATE UNIQUE INDEX sprint_one_active_per_project ON "Sprint"("projectId") WHERE state = 'ACTIVE'` [engineering-auditor]
- [x] (P1, S) Sprint lifecycle: emit sprint.updated realtime event on start/complete — board viewers in other tabs see stale data until page reload; no push notification emitted when a sprint starts or completes; inject RealtimeService into SprintsService and emit `sprint.updated` on lifecycle transitions [engineering-auditor]
- [x] (P1, M) Global exception filter: map Prisma errors to structured HTTP responses — unhandled P2002/P2025/P2003 + rankBetween edge cases surface as raw NestJS 500s with stack traces in responses; add @Catch() filter with consistent { statusCode, message, error } envelope; suppress stack traces in production [engineering-auditor]
- [x] (P1, S) Input bounds: @MaxLength on description, @Min/@Max on storyPoints, hex validation on label color — description is unbounded (MB payloads accepted); storyPoints accepts negative/astronomic values; label color accepts any 20-char string and can produce corrupt UI renders [engineering-auditor]
- [x] (P1, S) VIEWER-aware UI: hide/disable edit affordances based on role — role enforced at API; UI still renders Delete button and all field-edit controls for VIEWERs who then get a silent or confusing 403 on click [product-auditor, engineering-auditor] — added `useMyRole(workspaceId)` (derives role from the existing `/workspaces/:id/members`, no new endpoint) + a `canEdit` helper; VIEWERs no longer see create-issue/add-column/create-sprint, drag-and-drop is disabled, the issue drawer hides Delete and disables all field edits, and label/parent/comment edit affordances are gone, with a subtle "View only" hint on board + backlog + drawer. ADMIN/MEMBER unchanged. e2e covers VIEWER vs ADMIN on board + backlog (desktop + mobile)
- [x] (P1, M) Manage board columns (statuses) from the board UI — "+ Add column" affordance at the end of the column row opens a name + category (To Do / In Progress / Done) form (optimistic insert + toast, server assigns next order); per-column overflow menu with Rename/edit, Move left/right (PATCH order swap), and a themed delete ConfirmDialog that surfaces a clear "Move or delete its issues first" toast on the 400 when the column still has issues; backend status CRUD already existed (API blocks VIEWER), errors surfaced gracefully. e2e covers add → rename → create issue in column → blocked delete → delete empty column on desktop + mobile [user-reported]
- [x] (P1, M) Project Settings page — new `/projects/:id/settings` tab in ProjectNav (Board / Backlog / Reports / Settings) that owns project configuration. Column (status) management moved OFF the board into a clean "Columns" section (list with up/down reorder, edit, delete + "+ Add column" form reusing ColumnFormModal; delete-in-use guard toast preserved); the board's column ⋮ menu and "+ Add column" button were removed (board keeps per-column "+ add issue"). Adds a Project details section (editable name/description, read-only immutable key) via a new `useUpdateProject` hook, a Labels section (create with color swatch + delete, reusing labels API), and an ADMIN-only Danger zone (Archive project via `useArchiveProject` + ConfirmDialog). Whole page is ADMIN/MEMBER editable and VIEWER read-only (reuses useMyRole/canEdit); destructive actions (archive, delete column/label) gated to ADMIN. board-columns e2e rewritten to drive column CRUD through Settings (add → appears on board → rename → blocked delete → delete empty), plus edit-project-name and VIEWER-read-only specs; full suite green on desktop + mobile [user-reported]
- [x] (P1, S) Webhook SSRF guard (DNS pre-flight + redirect:manual + socket drain + p-limit fan-out cap) — DNS pre-flight resolves hostname to all IPs and rejects any in blocked ranges (loopback/link-local/private/unique-local); `redirect:'manual'` prevents post-check redirect to internal hosts; response body drained to avoid socket leak; fan-out capped at 10 concurrent deliveries via p-limit@3; opt-out via `WEBHOOK_ALLOW_PRIVATE=true` for self-hosters; unit tests assert 127.0.0.1 + 169.254.169.254 are blocked, public hosts pass, env flag bypasses guard [engineering-auditor, security]
- [x] (P1, S) Composite index for cursor pagination — added `@@index([projectId, createdAt, id])` to Issue model; migration `20260627012032_issue_pagination_index` generated and applied; covers the keyset WHERE+ORDER BY clause so large project issue lists don't full-scan [engineering-auditor]
- [x] (P1, S) helmet() + @nestjs/throttler (100/min global, 10/min auth) — helmet() adds CSP/HSTS/X-Frame/X-XSS-Protection and 9 other security headers on every response; ThrottlerModule enforces 100 req/min per IP globally, 10 req/min on auth routes (POST /auth/login, /auth/register); both env-configurable (RATE_LIMIT_TTL, RATE_LIMIT_MAX, AUTH_RATE_LIMIT_MAX); RATE_LIMIT_DISABLED=true for e2e/dev environments [engineering-auditor]
- [x] (P3, S) First-run onboarding + optional sample project + 4-step tour — empty board is a weak first impression [product-auditor] — shipped `OnboardingPanel` (welcome heading, product description, feature highlights grid, primary "Create your first project" CTA) on the project-list empty state; improved empty states on board columns (VIEWER hint), My Work (combined banner when both sections empty), and notifications bell (descriptive icon + copy); `apps/web/e2e/onboarding.spec.ts` covers all scenarios desktop + mobile (14 tests green). v1.0 criterion ticked.

- [x] (P1, S) Board type + priority filters — add Type and Priority multi-select filter controls in the board toolbar alongside the existing title search, assignee select, and label filter; client-side like the label filter; most common triage filters that board is missing today [product-auditor Pass 4] — shipped 2026-06-27: `MultiSelectFilter` generic component + `TypeFilter` + `PriorityFilter` in BoardPage.tsx; 18 Playwright tests (desktop + mobile) all green
- [x] (P1, M) @mention autocomplete in comment composer — when user types `@` in the comment textarea, pop a member-picker dropdown (co-members already fetched via useUsers) and insert `@email` on selection; notification fan-out on mention already works end-to-end; without autocomplete the feature is effectively hidden [product-auditor Pass 4] — shipped `MentionComposer` (floating listbox, Arrow/Enter/Tab/Escape nav, `stopImmediatePropagation` on Esc so the drawer doesn't close, `onMouseDown` click to keep textarea focused); `CommentsPanel` now takes `users` prop and uses it in both new-comment and edit-comment composers; `IssueDetailDrawer` passes `users` down; 16 e2e tests green (desktop + mobile) covering Enter insert, Tab insert, Esc dismiss, arrow nav, click insert, no picker on normal typing, notification fan-out end-to-end, focus retention per-keystroke. tsc clean, build clean.
- [x] (P1, M) Password reset (POST /auth/forgot-password + time-limited token + SMTP/dev-log delivery) — no account recovery path today; self-hosted teams get locked out; table-stakes auth for multi-user self-hosted product [product-auditor Pass 4] — shipped: `PasswordResetToken` Prisma model (SHA-256 hash stored, raw token never persisted; 1h TTL; prior tokens invalidated), migration `20260627021052_add_password_reset_token`, POST /auth/forgot-password (always 200, anti-enumeration), POST /auth/reset-password (400 on invalid/expired/used), pluggable delivery seam (dev-log fallback + SMTP_HOST stub, documented in .env.example), "Forgot password?" link on login page, /forgot-password and /reset-password FE pages reusing AuthShell + existing components, 9 unit tests (all green), 12 e2e tests desktop+mobile (all green), 141 unit tests total green, 175 e2e tests total green.
- [x] (P3, M) File attachments (uploads) — issue-scoped file uploads stored on local disk under `UPLOADS_DIR` (K8s-ready PVC path) [roadmap Phase 2/3] — shipped 2026-06-27: `Attachment` Prisma model + migration `20260627145511_add_attachment_model`; `AttachmentsModule` with `POST /issues/:id/attachments` (multipart, multer disk storage to OS temp then moved to UPLOADS_DIR, 10 MB cap via `MAX_FILE_BYTES`, MIME allowlist: images/PDF/text/office-docs/zip), `GET /issues/:id/attachments` (metadata list), `GET /attachments/:id` (stream with Content-Type + Content-Disposition + auth guard), `DELETE /attachments/:id` (uploader or project ADMIN only); VIEWER cannot upload or delete; `AttachmentDto` in shared types; drag-drop + click-to-browse `AttachmentsPanel` in `IssueDetailDrawer` with loading/error/empty states, per-row download (fetch+Blob→object URL) and delete (ConfirmDialog); 16 unit tests + 5 e2e tests desktop+mobile all green; 208 unit tests total; `UPLOADS_DIR`/`MAX_FILE_BYTES` documented in `.env.example`.

---

## Next (P1 — high value, queue as Ready empties)

- [x] (P1, M) Sprint burndown + velocity reports page — `/projects/:id/reports` with active-sprint burndown chart (story points remaining over calendar days), velocity bar chart (completed points per sprint); all data now in DB (Sprint.startDate/endDate, Issue.storyPoints, StatusCategory.DONE, ActivityLog timestamps); most glaring product gap post-agile-core [product-auditor]
  - [x] API: read-only `ReportsModule` — `GET /projects/:id/reports/velocity` (committed vs completed points per active/completed sprint) and `GET /projects/:id/sprints/:sprintId/burndown` (daily ideal vs actual-remaining derived from ActivityLog status→DONE transitions); membership-authorized; unit-tested with mocked Prisma; seed now sets story points + completion transitions
  - [x] UI: Reports page + ProjectNav link with hand-rolled responsive SVG velocity bar chart and burndown line chart (sprint selector defaulting to the active sprint, empty/loading/error states); e2e covers desktop + mobile
- [x] (P1, S) Board sprint indicator + backlog sprint dates — board now shows an active-sprint badge in the header (name · active · relative end-date countdown that turns amber when near / red when overdue), and the backlog sprint sections render each sprint's start–end date range plus an end-date warning chip for the active sprint when it's near/overdue. Reuses a shared sprintDates helper; handles missing dates gracefully. e2e covers the board badge + backlog dates/warning on desktop + mobile. [product-auditor]
- [x] (P1, M) Cross-project global search — membership-scoped `GET /search` + `GET /projects/:id/search` (title/description/key contains) surfaced via the global command palette and header search button [product-auditor]
- [x] (P1, M) "My Work" personal dashboard — top-level `/my-work` route reachable from the header (next to Search): membership-scoped `GET /me/work` returns issues assigned to and reported by the caller across every workspace/project they belong to (key, title, type, priority, status, project key, sprint name/state); page renders "Assigned to me" + "Reported by me" sections with type icon, status pill, project key and priority, clicking a row opens its board with `?issue=`; empty/loading/error states; unit test asserts workspace+caller scoping (no cross-tenant leak); e2e proves a foreign tenant's issue never appears [product-auditor]
- [x] (P1, L) Notifications: @mentions, auto-watch on assign/comment, in-app inbox (+ optional SMTP) — ActivityLog and Watcher models exist and are unused; high user value; exercises realtime properly [engineering-auditor, product-auditor]
  - [x] Schema + API: `Notification` model (recipient, type enum ASSIGNED/MENTIONED/COMMENTED/WATCHED_UPDATED, snapshot issueKey/projectId/message, read flag, `(userId, read, createdAt)` index) + migration. `NotificationsModule` creates notifications membership-safe on assignment (notify + auto-watch assignee), comment (notify watchers except author + auto-watch commenter), and `@<email>` mentions (co-member-scoped resolution → MENTIONED + auto-watch). Endpoints `GET /notifications`, `GET /notifications/unread-count`, `POST /notifications/:id/read`, `POST /notifications/read-all` (owner-scoped). Realtime: socket joins its OWN `user:<id>` room (derived from JWT, never client-supplied) and gets a `notification.created` push. Unit tests cover self-suppression, assignment/comment/mention fan-out + auto-watch, mention co-member scoping, and read-ownership.
  - [x] UI: notification bell in AppHeader with an unread badge + dropdown panel (actor avatar, message, issue-key chip, type, relative time). Click opens the issue's board (`?issue=`) and marks it read; "Mark all read" clears the badge. Live via the `user:<id>` socket room + a 60s TanStack Query poll fallback. e2e (`notifications.spec.ts`) proves A→B assignment + @mention surface a 2-badge, panel rows, click-navigates-and-marks-read, and mark-all-read clears — desktop + mobile.
  - [ ] Follow-ups (not in this slice): optional SMTP/email delivery, per-user notification preferences, and a WATCHED_UPDATED notification on non-comment issue edits. (@mention autocomplete is now shipped — ticked in Ready above.)
- [ ] (P1, L) Tenant-isolation test harness + declarative authz layer — reusable two-workspace matrix asserting every mutating endpoint + socket room rejects cross-tenant access; @RequireRole/@ResourceScope decorator so isolation is structural, not hand-rolled per service [engineering-auditor]
- [x] (P1, S) Isolate e2e suite data into per-test projects (stop polluting the demo) — several Playwright specs created/renamed/deleted issues, columns, labels, comments against the SHARED seeded NL demo project, so repeated runs accumulated junk ("QA activity …" issues, "QA RENAMED" columns) and risked cross-test flakiness. Added reusable fixtures in `apps/web/e2e/helpers.ts` (`registerNewUser`, `createWorkspace`, `createProject`, `createLabel`, `openProjectBoard`, and a one-call `setupIsolatedProject(page, request)` that registers a fresh user + workspace + project with default statuses, optionally seeds labels, logs in, and lands on the new board). Refactored every mutating spec (board create, issue-detail, input-focus, issue-points-hierarchy, labels, toast, themed-dialogs, issue-drawer-overlay, issue-assignee-tenant, issue-tenant-ownership) to write into its OWN isolated project/user instead of NL; read-only specs (auth, board render, reports, my-work, realtime) still use the demo. Proven: NL stays at exactly 8 issues / 3 statuses / 3 labels before and after a full run. 114 Playwright tests green (desktop + mobile), tsc clean [test-infra]

---

## Next (P2)

- [x] (P2, S) GET /users/:id authorization — scope to co-members — any authenticated user can fetch any other user's name/email/avatar by CUID; the co-member guard from `findAll` should also apply to `findOne` [engineering-auditor]
- [x] (P2, M) Replace assertNoParentCycle sequential waterfall with single recursive CTE — N sequential `findUnique` calls per hop (up to 1000 round-trips for deep trees); runs outside the update transaction (TOCTOU on concurrent parent reassignment); replaced with a `WITH RECURSIVE` CTE via `$queryRaw` inside the `$transaction` (atomically with the UPDATE); 100-hop depth cap; 6 new unit tests (self-parent, direct cycle, deep cycle, valid deep hierarchy, empty-result, single-query assertion); 145 total unit tests green [engineering-auditor Pass 3, Pass 4]
- [x] (P2, M) Cursor pagination for issue list — `GET /issues` now keyset-paginates on `(createdAt,id)` returning `{ items, nextCursor }` (default 50, cap 200); opaque base64url cursor, malformed-cursor-safe [engineering-auditor]
- [x] (P2, M) Transactional move + rank-collision rebalance — neighbor read/update outside a transaction can collide under concurrent moves; wrap in $transaction + add rebalance fallback [engineering-auditor]
- [x] (P2, S) Fix Dockerfile --no-frozen-lockfile → --frozen-lockfile — non-reproducible image builds; pnpm may silently update dependencies in production image context [engineering-auditor]
- [x] (P2, M) Command palette (Cmd-K) navigation & actions — global keyboard-driven palette (Cmd-K/Ctrl-K + header button): grouped cross-project Issues/Projects search with ↑/↓/Enter/Esc, jump to issue (opens its board + drawer) or project, and current-project quick actions (Create issue, Go to Board/Backlog/Reports) [product-auditor]
- [x] (P2, S) Inline issue creation in backlog (ghost row, type-and-Enter) — shipped as `GhostRow` in BacklogPage (git `60c0107`); appears at bottom of each sprint/backlog section for MEMBER/ADMIN; type title + Enter creates TASK at medium priority in that sprint/backlog; VIEWERs never see it; confirmed by product-auditor Pass 4 [product-auditor]
- [x] (P2, M) Plugin/webhook event system (HMAC-signed outbound POST on issue.* + sprint.* events) — automation and CI/CD integration prerequisite; RealtimeService.emitToProject infrastructure already in place; one new module [engineering-auditor, product-auditor]
  - [x] Backend slice: `WebhookSubscription` + `WebhookDelivery` Prisma models; ADMIN-only project-scoped CRUD under `/projects/:projectId/webhooks` + `/deliveries` + `/test`; fire-and-forget signed delivery (X-NextLane-Signature: sha256=HMAC) wired into existing emitToProject points (issue create/update/move/delete, comment.created, sprint.started/completed); bounded delivery log; unit tests (signature, ADMIN scoping, delivery rows w/ mocked HTTP) + API e2e (signed delivery + delivery row + 403 for non-admin)
  - [x] Frontend slice: Webhooks section on Project Settings (list/add/edit/delete via themed modal + ConfirmDialog, active toggle, expandable recent-delivery log, Send test); ADMIN-only (hidden for VIEWER/MEMBER); TanStack Query hooks in apps/web/src/api/webhooks.ts; e2e (admin adds webhook → listed → Send test → delivery row; section hidden from non-admins) desktop+mobile
  - [x] SSRF hardening shipped (see Ready queue above)
- [ ] (P2, M) Full-text search + structured filters + saved views — move beyond `title ILIKE`; Postgres full-text search (GIN/tsvector) across title + description + comments; filter grammar (status/assignee/label/sprint/type/priority); persisted SavedView model; tracker unusable at scale without this [engineering-auditor, product-auditor]
- [x] (P2, M) Observability baseline (pino structured logs, requestId, /health enrichment, OTel Prisma traces) — self-hosted product needs operator visibility without SSH; global exception filter (P1 item) is a prerequisite (now shipped); add nestjs-pino + correlation requestId header + enriched /health (DB ping, uptime, version) + optional OTel export via OTEL_EXPORTER_OTLP_ENDPOINT [engineering-auditor Pass 4] — **nestjs-pino structured logging shipped 2026-06-27** (JSON in prod, pino-pretty in dev, secrets redacted, LOG_LEVEL env var, bufferLogs). Follow-ups remain: requestId correlation header, enriched /health with DB ping, OTel Prisma traces.
- [ ] (P2, M) Public read-only project share link — token-authenticated read-only URL shareable with stakeholders who don't need an account; ShareToken model + unauthenticated board endpoint + readonly board view (no DnD, no create); top self-hosted adoption lever [product-auditor]
- [ ] (P2, S) Fix stale socket token after re-auth — getSocket() captures the token once at init; when refresh tokens land, the socket will carry a stale credential until page reload; pass the current token as a parameter or reconnect on auth state change [engineering-auditor]
- [x] (P2, M) Password reset back-end (POST /auth/forgot-password + POST /auth/reset-password) — shipped as part of P1 item above [product-auditor, engineering-auditor]
- [ ] (P2, S) Inline card status transition (right-click / keyboard shortcut) — tiny context menu on the card showing the 2–4 statuses; eliminates drawer round-trip for status changes; power-user flow [product-auditor]
- [x] (P2, L) Roadmap / timeline view (epics + sprints as bars) — `/projects/:id/roadmap` tab: hand-rolled responsive SVG timeline (month axis, today marker) showing dated sprints (colored by state) and epic rows with derived windows (child sprint dates → createdAt fallback, "No dates" lane) + DONE-category progress fill; click an epic opens it on the board (?issue=). Read endpoint `GET /projects/:projectId/roadmap` (membership-authorized, unit-tested); VIEWERs can view; no schema change [product-auditor]
- [ ] (P2, M) Live board presence indicators (per-project viewer avatars via WebSocket) — gateway already tracks connections; augment handleSubscribe to maintain a per-project presence map and emit presence.update; zero new API routes [engineering-auditor]
- [ ] (P2, S) Board-overview prefetch endpoint + stale-while-revalidate caching — collapses 4 sequential requests (auth → workspaces → projects → board) into 1 on first load; immediate perceived performance improvement [engineering-auditor]
- [x] (P2, S) Add take cap to board and roadmap endpoints (take: 500 + hasMore flag) — BoardService.getBoard and RoadmapService fetch all matching issues with no upper bound; graceful degradation needed before large-project OOM occurs [engineering-auditor Pass 4] — shipped 2026-06-27: `BOARD_ISSUES_CAP=500` in board.service.ts (take CAP+1 trick, no extra COUNT), `ROADMAP_EPICS_CAP=500` in roadmap.service.ts; `issuesTruncated` added to `BoardDto`, `epicsTruncated` to `RoadmapDto` in packages/shared; amber hint badge in BoardPage and RoadmapPage when truncated; 9 new unit tests (4 board, 5 roadmap) + all 150 tests green.
- [x] (P2, S) useProjectIssues pagination waterfall — partial fix: pass `limit=200` (API MAX_ISSUES_PAGE_SIZE cap) on each page request so the all-pages walk uses 5x fewer round-trips (200 items/page vs 50); correctness preserved (walk continues until nextCursor is null). Full slim-endpoint elimination still tracked below. [engineering-auditor Pass 4] — shipped 2026-06-27
- [ ] (P2, M) Slim planning-view endpoint (or virtual scroll) to avoid all-pages waterfall — full elimination: add GET /projects/:id/issues/planning with a slim projection (id, title, type, priority, statusId, sprintId, rank — no description, no labels, no comment count); reduces even the max-page-size walk from O(N/200) to O(1) for most projects [engineering-auditor Pass 4]
- [x] (P2, M) Redis-backed webhook delivery queue with retries / BullMQ — `REDIS_URL`-gated: when set, dispatch enqueues one BullMQ job per matching subscription (3 attempts, exponential backoff delay 2s, concurrency 10); a Worker in the same process processes jobs calling the shared `executeDelivery()` function (SSRF guard + HMAC signing + socket drain + delivery row recording); when unset, existing in-process p-limit fan-out runs unchanged. `executeDelivery()` is the single source of truth for SSRF/HMAC logic shared by both paths. docker-compose.yml already had REDIS_URL + depends_on for the redis service. 11 new unit tests (producer enqueues correct job shape in Redis mode; Queue not created without REDIS_URL; SSRF blocks 127.0.0.1 and 169.254.169.254; HMAC signature correct; redirect:manual; WEBHOOK_ALLOW_PRIVATE bypass). Live integration: BullMQ delivers to localhost receiver → success row recorded; SSRF guard blocks private IP without flag → error row recorded; no-Redis restart → in-process p-limit path delivers unchanged. 170 unit tests total green. [engineering-auditor Pass 4]
- [x] (P2, S) Wire Socket.io Redis adapter — `REDIS_URL`-gated: when set, `RealtimeGateway.afterInit()` attaches `@socket.io/redis-adapter` with duplicated pub/sub ioredis clients so events broadcast across all replicas; when unset, default in-memory adapter used (log: "single-node mode"). `RedisModule` (global, @Optional tokens) provides pub/sub/main ioredis clients; no change to handshake-auth / membership-check logic. Log confirms "Socket.io Redis adapter attached (multi-replica mode)" with Redis, "in-memory adapter" without. docker-compose.yml `api` service already had `REDIS_URL: redis://redis:6379` + `depends_on: redis`. [engineering-auditor Pass 4]
- [ ] (P2, M) Batch notifyComment inserts (createMany) + rebalanceAndPlace (executeRaw batch UPDATE) — notifications.service.ts iterates watchers with one DB INSERT + one socket emit per watcher in series; issues.service.ts rebalanceAndPlace issues one UPDATE per row inside a transaction; both are O(N) loops [engineering-auditor Pass 4]
- [x] (P2, S) Fix remaining e2e specs using DEMO user for mutating flows — board-columns, viewer-aware, and webhooks-ui specs still write against the shared DEMO project instead of isolated per-test users; creates workspace accumulation and cross-test flakiness; migrate to setupIsolatedProject fixture [qa-review Pass 4] — shipped 2026-06-27: all three specs converted to use setupIsolatedProject + registerNewUser + addWorkspaceMember; DEMO workspace count confirmed static before/after a full run; 16 Playwright tests green (desktop + mobile)
- [ ] (P2, S) Theme tokens for issueMeta hardcoded hex; create-issue modal single-column on mobile (grid-cols-1 sm:grid-cols-2) [ui-review]
- [ ] (P2, S) Extract shared InlineError/FormError (4 duplicated banners) + drawer title aria-label + min 40px touch tap targets [ui-review]
- [x] (P1, S) UI-REVIEW 2026-06-27 polish pass — MentionComposer no-results state (picker stays open, "No members match" placeholder row); password min-length aligned to 8 across ResetPasswordPage + ResetPasswordDto (was 6); setTimeout auto-redirect removed from reset success screen; board toolbar mobile overflow fixed (overflow-x-auto filter-pill strip on small screens); aria-haspopup="dialog"→"menu" on LabelFilter + MultiSelectFilter triggers; MentionComposer shadow-lg→shadow-cardHover + top-full picker position; MyWorkPage full-empty block replaced with shared EmptyState; autoFocus on first input in Login/Register/ForgotPassword/ResetPassword. 20 board-filter e2e green (new mobile no-wrap test); 18 mention e2e green (new no-results test); 14 password-reset e2e green (new short-password guard + no-auto-redirect tests) [ui-review]
- [x] (P2/P3, S) UI-REVIEW 2026-06-27 perf + polish pass #2 — (P2) useProjectIssues `limit=200` reduces planning-view round-trips 5x (was limit=50 default → 10 pages for 500 issues; now 3 pages max); (P3) BoardColumn empty-button contrast raised to `text-sm text-gray-500` (was `text-xs text-gray-400`, below WCAG-AA for interactive text); (P3) OnboardingPanel feature-highlight icons replaced emoji (`⬛`/`🗓`/`📊`) with inline SVGs at `h-5 w-5 text-gray-400` matching app icon vocabulary (consistent stroke weight, no cross-OS rendering variation); (P3) MyWork per-section EmptyState now passes `action` prop — "Nothing assigned" / "You haven't reported" sections each have a "Go to board" `Button` next step. 16 e2e tests green (onboarding + my-work, desktop + mobile). [ui-review, engineering-auditor]

---

## Later (P3)

- [x] (P3, M) REST API tokens (PATs) — shipped 2026-06-27: `nlp_` prefix + 32 random bytes base64url; only SHA-256 hash stored (never raw value); `ApiToken` Prisma model + migration; `/me/tokens` (POST create, GET list, DELETE revoke); JWT guard extended to detect PAT prefix and validate via `ApiTokensService.validateRawToken()`; `lastUsedAt` bumped async (fire-and-forget); owner-only revoke (NotFoundException hides existence of other users' tokens); `CreateTokenModal` (raw token shown once with warning + copy button, `data-testid="pat-raw-token"`); `ApiTokensSection` with token rows (`data-testid="pat-token-row"`), Active/Expired/Revoked badges, revoke ConfirmDialog; Profile settings page (`/me/settings`); "Profile settings" entry in user avatar dropdown (`data-testid="user-menu-button"`); `ConfirmDialog` now uses `role="alertdialog"` for accessibility; 22 unit tests + 14 e2e tests (desktop + mobile) all green; tsc + build clean. [roadmap]
- [x] (P3, M) Workspace audit log — shipped 2026-06-27: `AuditEvent` Prisma model (id, workspaceId FK, actorId FK nullable, action, targetType, targetId, metadata Json, ip, createdAt; composite `@@index([workspaceId, createdAt])`); migration `20260627200000_add_audit_event_model`; `AuditModule` + `AuditService.record()` (best-effort fire-and-forget — never throws, logs on failure); events recorded on: membership add/remove/role-change (workspaces.service), project create/archive (projects.service), webhook create/update/delete (webhooks.service), API-token create/revoke (api-tokens.service, fan-out to all user workspaces); `GET /workspaces/:id/audit-log?cursor=&limit=` (ADMIN-only via `assertWorkspaceRole(ADMIN)`, cursor-paginated keyset on `(createdAt DESC, id DESC)`, resolves actor name, tenant-scoped); `ListAuditEventsQueryDto` with `@IsOptional`/`@IsInt`/`@Min`/`@Max` validation; `AuditEventDto` + `PaginatedAuditEventsDto` in `packages/shared`; `WorkspaceAuditLogPage` with table (time/actor/action/target/details/IP columns), `ACTION_LABELS` human-readable map, badge-colored target types, `relativeTime()`, load-more button, ADMIN-gated access (locked icon for non-admin), loading/empty/error states; `useAuditLog` infinite query hook; "Audit log" button in PulseDashboardPage header (ADMIN-only); route `/workspaces/:workspaceId/audit-log`; 11 unit tests (record creates, best-effort swallows errors, list ADMIN-only, MEMBER/VIEWER rejected, tenant scoping, actor mapping, null actor, nextCursor logic, malformed cursor safe degradation) + 225 total unit tests green; API + web builds clean. [roadmap]
- [x] (P3, S) Label rename / edit — users can't correct a label name typo without delete-and-recreate; obvious gap in label management [product-auditor] — shipped 2026-06-27: `PATCH /labels/:id` endpoint (name + color, MEMBER+ only; VIEWER → 403; cross-project → 403; hex-color validated; name 1–50 chars); `UpdateLabelDto`; `useUpdateLabel` mutation (invalidates labels list + board; patches open-issue cache); inline edit affordance per label row in Settings + LabelPicker popover (pencil icon, cancel on Escape, save on Enter); renamed label propagates to board cards + drawer chips on success. 6 new unit tests (rename, recolor, both, VIEWER rejected, cross-project rejected, not-found) + 12 Playwright e2e tests (rename updates board card, recolor, cancel, VIEWER no-edit-button, picker reflects rename — desktop + mobile). Total: 204 unit tests + 16 label e2e tests green.
- [x] (P3, S) VITE_API_URL is baked at build-time — shipped 2026-06-27: `getApiUrl()` helper reads `window.__NL_CONFIG__.apiUrl` → `VITE_API_URL` → default; `docker-entrypoint.sh` writes `/config.js` from `API_URL` env at container start; `index.html` loads it before the bundle; nginx no-cache for config.js. One web image, any environment, no rebuild. 6 Playwright e2e tests (desktop + mobile) green. [qa-review Pass 4, roadmap]
- [x] (P3, M) "Team pulse" home dashboard — `PulseDashboardPage` at `/`: sprint snapshot (active sprint per project with name, end-date countdown in board badge colors, done/total progress bar), "Assigned to me" quick list (top 5, links to drawer), recent activity feed (top 8 notifications, links to board), projects grid. All assembled client-side from existing hooks (useSprints, useBoard, useMyWork, useNotifications, useProjects) — zero new endpoints. First-run OnboardingPanel preserved when user has no projects. 20 e2e tests green (10 desktop + 10 mobile). [product-auditor Pass 4]
- [x] (P3, L) Keyboard triage mode — full-screen dedicated triage view from the command palette: issues listed one-per-row, press `a` to assign, `p` to set priority, `l` to add label, `s` to change status, `Enter` to open drawer, `j`/`k` to navigate, `f` to filter; no schema changes needed; power-user differentiator [product-auditor Pass 4] — shipped 2026-06-27: `/projects/:id/triage` route + `TriagePage` (reuses `useProjectIssues`, `useBoard`, `useUsers`, `useLabels`, `useMyRole`, `useUpdateIssue`, `useToggleIssueLabel`); full keyboard model with global `keydown` handler; inline pickers for status/priority/assign/labels; `ShortcutHelp` overlay; ARIA listbox + `aria-activedescendant`; VIEWER read-only (readonly-hint, no pickers); mobile tap-to-open rows + open button; "Triage issues" command in command palette (Cmd-K); "Triage" tab in ProjectNav; 12 Playwright e2e tests green (desktop + mobile); tsc + build clean.
- [ ] (P3, M) Per-project "Definition of Done" checklist — ADMIN configures a per-project checklist (JSON array on Project model) surfaced as a blocking prompt when an issue is moved to a DONE-category status; minimal schema additions; workflow guardrail self-hosted teams will configure to their process [product-auditor Pass 4]
- [ ] (P3, L) Automation rules engine (trigger → action: status/assignment/label) — flagship differentiator for self-hosted; depends on webhook system; ActivityLog is natural event source [product-auditor, roadmap]
- [ ] (P3, L) Saved/shareable views + query DSL (JQL-like filter grammar) — the saved-filter pattern teams live in; depends on full-text search + structured filters (P2) [product-auditor, engineering-auditor, roadmap]
- [ ] (P3, M) Custom fields (typed, JSONB-backed) [roadmap]
- [ ] (P3, M) Attachments (uploads volume) [roadmap]
- [ ] (P3, M) SMTP email notification delivery (opt-in per user; sendgrid/SMTP env config) — in-app only today; async notifications for users not logged in [product-auditor Pass 4]
- [ ] (P3, L) Time tracking / worklogs; CSV import + tracker importers; SSO/OIDC [roadmap]
- [x] (P3, M) Structured request logging (pino/nestjs-pino) + enriched /health + optional OTel export — self-hosted operators need operator-grade visibility; structured logs make webhook errors and slow queries diagnosable [engineering-auditor Pass 4] — **shipped 2026-06-27** (nestjs-pino; enriched /health + OTel remain as follow-ups)
- [ ] (P3, L) JWT migration to httpOnly cookie + POST /auth/refresh (short-lived access tokens) — token in localStorage is XSS-extractable; cookie migration + short-lived access tokens is the durable fix; helmet CSP mitigates the XSS vector adequately for now; revisit when a rich-text editor lands [engineering-auditor Pass 4]
- [x] (P3, S) Scrub trademarked category phrase from seed data — DONE: `seed.ts` now reads "issue & project tracker"; no trademarked terms remain [ui-review]

## Cloud-native / Kubernetes (post-v1 epic — ROADMAP Phase 4)

Make Next Lane deployable on Kubernetes with HA + autoscaling, keeping the
single-host Compose path for small installs. Sequenced so single-replica Helm
can ship first; multi-replica HA depends on the Redis items below.

- [x] (P3, S) Web runtime config for `VITE_API_URL` (+ public config) — shipped 2026-06-27 (two mechanisms): standalone image runs `docker-entrypoint.sh` to write `config.js` from `API_URL` (`getApiUrl()` reads `window.__NL_CONFIG__.apiUrl` → `VITE_API_URL` → default; 6 e2e green); Helm additionally defaults to `web.apiMode: same-origin` (nginx reverse-proxies `/api` + `/socket.io`). One web image works across environments without rebuilds. Closes the QA P3 "VITE_API_URL build-time" note. [roadmap, qa]
- [x] (P3, M) Socket.io Redis adapter — shipped 2026-06-27 (see P2 item above). [engineering-auditor]
- [x] (P3, M) Redis-backed webhook delivery queue (BullMQ) — shipped 2026-06-27 (see P2 item above). [engineering-auditor Pass 4]
- [x] (P3, M) Schema migrations as a Helm pre-upgrade Job — shipped 2026-06-27. `prisma migrate deploy` runs as a pre-install/pre-upgrade hook Job (gated before api rollout, exactly once); Kustomize ships the equivalent Job + documented `kubectl wait` ordering. [roadmap]
- [x] (P3, L) Helm chart `deploy/helm/next-lane` — shipped 2026-06-27. api/web Deployments, Services, toggleable cert-manager Ingress, ConfigMap + fail-fast Secret, HPA + PDB (both tiers), resource limits, liveness/readiness/startup probes on `/health`, non-root + readOnlyRootFilesystem securityContext, `_helpers.tpl`, NOTES.txt; values toggles for replicas/image/ingress/TLS/datastores (bundled Bitnami Postgres+Redis subcharts OR external/managed). Render-validated to valid YAML (default + external value sets); not yet `helm lint`'d/applied on a live cluster (sandbox egress blocks the helm binary). [roadmap]
- [x] (P3, S) K8s Secret strategy — shipped 2026-06-27. Chart-managed Secret for `JWT_SECRET` + DB/Redis creds OR `secrets.existingSecret` for external-secrets/sealed-secrets/SOPS; templating fails fast with a clear error when no secret is provided — never ships a default. [roadmap]
- [x] (P3, M) Kustomize base + overlays as a Helm alternative — shipped 2026-06-27. `deploy/kustomize/base` + `overlays/dev` (single-replica, plain HTTP, demo seed) + `overlays/prod` (3 replicas, cert-manager TLS, HPA + PDB, pinned tags). All manifests parse; `kubectl`/`kustomize` unavailable in sandbox so not built live. [roadmap]
- [x] (P3, S) `docs/DEPLOY-KUBERNETES.md` — shipped 2026-06-27. `helm install` quickstart, full values reference table, secret-management guidance, single-replica vs HA (`REDIS_URL`) section, HA topology overview, upgrade/migration runbook, Kustomize guide, image-build notes, troubleshooting. [roadmap]
- [x] (P3, M) Publish `api` + `web` container images to GHCR via CI — shipped 2026-06-27 (`.github/workflows/images.yml`): push to `main` + `v*` tags → multi-arch (amd64/arm64) builds to `ghcr.io/<owner>/next-lane-{api,web}` (owner derived + lowercased) with metadata-action semver + `latest` + sha/branch/edge tags, gha cache, SBOM (buildx attestation + anchore SPDX artifact), Trivy scan, `GITHUB_TOKEN` (`packages: write`); web built with empty `VITE_API_URL`. Static-validated only (YAML + actionlint clean); first GitHub run is the maintainer gate. The Helm chart/Kustomize image refs (`next-lane-{api,web}`) are populated by this. [roadmap]
- [x] (P2, M) Wire the Playwright e2e suite into CI — shipped 2026-06-27 (`.github/workflows/e2e.yml`): Postgres 16 + Redis 7 services, build shared/api/web, migrate + seed, start API (`RATE_LIMIT_DISABLED`/`WEBHOOK_ALLOW_PRIVATE`) + `vite preview` (with `VITE_API_URL` baked for preview), install chromium, run `pnpm --filter @next-lane/web exec playwright test` (desktop + mobile), upload HTML report on failure. Mirrors local harness env (`PW_BASE_URL`/`PW_API_URL`/`PW_NO_WEBSERVER`). Static-validated only (YAML + actionlint clean); first GitHub run is the maintainer gate. [hardening, roadmap]
- [ ] (P3, S) Optional metrics endpoint + `ServiceMonitor` + OTLP traces (builds on pino structured logging) for operator-grade observability in-cluster [engineering-auditor]

---

## Changelog
- 2026-06-27 — CI/CD pipelines (P3, M + P2, M): two new GitHub Actions workflows.
  - **`.github/workflows/images.yml`** — GHCR image publish. Matrix (api/web) build + push on `main` push and `v*` tags; QEMU + Buildx multi-arch (linux/amd64,linux/arm64); `docker/metadata-action` tags (semver `{{version}}` + `{{major}}.{{minor}}`, `sha-<short>`, branch, `edge` on main, `latest` only on tags); `docker/build-push-action@v6` with `provenance`+`sbom` attestations, gha layer cache (`scope` per app), and `build-args: VITE_API_URL=` (empty) for the web image. Owner namespace from `${{ github.repository_owner }}` lowercased → `ghcr.io/<owner>/next-lane-{api,web}`. Auth via `GITHUB_TOKEN` (`permissions: packages: write`). Plus `anchore/sbom-action` SPDX artifact and `aquasecurity/trivy-action` SARIF scan (report-only), both uploaded as artifacts.
  - **`.github/workflows/e2e.yml`** — Playwright e2e in CI (ci.yml still skips it). Postgres 16 + Redis 7 service containers (healthchecks); install deps, generate Prisma client, build shared/api/web (web with `VITE_API_URL=http://localhost:4000` since `vite preview` doesn't load `/config.js`); `prisma migrate deploy` + `db:seed`; start `node apps/api/dist/main.js` (env: `DATABASE_URL`, `REDIS_URL`, `JWT_SECRET`, `RATE_LIMIT_DISABLED=true`, `WEBHOOK_ALLOW_PRIVATE=true`) and wait on `/health`; start `vite preview` on :3000 and wait; `playwright install --with-deps chromium`; run `pnpm --filter @next-lane/web exec playwright test` (desktop + mobile). Harness env mirrors local: `PW_BASE_URL`, `PW_API_URL`, `PW_NO_WEBSERVER=1`. Server logs dumped + HTML report (`apps/web/playwright-report`) uploaded on failure.
  - **Verification**: all three workflows parse via PyYAML and pass `actionlint` v1.7.7 clean (shellcheck not installed locally, so shell-snippet linting skipped); referenced filters/scripts/paths confirmed (`@next-lane/web` filter + playwright present, `db:seed`/`build` scripts exist, Dockerfile paths + `VITE_API_URL` build-arg + `/health` route verified). **Not yet executed on GitHub** — the first real run (image push perms, multi-arch build, e2e flakiness) is the maintainer gate, alongside the docker-compose/cluster gates.
- 2026-06-27 — Kubernetes packaging (P3, L+M+S×4): shipped the Phase 4 deploy story under `deploy/`.
  - **Helm chart** `deploy/helm/next-lane`: `Chart.yaml` (optional Bitnami `postgresql`/`redis` deps), documented `values.yaml` + `values-prod.example.yaml`, `_helpers.tpl`, and templates — api/web Deployments, Services, toggleable cert-manager Ingress, api ConfigMap (+ same-origin nginx reverse-proxy ConfigMap), fail-fast Secret (no default; `existingSecret` for external-secrets), HPA (api+web), PDB (api+web), uploads PVC, ServiceAccount, `prisma migrate deploy` pre-install/pre-upgrade hook Job, NOTES.txt.
  - **Datastore toggles**: `postgresql.enabled`/`redis.enabled` for bundled quick-start, or `externalDatabase.*`/`externalRedis.*` for managed — DATABASE_URL/REDIS_URL derived in either mode.
  - **Web runtime-config gap closed**: `web.apiMode: same-origin` (default) proxies `/api` + `/socket.io` to the API Service so one image works everywhere; `external` mode documented.
  - **Security**: runAsNonRoot, readOnlyRootFilesystem (+ emptyDir for `/tmp`, nginx scratch, uploads), drop ALL caps, seccomp RuntimeDefault, `automountServiceAccountToken: false`.
  - **Kustomize** `deploy/kustomize`: base (single-replica, external datastores, placeholder Secret) + `overlays/dev` (1 replica, plain-HTTP ingress, demo seed, `:latest`) + `overlays/prod` (3 replicas, cert-manager TLS, HPA + PDB, pinned tags).
  - **Docs** `docs/DEPLOY-KUBERNETES.md`: quickstart, full values table, secret management, single-replica vs HA, HA topology, upgrade/migration runbook, Kustomize guide, troubleshooting.
  - **Verification**: authored a minimal Go-template renderer (sandbox blocks the `helm` binary) — every chart template renders to valid YAML for default + external value sets; caught & fixed a real `---`-glued-to-next-doc trim bug; all static manifests parse via PyYAML. NOT yet `helm lint`'d or applied to a live cluster.
- 2026-06-27 — Web runtime config (P3, S): `VITE_API_URL` no longer baked into the web bundle at build time. ONE built web image works in every deployment environment.
  - **`apps/web/src/api/config.ts`** (`getApiUrl()`): reads `window.__NL_CONFIG__?.apiUrl` → `import.meta.env.VITE_API_URL` → `'http://localhost:4000'`. Exported for socket.ts; `API_URL` in client.ts now calls this helper at module-init time.
  - **`apps/web/index.html`**: loads `/config.js` as the first script tag (before the bundle) using a plain `<script src>` with `onerror` handler so dev/preview works even when the file is absent.
  - **`apps/web/docker-entrypoint.sh`**: writes `/usr/share/nginx/html/config.js` from `API_URL` env var (falls back to `VITE_API_URL`, then `http://localhost:4000`) on every container start; `exec nginx -g "daemon off;"` replaces the shell process so signals propagate.
  - **`apps/web/nginx.conf`**: added `location = /config.js { expires off; Cache-Control: no-store; }` so a container restart always delivers the updated config.
  - **`apps/web/Dockerfile`**: `ENTRYPOINT ["/docker-entrypoint.sh"]` replaces `CMD ["nginx", …]`; `ARG/ENV VITE_API_URL=` kept (empty) for backward-compat with pipelines that still pass `--build-arg`.
  - **`apps/web/e2e/runtime-config.spec.ts`**: 3 new tests — (1) dev fallback boots and reaches dashboard; (2) `__NL_CONFIG__` readable after `addInitScript` injection; (3) all API fetches on authenticated route go to runtime-configured origin (intercepted via `page.route()`). 6 tests green (desktop + mobile).
  - **Dev path unchanged**: `pnpm dev` / `vite preview` — no config.js, fallback to `VITE_API_URL`. Backward-compat: passing `VITE_API_URL` still works (second fallback).
- 2026-06-27 — Workspace audit log shipped (P3, M).
  - **Schema:** `AuditEvent` Prisma model (id, workspaceId FK cascade-delete, actorId FK set-null, action string, targetType, targetId, metadata Json nullable, ip nullable, createdAt); `@@index([workspaceId, createdAt])`. Migration `20260627200000_add_audit_event_model` applied with `prisma migrate deploy`. Reverse relations on `Workspace` and `User`.
  - **Backend:** `AuditModule` provides and exports `AuditService`. `record()` is a best-effort fire-and-forget helper — never throws (catches and logs on error so the main operation always completes). Events recorded from: `WorkspacesService` (membership.add, membership.role_change, membership.remove), `ProjectsService` (project.create, project.archive), `WebhooksService` (webhook.create, webhook.update, webhook.delete), `ApiTokensService` (token.create, token.revoke — fan-out to all user workspaces via `recordForAllWorkspaces()` since PATs are user-scoped). All services pass `ip?` from controllers (`X-Forwarded-For` safe). `AuditController` under `GET /workspaces/:id/audit-log` (protected by `AuthGuard` + `assertWorkspaceRole(ADMIN)`; cursor-paginated keyset on `(createdAt DESC, id DESC)`; opaque base64url cursor; malformed cursor safely degrades to first page; resolves actor name/email).
  - **Shared types:** `AuditEventDto` (id, workspaceId, actor object or null, action, targetType, targetId, metadata, ip, createdAt ISO string) + `PaginatedAuditEventsDto` (items, nextCursor) added to `packages/shared/src/types.ts`.
  - **Frontend:** `useAuditLog` infinite query hook (`apps/web/src/api/audit.ts`). `WorkspaceAuditLogPage` at `/workspaces/:workspaceId/audit-log` — ADMIN-gated (shows locked-icon message for non-admins); paginated table with columns Time/Actor/Action/Target/Details/IP; `ACTION_LABELS` map for human-readable action names; `TARGET_COLORS` for badge-colored target types; `MetadataSnippet` for role/email detail; load-more button (`data-testid="audit-log-load-more"`), `data-testid="audit-log-page"` + `data-testid="audit-log-table"`. Route registered in `App.tsx`. "Audit log" button added to `PulseDashboardPage` workspace header (visible only to ADMIN).
  - **Tests:** 11 unit tests in `audit.service.spec.ts` covering all scenarios (record creates, best-effort swallows DB errors, list ADMIN-only, MEMBER/VIEWER rejected with ForbiddenException, tenant scoping, actor resolve, null actor, nextCursor null/set, malformed cursor graceful). 225 total unit tests green. API + web builds clean.
- 2026-06-27 — Personal API tokens (PATs) shipped (P3, M).
  - **Backend:** `ApiToken` Prisma model (id, userId FK, name, tokenHash UNIQUE, lastUsedAt, expiresAt, createdAt, revokedAt); migration `20260627100000_add_api_token`. `ApiTokensService` with `generateRawToken()` (`nlp_` + 32 random bytes base64url), `hashToken()` (SHA-256), `static isPat()`, `create()` (stores only hash; returns raw token once), `findAll()` (metadata only, newest-first), `revoke()` (owner-only soft-delete; NotFoundException on missing or wrong-owner to prevent existence leak), `validateRawToken()` (hash lookup + revoked/expired check + async `lastUsedAt` bump). `ApiTokensController` under `/me/tokens` (POST/GET/DELETE). `JwtAuthGuard` extended: `isPat()` branch short-circuits to `validateRawToken()` before falling through to normal JWT; only parses bearer header once. `ApiTokensModule` exported to `AuthModule` + imported in `AppModule`.
  - **Frontend:** `useApiTokens`, `useCreateApiToken`, `useRevokeApiToken` TanStack Query hooks in `apps/web/src/api/api-tokens.ts`. `CreateTokenModal` (2-phase: form → raw-token reveal with amber warning, copy button, metadata grid; `data-testid="pat-raw-token"`). `ApiTokensSection` (list with `data-testid="pat-token-row"`, Active/Expired/Revoked badge, revoke ConfirmDialog, empty/loading states). `ProfileSettingsPage` at `/me/settings`. `AppHeader` user dropdown "Profile settings" button with `data-testid="user-menu-button"`. `ConfirmDialog` now passes `role="alertdialog"` to `Modal`; `Modal` accepts optional `role` prop (default `'dialog'`).
  - **Tests:** 22 unit tests (generateRawToken, hashToken, isPat, create, findAll, revoke, validateRawToken — all scenarios); 14 Playwright e2e tests across `api-tokens.spec.ts` (7 desktop + 7 mobile) — create shows raw once, revoke via alertdialog, profile settings via user menu, PAT authenticates /api/auth/me, revoked PAT returns 401, list never exposes rawToken/tokenHash, cross-user revoke returns 404.
  - **Accessibility fix:** `ConfirmDialog` upgraded from `role="dialog"` to `role="alertdialog"` (correct ARIA role for destructive confirmations requiring immediate response).
  - **Shared types:** `ApiTokenDto` and `CreateApiTokenResponse` added to `packages/shared/src/types.ts`.
- 2026-06-27 — Team Pulse Dashboard (P3, M) shipped. `PulseDashboardPage` replaces `DashboardPage` as the home route (`/`). Four sections assembled client-side from existing hooks with zero new API endpoints: (1) Sprint snapshot — `SprintProjectRow` per project calls `useSprints` + `useBoard`, renders active sprint name, end-date countdown badge (reuses board's color logic: green/amber/red), done/total progress bar with ARIA progressbar; (2) "Assigned to me" — top 5 issues from `useMyWork().assigned`, status pill, click → `?issue=` drawer; "View all" link to `/my-work`; (3) Recent activity — top 8 notifications from `useNotifications`, actor avatar, issue-key badge, relative time, click → board drawer; (4) Projects grid — existing `ProjectCard` components. First-run: `OnboardingPanel` still shown when `projects.length === 0`. `DashboardPage` preserved; `App.tsx` now imports `PulseDashboardPage`. 20 Playwright e2e tests green (10 desktop + 10 mobile) in `apps/web/e2e/pulse-dashboard.spec.ts`. All 14 onboarding tests still pass. tsc clean, build clean.
- 2026-06-27 — Redis fan-out + BullMQ webhook queue (P2, M+S): wired `REDIS_URL`-gated Redis infrastructure as Phase 4 prerequisites for multi-replica HA.
  - **RedisModule** (`apps/api/src/redis/redis.module.ts`): global @Module providing three optional ioredis client tokens (`REDIS_PUB_CLIENT`, `REDIS_SUB_CLIENT`, `REDIS_CLIENT`); all `null` when `REDIS_URL` is unset.
  - **Socket.io Redis adapter**: `RealtimeGateway` now implements `OnGatewayInit`; `afterInit()` attaches `@socket.io/redis-adapter` with pub/sub clients when Redis is available, logs "multi-replica mode"; falls back to in-memory adapter with "single-node mode" log when unset. Handshake-auth and membership-check logic unchanged.
  - **BullMQ webhook delivery queue**: `WebhooksService` creates a `Queue<WebhookJobData>` + `Worker` when `REDIS_URL` is set; `dispatch()` enqueues one job per matching subscription instead of calling fetch inline; Worker processes jobs with 3 attempts + exponential backoff (2s base) + concurrency 10. When `REDIS_URL` is unset, the original `pLimit(10)` in-process fan-out runs unchanged.
  - **`executeDelivery()` shared function** (exported): single source of truth for SSRF guard (DNS pre-flight + `isBlockedIp` + `WEBHOOK_ALLOW_PRIVATE` bypass), HMAC signing (`signPayload`), `redirect:'manual'`, response body drain, retry loop, and result shape. Both the in-process path and the BullMQ Worker call this function — SSRF/HMAC cannot drift.
  - **Backward compatibility**: when `REDIS_URL` is unset the app starts and behaves exactly as before — no crash, no Redis required, zero-config Docker Compose / dev path unchanged.
  - **`.env.example`**: documented `REDIS_URL` as optional for dev, explains single-node vs multi-replica semantics.
  - **`docker-compose.yml`**: `api` service already had `REDIS_URL: redis://redis:6379` + `depends_on: redis` — verified correct, added comment.
  - **11 new unit tests** (producer enqueues correct shape; Queue not created without REDIS_URL; SSRF blocks 127.0.0.1/169.254.169.254; HMAC correct; redirect:manual enforced; WEBHOOK_ALLOW_PRIVATE bypass). 170 total unit tests green.
  - **Live integration verified**: Redis-on → BullMQ delivers to localhost receiver → success row; SSRF guard blocks 127.0.0.1 (no flag) → SSRF-blocked error row; Redis-off → in-process p-limit delivers unchanged. API log confirms "BullMQ / Redis mode" and "Socket.io Redis adapter attached" in Redis-on path; "in-process mode" and "in-memory adapter" in Redis-off path.
  - **Deps added**: `ioredis@^5.11.1`, `@socket.io/redis-adapter@^8.3.0`, `bullmq@^5.79.1`.
  - **Phase 4 prerequisites for multi-replica HA now met** (Socket.io adapter + BullMQ queue both gating items ticked).
- 2026-06-27 — Structured logging (P2/P3, M): `nestjs-pino` + `pino-http` wired into AppModule and `main.ts`. All Nest framework and application logger calls now emit structured JSON in production (`level`, `time`, `pid`, `hostname`, `context`, `msg` fields) and pino-pretty colorized output in development (`NODE_ENV !== 'production'`). Sensitive fields redacted from every request log line: `req.headers.authorization`, `req.headers.cookie`, `req.body.password`, `req.body.token`, `req.body.newPassword`. Health-check requests (`GET /health`) silenced to avoid log noise. Log level configurable via `LOG_LEVEL` env var (default `info`; documented in `.env.example`). `bufferLogs: true` + `app.useLogger(app.get(Logger))` routes all startup logs through pino. `console.log`/`console.error` in `main.ts` replaced with the pino Logger and `process.stderr.write`. `pino-pretty` added as devDependency only. Build clean (`pnpm --filter @next-lane/api build`). 154 unit tests pass. Verified: production JSON output, `"authorization":"[REDACTED]"` in request log, `/health` returns 200. P2 observability baseline item and P3 structured-logging item both ticked.
- 2026-06-27 — Cumulative Flow Diagram (CFD) report shipped (Phase 2 Remaining). `GET /projects/:id/reports/cfd?days=N` returns a daily series of issue counts per status category (TODO/IN_PROGRESS/DONE) reconstructed from ActivityLog status-change entries (exact historical replay; falls back to current status for issues with no log history). Window defaults to 30 days, clamped to 366 max. `CfdPointDto` + `CfdDto` types added to `packages/shared`. Frontend: `CumulativeFlowChart` component (hand-rolled responsive SVG stacked-area chart, 3 bands, polyline borders, day-axis labels, `<title>` tooltips, legend); `useCfd` query hook; CFD section added to `ReportsPage` with 14/30/90-day window selector, loading/error/empty states. 5 new unit tests (empty project, no-log fallback, todo→done transition, issue-not-yet-created exclusion, 366-day clamp); 159 unit tests total green. 6 Playwright e2e tests green (3 desktop + 3 mobile): chart renders, stacked paths present, window selector works, legend visible. `pnpm --filter @next-lane/api build` and `pnpm --filter @next-lane/web build` clean. ROADMAP Phase 2 cumulative-flow item ticked.
- 2026-06-27 — e2e test hygiene (P2, S): converted board-columns, viewer-aware-ui, and webhooks-ui specs from using the shared DEMO account as admin to the existing `setupIsolatedProject` + `registerNewUser` + `addWorkspaceMember` isolation pattern. Each test now registers its own fresh user + workspace so the demo account is untouched. VIEWER/ADMIN role setups are created within the isolated workspace. DEMO workspace count confirmed static (225) before and after a full run. 16 Playwright tests green (8 desktop + 8 mobile). Static imports used throughout; no dynamic `import()` patterns.

- 2026-06-27 — Board and roadmap result caps shipped (P2, S). `BoardService.getBoard` now uses `take: BOARD_ISSUES_CAP + 1` (cap = 500) on the Prisma issue query; the extra row detects truncation without a second COUNT. `RoadmapService.getRoadmap` applies the same pattern with `ROADMAP_EPICS_CAP = 500` on the epic fetch. `BoardDto.issuesTruncated` and `RoadmapDto.epicsTruncated` flags added to `packages/shared/src/types.ts` and built. Web UI shows an amber "Showing first 500 issues/epics" hint badge in the board toolbar and roadmap header when the flag is true. 9 new unit tests assert cap application, flag behavior, and ordering preservation; all 150 unit tests pass. tsc clean on api + web. v1.0 "performance sane at scale" criterion ticked.
- 2026-06-27 — assertNoParentCycle CTE refactor (P2, M) shipped. Replaced the serial N-hop `findUnique` loop with a single `WITH RECURSIVE` CTE executed via `prisma.$queryRaw` inside the `$transaction` that also performs the parent UPDATE (cycle check + write are now atomic; TOCTOU window closed). Depth capped at 100 hops defensively. Method renamed `assertNoParentCycleCTE`; takes the transaction client rather than `this.prisma`. The update method's `issue.update` + `activityLog.createMany` are now also inside that same transaction. Unit tests: 6 new cycle-check tests (self-parent short-circuits without DB call; valid parent passes; direct cycle rejected; deep multi-level cycle rejected; valid deep hierarchy allowed; asserts exactly 1 `$queryRaw` call). Total unit tests: 145 (up from 141). Manual API verification: valid parent assignment succeeds, cycle returns 400, self-parent returns 400.
- 2026-06-27 — Board type + priority filters shipped. `MultiSelectFilter` generic component reuses the LabelFilter pattern (toggle button turns brand-coloured when active, popover checklist, "Clear" footer, keyboard Escape closes). `TypeFilter` (Task/Bug/Story/Epic/Subtask) and `PriorityFilter` (Highest/High/Medium/Low/Lowest) added to the board toolbar. Filtering is client-side over the already-loaded board issues; all filters compose with AND semantics. 18 Playwright tests (9 desktop + 9 mobile) covering button visibility, single-type filter, single-priority filter, combined AND filter, multi-select, clear, and mobile viewport — all green.
- 2026-06-27 — Password reset (P1, M) shipped end-to-end.
  - **Backend:** `PasswordResetToken` Prisma model (id, userId FK, tokenHash UNIQUE, expiresAt, usedAt nullable). Only SHA-256 of the raw 32-byte token is stored. Migration `20260627021052_add_password_reset_token`. `PasswordResetService` in `apps/api/src/auth/`: `requestReset()` — anti-enumeration (always 200), invalidates prior unused tokens, creates new token with 1h TTL, calls delivery seam. `resetPassword()` — validates token (exists/not-expired/not-used) then atomically updates password (argon2) + marks token used in a `$transaction`. Pluggable `deliverResetLink()` seam: logs reset URL in dev (fallback); stub ready for SMTP when `SMTP_HOST` is set (documented in `.env.example` + inline). Auth-route `@Throttle` applied to both endpoints (brute-force protection). DTOs: `ForgotPasswordDto` (email), `ResetPasswordDto` (token, newPassword ≥6 chars, ≤200). 9 unit tests all green.
  - **Frontend:** `ForgotPasswordPage` (/forgot-password): email field → confirmation screen (same message for unknown addresses, anti-enumeration). `ResetPasswordPage` (/reset-password?token=…): reads token from URL, password + confirm fields, success → redirects to /login after 2s, missing/invalid token states. "Forgot password?" link added to `LoginPage`. API functions `forgotPassword()` + `resetPassword()` in `apps/web/src/api/auth.ts`. Routes wired in `App.tsx`.
  - **Tests:** 12 Playwright e2e tests (desktop + mobile) — link visible, anti-enumeration, missing token, invalid token, full happy-path flow, double-use rejection. 141 unit tests green. 175 e2e total green (1 pre-existing skip).
  - **P1 Ready item ticked; P2 duplicate ticked.**
- 2026-06-27 — @mention autocomplete in comment composer. Shipped `MentionComposer` — a textarea wrapper that detects `@` context per-keystroke, shows a floating `listbox` of co-members filtered by name/email, and inserts `@<email>` (matching the backend mention parser) on Enter/Tab/click. Arrow up/down navigation; Escape dismisses picker only (uses `stopImmediatePropagation` to prevent the drawer overlay from closing). `onMouseDown` on picker rows preserves textarea focus on selection (no focus-loss). `CommentsPanel` now takes `users?: UserDto[]` prop and uses `MentionComposer` in both the new-comment and edit-comment surfaces; `IssueDetailDrawer` passes `users` through. `apps/web/e2e/mention-autocomplete.spec.ts` — 16 Playwright tests green (desktop + mobile): Enter insert, Tab insert, Esc dismiss, arrow nav, click insert, no picker on normal text, notification end-to-end, focus retention per-keystroke. tsc clean, build clean.
- 2026-06-27 (Pass 4 groom) — Ingested product-auditor Pass 4, engineering-auditor Pass 4, and QA findings from the post-hardening/onboarding sprint.
  - **Ticked done [x]:** Webhook SSRF guard (DNS pre-flight + blocked IP ranges + redirect:manual + socket drain + p-limit@3 fan-out cap + WEBHOOK_ALLOW_PRIVATE opt-out + unit tests); composite pagination index `@@index([projectId,createdAt,id])` on Issue (migration `20260627012032_issue_pagination_index`); helmet() security headers; global throttler (100 req/min) + stricter auth throttle (10 req/min), both env-configurable; first-run onboarding (`OnboardingPanel` + 14 e2e tests, v1.0 criterion ticked). All confirmed by engineering-auditor Pass 4 deep-dives on the SSRF guard and cursor pagination implementation.
  - **Promoted to P1 Ready (new items):** board type + priority filters (S — highest daily-driver gap per product auditor; board has title/assignee/labels but no type/priority); @mention autocomplete in comment composer (M — notification fan-out works but the feature is hidden without a picker; users must know email addresses verbatim); password reset (M — no account recovery path at all; table-stakes for self-hosted multi-user).
  - **Added P2 (engineering, Pass 4 new findings):** add take cap to board/roadmap endpoints (S); slim planning-view endpoint to avoid useProjectIssues all-pages waterfall (M); Redis-backed webhook delivery queue / BullMQ (M — Redis already in compose, unused); Socket.io Redis adapter OR remove Redis from compose (S); batch notifyComment inserts + rebalanceAndPlace (M).
  - **Added P2 (QA hygiene):** fix remaining e2e specs using DEMO user for mutating flows — board-columns, viewer-aware, webhooks-ui still write against shared DEMO project.
  - **Added P3 (new product ideas from Pass 4):** "Team pulse" home dashboard (M); keyboard triage mode (L); per-project "Definition of Done" checklist (M). Also added: SMTP email notifications (M), VITE_API_URL build-time note (S, docs/README).
  - **Demoted:** JWT cookie migration moved P2 → P3 (no active XSS vector; helmet CSP closes the practical risk; revisit when rich-text editor lands). Redis-backed webhook queue stays P2 (not P1) because it is not user-visible and the fire-and-forget MVP is functional.
  - **Verified carry-forward (still unshipped):** assertNoParentCycle CTE refactor (P2, M); useProjectIssues waterfall (P2, M); full-text search + saved views (P2, M); observability/pino (now P3 per groomer demotion of pure-operator items); public share link (P2, M); stale socket token (P2, S); inline card status transition (P2, S); board/roadmap unbounded (P2, S); live presence indicators (P2, M); board-overview prefetch (P2, S); UI nits — theme tokens + mobile grid + InlineError + tap targets (P2, S).
  - **Inline backlog creation:** GhostRow confirmed shipped (git `60c0107`); the P2-S item flagged for verification — if the modal round-trip is fully superseded, tick done next pass.
  - **Tension resolved:** engineering auditor flagged Redis webhook queue + JWT cookie migration as P1-class pre-release; product auditor rates board filters + @mention + password reset higher for user conversion. Resolution: user-facing gaps win P1; infrastructure upgrades stay P2 with JWT cookie moving to P3.
- 2026-06-27 (Security + scale hardening pass) — Implemented all P1/P2 security hardening items flagged by both auditors.
  - **Ticked done [x]:** Webhook SSRF guard; composite pagination index; helmet() security headers; global throttler (100 req/min) + auth-route throttle (10 req/min). All 132 unit tests green. API build (tsc + nest build) clean. Instance i3 health 200.
- 2026-06-27 — First-run onboarding + key empty-state improvements. Shipped `OnboardingPanel` on the project-list empty state. Improved empty states: board columns, My Work, notifications. `apps/web/e2e/onboarding.spec.ts` — 14 Playwright tests (all green). Ticked v1.0 criterion "first-run experience isn't an empty void."
- 2026-06-26 (Pass 3 groom) — Ingested engineering-auditor Pass 3 + product-auditor Pass 3 findings.
  - **Ticked done [x]:** backlog + sprint planning view, sprint lifecycle backend (single-active enforcement + incomplete-return), story points + parent/child hierarchy, labels management + filter UI, activity log human-readable names, comment edit/delete, unit tests + GitHub Actions CI, assigneeId workspace validation, GET /users co-member scoping.
  - **Escalated to P1 (added to Ready):** Sprint TOCTOU race; sprint realtime events; input bounds; VIEWER-aware UI.
  - **Added P1 (Next):** sprint burndown + velocity reports; board sprint indicator; sprint date display; cross-project global search; "My Work" personal dashboard.
  - **Added P2:** GET /users/:id co-member guard; assertNoParentCycle CTE refactor; Dockerfile frozen-lockfile fix; command palette; inline backlog issue creation; roadmap/timeline.
  - **Added P3:** label rename/edit.
- 2026-06-26 (Pass 2 groom) — Ingested engineering-auditor Pass 2 + product-auditor Pass 2 findings.
  - **Ticked done [x]:** realtime gateway auth, tenant-FK ownership validation, role enforcement, JWT fail-fast + docker-compose + RealtimeModule bypass closure, CORS allowlist, Badge labels in drawer, drawer overlay, toast system, themed dialogs.
  - **Escalated to P0:** assigneeId workspace-member validation; GET /users cross-tenant scope.
  - **Added P1:** global exception filter; comment edit/delete UI; activity log ID → human name resolution.
  - **Added P2:** stale socket token; "My Work"; inline card status transition; public share link; plugin/webhook event system; board-overview prefetch; live presence indicators.
- 2026-06-26 (Pass 1 groom) — Major groom from auditor Pass 1 (product + engineering) and UI review.
  - **Added & escalated to P0:** realtime gateway auth, tenant-FK-ownership validation, role enforcement, fail-fast JWT secret.
  - **Added P1:** CORS allowlist, authz unit tests + CI, notifications/@mentions, tenant-isolation test harness, story-points/epics in drawer.
  - **Reprioritized:** roles enforcement P2→P0.
  - **Added P2:** full-text search, command palette, reports hub, cursor pagination, transactional move, observability, bulk ops + optimistic concurrency.
  - **Added P3:** automation rules, saved views/query DSL, onboarding + sample project.
- (initial) Seeded from UI review + roadmap.
